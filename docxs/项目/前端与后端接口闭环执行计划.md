# FoodMate 前端与后端接口闭环执行计划

> 制定日期：2026-09-15
> 适用范围：`foodmate-ui` 前端、`foodmate-api`/Application 后端接口消费者，以及 Figma 视觉落地。
> 当前原则：Figma 是颜色、字体、布局、组件状态和交互的视觉来源，但不要求页面像素级一比一；本计划不执行 105 个画板全量像素差异，也不执行花瓣像素对比。

## 当前完成批次：Proposal/Result 与 DLQ reconciliation RocketMQ 本地真实闭环（2026-09-16）

- [x] 按仓库 README 的运行约束，在 `foodmate` Docker 网络内执行 `M14ProposalResultE2ETest` 和 `M14DlqReconciliationE2ETest`，避免宿主机无法解析 Broker 广播地址 `foodmate-rocketmq-broker:10911`。
- [x] 修复 `ToolRegistryMapper` 三个查询的结果列顺序，使 `published_at`、`revision`、`skippable` 与 `ToolDefinition` record 构造顺序一致，消除布尔值被按时间类型读取的问题。
- [x] 将 PostgreSQL 只读 Tool 执行放入独立 `REQUIRES_NEW` 只读事务；SQL 失败先回滚自己的连接，外层 Proposal 事务继续写入失败审计、Inbox 和 Result。
- [x] 本地 RocketMQ E2E 共 `5/5` 通过：DLQ reconciliation `3/3`，Proposal/Result `2/2`，覆盖正常 Result、SQL 失败 Result、重复 Proposal 幂等和 DLQ 终态边界。
- [x] 受影响模块 Spotless 通过，Alibaba Checkstyle 为 `0 violations`；本批次没有修改前端页面、HTTP/SSE 协议或生产消息协议。
- [ ] 证据仍限定为本地 Docker/PostgreSQL/RocketMQ；不代表生产消息系统、跨进程重启、长稳、部署故障矩阵或付费模型链路完成。Figma 继续只做功能和代表页面检查，iconfont 继续保持 `BLOCKED`。

## 当前完成批次：Agent 接续与 checkpoint 恢复本地真实闭环（2026-09-16）

- [x] 启用 `M14ContinuationE2ETest` 与 `M14RuntimeCheckpointRecoveryE2ETest`，本地受控 E2E `4/4` 通过。
- [x] 验证 `waiting_user` 旧 Run 进入 `superseded`，新 Run 建立 `parent_run_id`、`continuation_reason` 和 `superseded_by_run_id`，旧 dispatch 出局并产生 `run.superseded` SSE Outbox 事实。
- [x] 验证新 Run 固化 revision `1` 的初始预算快照；普通消息不创建 continuation；终态 Run 不被迟到投影改写。
- [x] 验证 `run.checkpoint_saved` 事件写入 Java Inbox 后，服务端恢复入口从持久化 checkpoint 创建新的 dispatch attempt，并校验预算 revision、事件摘要和 deadline。
- [x] Chat/Agent 前端消费者定向测试 `4` 个文件、`67/67` 通过；现有真实页面继续消费 `/recover`、`/recover-from-checkpoint` 和 continuation 状态，不创建伪造会话或终态。
- [ ] 本批次没有修改 Java/前端生产代码或 HTTP/SSE 协议；证据不包含真实 Python 进程重启、RocketMQ ACK 丢失、PostgreSQL 重启、长稳和生产部署故障矩阵。
- [ ] 本批次不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 当前完成批次：Model Governance 与 DLQ Replay 本地真实接口闭环（2026-09-16）

- [x] 新增 `M11ModelGovernanceAndDlqReplayE2ETest`，使用随机临时账号、供应商、价格、预算和 DLQ 事实，覆盖真实 HTTP、Cookie/CSRF、数据库回读和清理。
- [x] 本地 HTTP E2E `2/2` 通过：Model Governance 覆盖 Admin 拒绝、Superadmin 状态变更、revision、价格/预算创建、幂等回放、治理读取和 API Key 脱敏；DLQ Replay 覆盖非 Superadmin `403`、Superadmin `queued`、幂等回放、活跃重放 `409`、原始 payload 不返回和异步 Outbox 状态。
- [x] Application 定向测试 `12/12`、API 定向测试 `8/8`、前端相关测试 `5` 个文件 `28/28` 通过；Spotless 检查已通过，本批次没有修改生产 Java、HTTP/SSE 协议或前端业务代码。
- [x] 数据库断言确认 DLQ 保持 `needs_attention`，重放 Outbox 保持 `pending`；HTTP `queued` 仅表示命令入队，不作为 Broker 重放完成或业务运行成功。
- [ ] 本批次没有触发真实 RocketMQ Broker Relay，因此不宣称 DLQ 已完成真实消息重放、原消息对账或下游 AgentRun 完成；生产消息系统、故障恢复和告警仍需单独验证。
- [ ] 本批次不改变 Retention 的 V27 清理结果表阻塞、硬删除/对象/向量清理边界、iconfont `BLOCKED` 状态，也不执行 105 个 Figma 画板全量像素差异或花瓣像素对比。

## 当前完成批次：Admin Retention 本地真实接口闭环（2026-09-16）

- [x] 新增 `M11RetentionGovernanceE2ETest`，使用随机 admin、superadmin、operator 账号和唯一软删除 Knowledge 文档，覆盖真实 HTTP、Cookie/CSRF、数据库回读和测试数据清理。
- [x] 真实 E2E `1/1` 通过：清理申请、相同幂等键回放、详情查询、preflight、法律保留、operator 审批拒绝、active hold 阻断、superadmin 释放保留、superadmin 审批和三类清理任务生成。
- [x] 业务断言确认资源已软删除且保留期已到、默认硬删除关闭、法律保留状态、任务契约和安全 preflight 摘要均由服务端返回；审批没有被当作数据已删除。
- [x] Retention Application 定向测试 `5/5`、API 定向测试 `4/4`、前端 `RetentionTab` 定向测试 `5/5` 通过；本批次没有新增 HTTP 接口或修改生产业务协议。
- [ ] 当前本地人工迁移数据库没有 `data_purge_task_results`（V27）表，本批次未执行真实硬删除或清理结果对账；测试清理对该可选表做了兼容，生产迁移仍需单独执行和 validation。
- [ ] 证据限定为当前本地 PostgreSQL/Java HTTP 和前端定向测试，不代表生产对象存储、向量索引、数据库硬删除、备份恢复或部署环境联调完成；不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续为 `BLOCKED`。

## 当前完成批次：Chat 默认历史与 Profile Memory 本地闭环收口（2026-09-16）

- [x] Chat `figma-v2` 默认 Fixture 已补齐第一页会话历史、`1 / 3` 分页和 `共 15 条会话`，不再错误显示空态；定向测试 `41/41` 通过。
- [x] 新增 `script/local/local-memory-e2e.ps1` 及契约检查，使用随机本地账号覆盖记忆候选读取、过期过滤、确认、修改、删除、数据库软删除和操作审计。
- [x] 注销清理已加强为轮询 `account_deletion_jobs`，实际本地运行确认 job `completed`、用户 `is_deleted=true`，清理错误为 `0`。
- [x] 前端 `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 通过；接口审计为 Controller `128`、浏览器接口 `121`、内部接口 `7`，页面消费者 `121/121`。
- [x] 本批次没有新增或修改 Java Controller、DTO、错误码或 HTTP/SSE 协议；新增脚本注释使用中文。
- [ ] 证据限定为当前本地 deterministic REST、数据库复核和前端定向测试，不代表生产外部依赖或长连接联调完成；不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续为 `BLOCKED`。

## 当前完成批次：Workspace/Chat/Agent 会话消息 REST 生命周期收口（2026-09-16）

- [x] 新增 `script/local/local-session-chat-e2e.ps1` 和 `local-session-chat-e2e.tests.ps1`，使用随机本地账号覆盖会话创建、列表、重命名、搜索、归档、取消归档、消息写入、消息读取、消息修改、消息删除、会话删除、回收站读取、恢复和最终删除。
- [x] 实际本地运行通过：Session `358570767127941120`，Message `358570769141207040`，ChatRun `358570769032155136`，Run 终态为 `completed`；会话最终状态为删除，清理错误为 `0`。
- [x] PowerShell 5.1 契约检查通过；Workspace/Chat/Agent 前端定向测试为 `4` 个文件、`48/48`；后端 API 定向测试为 `11/11`。
- [x] `npm.cmd run audit:api` 通过：Controller `128`、浏览器接口 `121`、内部接口 `7`，文档登记 `121`，生产消费者和页面消费者均为 `121/121`。
- [x] 本批次没有修改 Java Controller、DTO、错误码或 HTTP/SSE 协议；既有 AgentRun 终态、SSE 游标和前端真实/Fixture 隔离边界保持不变。
- [ ] 证据限定为本地 deterministic REST、代码契约和定向测试，不代表生产数据库、Runtime、消息系统、外部服务或 SSE 长连接已完成目标环境联调；不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 当前完成批次：Auth/Account 本地真实 REST E2E 收口（2026-09-16）

- [x] 重建并重启当前源码对应的 `foodmate` Docker 镜像；readiness 返回 HTTP `200`，容器状态为 `healthy`，账户注销 snake_case 请求契约已加载。
- [x] 新增 `script/local/local-account-e2e.ps1` 和 `local-account-e2e.tests.ps1`，覆盖注册、登录、当前用户、资料读取/更新、多会话列表、单会话撤销、登出、Refresh Token 轮换、旧 Token 拒绝、修改密码、旧会话失效、账户注销和清理。
- [x] 修正脚本对后端真实错误码 `AUTH_REFRESH_TOKEN_INVALID` 的断言；同时补充所有短生命周期 `HttpClient` 的释放，兼容 Windows PowerShell 5.1。
- [x] 实际本地运行通过：注册成功、资料更新成功、会话列表 `2` 条、次会话撤销成功、Refresh Token 已轮换且旧 Token 被拒绝、密码更新成功、注销 job 创建成功。
- [x] 数据库复核通过：本次注销 job 状态为 `completed`，账户 `is_deleted=true` 且 `status=disabled`；活动 Refresh Token、活动认证会话和用户个人数据活动行均为 `0`。
- [x] 账户 Application 定向测试 `16/16`、API 定向测试 `15/15`、脚本契约检查通过；前端 Auth/Account 定向测试 `4` 个文件、`37/37`。
- [x] 本批次没有新增 HTTP/SSE 协议或虚构成功状态；当前接口统计仍为 Controller `128`、浏览器接口 `121`、内部接口 `7`。
- [ ] 证据限定为本地受控 Docker/REST 和代码契约，不代表生产邮件、Cookie 域、数据库部署或其它外部依赖已完成联调；Figma 不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 当前完成批次：餐食计划本地 REST E2E 收口（2026-09-16）

- [x] 修复本地餐食计划脚本的 revision URL 构造。原请求将 `$mealPlanId?revision=1` 解析为错误变量，实际路径为 `/api/meal-plans/=1`；后端日志确认该 `500` 来自非法 `long` 路径参数，不是 Java/SQL 业务错误。
- [x] 新增 `script/local/local-meal-plan-e2e.ps1` 和 `local-meal-plan-e2e.tests.ps1`，实际覆盖注册、计划创建、详情、列表、修改、校验、保存、购物清单生成/读取、购物项勾选、进度、删除、恢复、恢复后购物清单重建和最终删除后的 `404`。
- [x] 实际本地运行通过：计划状态经历 `draft -> draft -> validated -> saved`，恢复后为 `saved`；revision 经过服务端递增，购物清单包含 `6` 项，进度返回 `3` 个可执行餐次，清理错误为 `0`。
- [x] `local-meal-plan-e2e.tests.ps1`、PowerShell 5.1 语法检查、`local-food-log-e2e.tests.ps1` 和 `local-food-log-e2e.ps1` 实际运行均通过；餐食计划 Application `12/12`、API `8/8` 定向测试通过，Maven reactor 为 `BUILD SUCCESS`。
- [x] 精确复核并软删除此前清理失效遗留的 `11` 条活动 `local-food-log-e2e` 测试记录；活动同名饮食记录和餐食计划均为 `0`。未触碰其它业务数据。
- [x] 提交 `05b52f47`：`test: add local meal plan api e2e`。本批次未修改 Java Controller、DTO、数据库结构或 HTTP/SSE 协议。
- [ ] 证据限定为本地 deterministic REST 和代码契约，不代表生产数据库、外部服务或部署环境全部联调；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 当前执行批次：本地 deterministic Agent/SSE 真实链路（2026-09-16）

- [x] 执行 `script/local/deterministic-agent-e2e.tests.ps1`，脚本契约检查通过；测试确认本地 provider、付费执行禁用、真实 ChatRun、持久化 SSE、`Last-Event-ID` 和会话清理约束均存在。
- [x] 执行 `script/local/deterministic-agent-e2e.ps1 -RunTimeoutSeconds 180`；本地 deterministic provider 成功完成注册、ChatRun 创建和 AgentRun 消费。
- [x] 实际证据：Run `358523810279854080`、Session `358523810233716736`，SSE `8` 条事件，事件 ID 唯一，唯一终态为 `run.completed`；服务端 Run 状态为 `completed`，持久化事件查询返回 `8` 条。
- [x] 使用中间 `Last-Event-ID` 回放成功返回 `1` 条后续终态事件；会话软删除清理成功，清理错误为 `0`。
- [ ] 该证据限定为当前本地 deterministic 环境，不代表云模型、生产部署、容量或故障矩阵已完成；不执行 105 个 Figma 画板全量像素差异、花瓣像素对比或付费云模型测试。

## 当前执行批次：接口覆盖与 shadcn 边界门禁复验（2026-09-16）

- [x] `npm.cmd run audit:api` 通过：Controller `128`、浏览器接口 `121`、内部接口 `7`、文档登记 `121`、生产消费者证据 `121/121`、页面消费者证据 `121/121`，状态字段不规范数量为 `0`。
- [x] 页面 shadcn 边界测试和基础 UI 组件测试共 `2` 个文件、`13/13`；未发现页面直接声明原生交互控件、直接引入 Radix 或依赖 `AdminPrimitives`。
- [x] 前端 `typecheck`、`lint`、`format:check` 和生产 `build` 均通过；构建产物正常生成，未引入 iconfont 虚构字体或映射。
- [ ] 本批次只证明代码侧接口台账和 UI 基础设施门禁通过，不代表所有外部依赖已完成目标环境联调；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比。

## 当前执行批次：后端代码契约与业务 E2E 脚本门禁（2026-09-16）

- [x] 执行 `mvnw.cmd verify`，六个 Java 模块均 `SUCCESS`，Spotless 检查通过；Application、API 和架构测试无失败，Bootstrap 环境依赖测试按配置跳过 `38` 项。
- [x] `real-food-log-e2e.tests.ps1`、`real-meal-plan-e2e.tests.ps1`、`real-nutrition-catalog-e2e.tests.ps1`、`real-memory-e2e.tests.ps1`、`real-rag-e2e.tests.ps1` 和 `real-sql-agent-e2e.tests.ps1` 的脚本契约检查全部通过。
- [x] 已确认付费业务脚本要求显式 `-ExecutePaid`、管理员凭据和预算上限；当前进程未设置付费执行凭据，因此没有执行云模型调用。
- [ ] 本批次证明后端代码契约和脚本安全门禁，不代表付费云链路、生产数据库、外部服务或目标环境故障矩阵完成；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比。

## 当前执行批次：代表页面浏览器运行复核（2026-09-16）

- [x] 实际打开 Auth、Chat、Diet Records、Intake Analysis、Meal Planning、Knowledge、Profile 和 Admin 代表路由；页面均完成渲染，字体状态均为 `loaded`。
- [x] 在 `759×698` 视口下，以上页面的 `document.documentElement.scrollWidth` 和 `document.body.scrollWidth` 均为 `759`，没有页面级横向溢出。
- [x] 控制台 warning/error 为空；Chat 用户消息使用黑色正文和浅黄色气泡，工作区、顶栏和用户消息头像均使用同一登记资源，Agent 状态方块保持独立语义。
- [ ] 本批次是代表路由的运行和可读性检查，不是 105 个画板像素级验收，不执行花瓣像素对比；iconfont 继续保持 `BLOCKED`。

## 当前执行批次：普通用户饮食记录真实接口链路（2026-09-16）

- [x] 新增 `script/local/local-food-log-e2e.ps1` 和对应契约测试，使用随机本地账号验证营养目录搜索、饮食记录写入、营养分析回读和 `revision` 删除清理。
- [x] `local-food-log-e2e.tests.ps1` 通过；实际脚本验证返回一个 `cooked` 米饭目录候选、饮食记录 `nutrition_status=matched`、分析 `matched_items=1`，并成功完成 `food_log_deleted=true`。
- [x] 本链路不读取管理员凭据、不使用 `-ExecutePaid`、不调用云模型；数据库复核确认 `local-food-log-e2e` 当前活动测试记录数量为 `0`。
- [ ] 本批次只证明普通用户本地 REST 业务链路，不代表所有浏览器接口已在目标生产环境逐条运行；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比。

## 当前执行批次：Workspace/Home Figma 快捷操作资源收口（2026-09-16）

- [x] Figma Fixture 首页的“记录饮食”“分析摄入”“创建计划”“搜索知识”已移除 Emoji，改用已登记的 Figma SVG 资源；“快速计算”没有对应实体资源，继续使用 Lucide `Calculator`。
- [x] 浏览器实际复核 `/?state=figma-v2&visual-qa=1`：视口 `759×698`、DPR `1.25`、字体 `loaded`；四个 SVG 均以 `16×16` 成功加载，页面级 `scrollWidth=759`。
- [x] Home 定向测试 `1` 个文件、`8/8`；`typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 通过；控制台没有 warning/error。
- [x] 本批次没有新增后端接口、没有改变 HTTP/SSE 协议，真实模式仍使用 Lucide/服务端契约，Fixture 与真实模式保持隔离。
- [ ] 本批次不是 105 个 Figma 画板全量像素验收，也不执行花瓣像素对比；iconfont 实体包、完整映射、来源和许可证继续保持 `BLOCKED`。

## 当前执行批次：Auth 页面组短视口滚动收口（2026-09-16）

- [x] 读取并复核 Figma Auth 节点 `647:214`、`680:216` 的结构、颜色、字体、控件高度和圆角；Auth 页面继续使用 shadcn `Button`/`Input` 与语义 Token，不从现有页面样式反推设计。
- [x] 修复 Auth 壳层在窄桌面视口被内容撑高、底部操作区不可访问的问题：`authPage` 固定在根容器高度内并保留自身纵向滚动，短视口不再依赖全局 Body 溢出。
- [x] 浏览器实测 `759×698`、DPR `1.25`、字体 `loaded`：Register Auth 容器 `clientHeight=698`、`scrollHeight=895`，Forgot `scrollHeight=827`；滚动后注册按钮和底部登录入口可见；页面级 `scrollWidth=759`，控制台 warning/error 为空。
- [x] Login、Register、Forgot Password、Reset Password 和 Token Invalid/Expired/Used 路由均稳定渲染；Auth 定向测试 `3` 个文件、`43/43`，`lint`、`format:check`、`typecheck`、`build` 和 `git diff --check` 通过。
- [ ] 本批次只收口 Auth 页面响应式容器和本地视觉/交互复核，不代表邮件、Cookie、数据库或生产外部依赖完成联调；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续为 `BLOCKED`。

## 当前执行批次：Chat Run 服务端终态消息收口（2026-09-16）

- [x] 真实 Chat 在收到 `run.completed` 后重新读取当前会话消息，按 `sequence_no` 排序并写回页面消息列表；当前 Run 的 assistant 消息 ID 从同一批服务端消息中恢复。
- [x] 只有服务端确实返回当前 Run 的 assistant 消息时才清除临时流式文本；服务端暂未返回 assistant 时保留已有文本，避免把最终一致性延迟显示成空回答。
- [x] 新增回归测试覆盖“临时流式文本被服务端最终消息替换”；没有新增后端路由、没有改变 HTTP/SSE 协议，也没有把 HTTP 接受响应当作运行终态。
- [x] 浏览器稳定复核六个独立状态入口：写入确认、预算上限、可重试工具失败、安全降级、用户取消和 SSE 第 `2/5` 次重连；重连页保留已展示文本，控制台 warning/error 为空。
- [x] Chat/Agent 定向测试 `3` 个文件、`43/43`；`format:check`、`lint`、`build`、`audit:api` 和 `git diff --check` 通过。当前接口审计保持 Controller `128`、浏览器接口 `121`、内部接口 `7`、生产消费者证据 `121/121`。
- [ ] 本批次只收口前端终态消息投影和本地可验证状态，不代表生产数据库、Runtime、消息系统、外部服务或长连接已完成联调；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续为 `BLOCKED`。

## 当前执行批次：Workspace/Home 移动导航收口（2026-09-16）

- [x] `WorkspaceLayout` 在 `680px` 以下提供移动端导航按钮，并使用现有 shadcn `Sheet` 承载完整侧栏内容。
- [x] 移动导航复用真实会话、饮食工具、知识库、设置和账户入口；导航跳转后自动关闭 Sheet，没有新增接口或 Fixture 成功状态。
- [x] `SidebarSessionList` 增加导航关闭回调，会话列表和 Agent 对话入口在移动 Sheet 内保持可访问。
- [x] 浏览器实际复核 `/?state=figma-v2&visual-qa=1`：`640×698`、DPR `1`、字体 `loaded`、页面和文档 `scrollWidth=640`；打开 Sheet 后点击“知识库”跳转至 `/knowledge` 并关闭 Sheet。
- [x] 浏览器控制台 warning/error 为空；WorkspaceLayout 定向测试 `25/25`，前端全量测试 `72` 个文件、`577/577`。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 通过；接口审计为 Controller `128`、浏览器接口 `121`、内部接口 `7`、生产消费者证据 `121/121`。
- [x] `mvnw.cmd verify` 在 Java `21.0.9` 下 `BUILD SUCCESS`，Spotless 通过；Bootstrap 测试 `60` 项中 `38` 项按环境配置跳过。
- [ ] 本批次只收口移动工作区壳层，不代表生产外部依赖全部联调完成；Figma 继续采用代表页面结构、可读性和响应式复核，不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续为 `BLOCKED`。

## 当前执行批次：Chat 历史事件与持久化 SSE 游标收口（2026-09-16）

- [x] Chat 历史事件接口 `/api/chat/runs/{runId}/events` 现在同时返回 Runtime `event_id` 和持久化 SSE `sse_event_id`；前者用于运行事件标识，后者用于 `Last-Event-ID` 游标恢复。
- [x] PostgreSQL Runtime 事件查询已关联 `agent_run_sse_outbox`，按 `source_event_key={agent_run_id}:{event_id}` 找回对应 SSE 游标；内存模式也保留稳定的 `sse_memory_...` 游标，避免恢复逻辑退化为 Runtime ID。
- [x] 前端历史 Run 恢复优先使用 `sse_event_id`，兼容后端下划线和 camelCase 字段；SSE 重连继续使用 `Last-Event-ID`、`sse_event_id` 去重，Runtime `event_id` 与 SSE `sse_event_id` 不再混用。
- [x] Agent/Chat/SSE 定向测试 `4` 个文件、`37/37` 通过；Application 定向测试 `5/5`、API 定向测试 `4/4` 通过；Infrastructure 编译通过，`mvnw.cmd verify` 为 `BUILD SUCCESS`。
- [x] 当前接口审计保持 Controller `128`、浏览器接口 `121`、内部接口 `7`，生产消费者证据 `121/121`；本批次修改既有 Chat 历史响应字段，不新增浏览器接口数量。
- [x] 当前 `V1AgentRunController` 已提供 `POST /api/agent-runs/{runId}/tool-proposals/{proposalId}/skip`；早期批次中“工具跳过接口缺失”的记录仅保留为历史证据，不代表当前 Controller 状态。
- [ ] Infrastructure 外部依赖测试按项目配置跳过的项目，仍不等同于生产数据库、Runtime、消息队列、外部服务和长连接联调完成；本批次不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续为 `BLOCKED`。

## 当前执行批次：Workspace 与 Chat Run 消息归属修复（2026-09-16）

- [x] 修复 `useRealAgentReplay` 的历史消息判断：只有最新 `agent_run_id` 对应的 assistant 消息才会被视为已持久化，旧 Run 的回答不会阻断新 Run 的 SSE 流式文本。
- [x] 新增回归测试覆盖同一会话中“旧 Run 已有 assistant、新 Run 只有 user 消息”的恢复场景，并确认新 Run 的流式回答可以正常进入页面状态。
- [x] Chat/Agent 定向验证：7 个测试文件、`78/78` 个用例通过；前端全量验证：71 个测试文件、`566/566` 个用例通过。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 通过；接口审计保持 Controller `128`、浏览器接口 `121`、内部接口 `7`、生产消费者证据 `121/121`。
- [x] `.\mvnw.cmd verify` 通过，Java 全模块构建成功；本批次没有修改 Java Controller、DTO、错误码、数据库或 HTTP/SSE 协议。
- [ ] 本批次只修复 Run 与历史消息的前端归属边界，不代表生产环境长连接、数据库、Runtime、外部依赖或部署全部联调完成；不执行 105 个 Figma 画板全量像素差异和花瓣像素对比，iconfont 继续为 `BLOCKED`。

## 当前执行批次：Profile 安全会话真实状态收口（2026-09-16）

- [x] 真实 Profile 安全页继续使用既有 `GET /api/users/me/sessions`；会话行的最近活动只读取后端 `last_seen_at`，不再根据 `device_id` 猜测活跃时长。
- [x] 真实模式不再展示没有后端查询接口支持的 Figma 安全活动示例，改为明确的安全活动历史空态；Fixture 模式继续保留设计稿示例，两个模式没有互相污染。
- [x] 新增回归测试覆盖后端时间字段、Fixture 示例隔离和安全活动空态；Profile 定向验证 `2` 个文件、`42/42` 个用例通过。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 均通过；接口审计保持 Controller `128`、浏览器接口 `121`、内部接口 `7`、生产消费者证据 `121/121`。
- [x] 实现提交：`eae812bd`。本批次没有新增或修改 Java Controller、DTO、错误码或 HTTP/SSE 协议。
- [ ] 本批次只证明代码契约和本地前端行为；生产安全活动审计接口、数据库、Cookie 和部署环境不存在新增证据。Figma 继续按代表页面复核，不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续为 `BLOCKED`。

## 执行更新：代表页面可读性与响应式边界复核（2026-09-16）

- [x] Chat 代表页 `/chat?state=figma-v2&visual-qa=1` 在 `759×698`、DPR `1.25` 下字体状态为 `loaded`，页面级 `scrollWidth=759`；用户气泡实际前景为 `rgb(0,0,0)`，表面为 `rgb(255,246,226)`。
- [x] Chat 工作区侧栏、顶栏和用户消息的运行时头像均指向 `/assets/avatars/default-male.svg`；绿色 Agent 方块保留为独立状态标识，不作为人物头像。
- [x] Admin 代表页 `/admin?state=figma-v2&visual-qa=1` 在 `1280×720`、DPR `1.25` 下字体状态为 `loaded`，页面级 `scrollWidth=1280`；时间、结果、降级和搜索控件不存在几何重叠。
- [x] Admin 运行表格使用自身 `overflow-x: auto` 容器（容器宽 `956px`、内容宽 `1116px`），横向滚动不会扩展页面宽度。
- [x] 本批次只完成代表页面的结构、可读性、头像一致性和响应式边界复核；没有把人工复核写成像素级 `PASS`，不执行 105 个画板全量像素差异或花瓣像素对比。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源、版本、许可证和 glyph-Figma 映射仍未提供，资源登记继续保持 `BLOCKED`。

## 当前执行批次：Admin 用户凭证重置真实契约闭环（2026-09-16）

- [x] 新增 `POST /api/admin/users/{id}/credentials/reset`；仅 `admin/superadmin` 可用，沿用确认摘要、`revision`、`Idempotency-Key`、目标账户版本校验和操作审计。
- [x] Application 生成一次性密码重置令牌并通过 `PasswordResetNotifier` 投递；PostgreSQL 保存令牌哈希，响应只返回 `requested` 和新 `revision`，不返回令牌或密码。
- [x] 凭证重置使用 `AdminManagementRepository.revokeSessions` 的原子操作，同时递增账户 `revision`、撤销目标账户认证会话和刷新令牌；账户版本递增不再被单独当作会话失效证据。
- [x] SMTP 通知适配器归属 Infrastructure；通知服务不可用或发送失败返回 `COORDINATION_UNAVAILABLE`，不把令牌生成包装成成功。
- [x] `UsersSection` 真实模式接入正式 service 和 Admin 操作确认状态机；Fixture 仍只展示视觉预览，不发送真实请求。
- [x] Application 定向验证 `9/9`，API 定向验证 `3/3`；前端 `adminExtendedService`、`UsersTab.real` 共 `12/12`；受控数据库 E2E `M11AdminManagementE2ETest` 为 `2/2`；当前接口统计为 Controller `127`、浏览器接口 `120`、内部接口 `7`。
- [x] 最终门禁已完成：Java `21.0.9` 下 `mvnw.cmd verify` 为 `BUILD SUCCESS`；Shared `12/12`、Application `268/268`、Infrastructure `119`（跳过 `20` 个环境测试）、API `96/96`、Bootstrap `60`（跳过 `38` 个环境测试），Spotless clean。
- [x] 前端全量 Vitest `71` 个文件、`562/562` 通过；`typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 均通过。本批次不执行 105 个 Figma 全量像素差异或花瓣像素对比，`iconfont` 继续为 `BLOCKED`。

## 当前执行批次：Admin Runs 结果与降级筛选真实契约闭环（2026-09-16）

- [x] 在既有 `GET /api/admin/queries/{resource}` 契约上补充 `result_type`、`error_code` 和 `degraded` 查询参数；没有新增虚构路由或改变权限边界。
- [x] `agent_runs.result_type`、`agent_runs.error_code` 已通过 Application 查询端口、PostgreSQL Mapper 和响应 DTO 传递到浏览器；运行响应新增 `result_type`、`error_code` 和 `degraded` 字段。
- [x] `degraded=true` 只匹配 `result_type=safety_degraded`；`degraded=false` 匹配非降级结果（包括尚未产生结果类型的运行），不再使用错误码推断降级。
- [x] Admin Runs 真实模式启用结果类型和错误码筛选；真实结果类型使用后端实际值 `normal`、`safety_degraded`，Fixture 继续保留自己的展示数据。
- [x] Admin Overview 真实模式启用降级筛选，并把筛选条件提交给服务端；表格结果类型、错误码和降级标记均来自服务端响应。
- [x] Application 定向验证 `AdminOperationalQueryServiceImplTest` 为 `10/10`，API 定向验证 `AdminOperationalQueryControllerTest` 为 `6/6`；前端定向验证 3 个文件、`21/21`，`typecheck` 通过。
- [x] 本批次已完成完整 Maven `verify`；Shared、Application、Infrastructure、API 和 Bootstrap 均通过，Bootstrap 测试 `59` 项中 `37` 项按环境跳过、无失败。本批次不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，`iconfont` 继续保持 `BLOCKED`。

## 当前执行批次：Diet Records、Intake Analysis 与 Meal Planning 证据收口（2026-09-16）

- [x] 前端定向验证覆盖 `foodLogService`、`nutritionFoodService`、`compositeDishService`、`analysisService`、`planningService` 及三个页面的 Fixture/真实模式测试，共 `11` 个文件、`100/100` 个用例通过。
- [x] 后端 API 定向验证覆盖 `FoodLogControllerTest`、`CompositeDishControllerTest`、`NutritionFoodControllerTest`、`NutritionAnalysisControllerTest` 和 `MealPlanControllerTest`，共 `21/21` 个用例通过。
- [x] 后端 Application 定向验证覆盖饮食记录、复合菜、营养食品、营养分析、餐食计划和写入审批相关服务，共 `8` 个测试类、`61/61` 个用例通过；Maven reactor 构建成功。
- [x] 实际浏览器复核 `/analysis?view=records&state=figma-v2&visual-qa=1`、`/analysis?state=figma-v2&visual-qa=1` 和 `/planning?state=figma-v2&visual-qa=1`；视口为 `759×698`、DPR `1.25`，三个页面字体状态为 `loaded`，页面级 `scrollWidth` 均为 `759`。
- [x] Diet Records 可见营养指标、餐次、待确认记录和详情操作；Intake Analysis 可见 `7 天`、`30 天`、`90 天`、自定义范围、全部餐次和数据质量说明；Meal Planning 可见计划摘要、五日餐表和内部横向滚动日程。
- [x] Meal Planning 窄视口的日程容器保持 `720px` 内容宽度并在自身区域滚动，未造成页面级横向溢出；真实模式接口参数和服务端状态语义未改变。
- [x] 本批次没有新增后端路由、DTO、错误码或 HTTP/SSE 协议；购物清单生成接口继续不携带未定义的 `Idempotency-Key`，购物项更新继续使用既有幂等契约。
- [x] 本批次只完成代码契约、定向测试和代表页面可读性复核，不执行 105 个 Figma 画板全量 PNG/diff，不执行花瓣像素对比；`iconfont` 资料不完整时继续保持 `BLOCKED`。

## 当前执行批次：Knowledge、Profile 与密码重置契约口径收口（2026-09-16）

- [x] 前端用户 Knowledge/Profile 定向验证覆盖 `10` 个文件、`81/81` 个用例；Admin Knowledge 的 `KnowledgeTab`、真实模式和控制项验证覆盖 `3` 个文件、`11/11` 个用例。
- [x] 后端 API 定向验证覆盖 Knowledge、Knowledge Search、账户、会话和请求 JSON 契约，共 `5` 个测试类、`19/19` 个用例通过。
- [x] 后端 Application 定向验证覆盖个人数据、账户、记忆、会话摘要、知识检索、上传、索引结果和 outbox，共 `9` 个测试类、`55/55` 个用例通过；Maven reactor 构建成功。
- [x] 实际浏览器复核 `/knowledge?state=figma-v2&visual-qa=1` 和 `/profile?state=figma-v2&visual-qa=1`；视口 `759×698`、DPR `1.25`、字体 `loaded`、页面级 `scrollWidth=759`。
- [x] Knowledge 用户搜索、主题筛选、引用结果和当前引用详情可见；Profile 基础资料、头像入口、记忆/偏好、安全设备和数据隐私入口可见；默认人物头像仅使用 `/assets/avatars/default-male.svg`。
- [x] Knowledge 批次上传、发布、下线、恢复、删除、重试、重索引及进度 SSE 的真实 service/Tab 测试均通过；真实写操作继续以服务端回读为最终状态，Fixture 不伪造成功结果。
- [x] 已核正接口口径：用户自助密码重置使用既有 `/api/auth/password-reset/request` 和 `/api/auth/password-reset/confirm`，前端 `authService`、忘记密码页和重置密码页均有消费者。
- [x] Admin 用户详情凭证重置已由 `POST /api/admin/users/{id}/credentials/reset` 和真实页面消费者覆盖；通知不可用时保持稳定失败，不伪造成功。
- [ ] Agent “跳过单个工具步骤”仍没有公开接口；前端继续只展示说明，不发送后端未声明的请求。本批次不执行 105 个 Figma 全量像素差异或花瓣像素对比，`iconfont` 继续为 `BLOCKED`。

## 当前执行批次：Admin 全域接口与页面消费者复核（2026-09-16）

- [x] Admin 前端定向验证覆盖 `25` 个测试文件、`127/127` 个用例，包含概览、用户、运行、工具、用量、知识库、删除资源、数据保留、操作审计和模型治理页面/服务。
- [x] Admin API 定向验证覆盖 `11` 个 Controller 测试类、`33/33` 个用例；Application 定向验证覆盖 `12` 个测试类、`61/61` 个用例，Maven reactor 构建成功。
- [x] 实际浏览器复核 `/admin?state=figma-v2&visual-qa=1`、`/admin/users?state=figma-v2&visual-qa=1`、`/admin/knowledge?state=figma-v2&visual-qa=1`、`/admin/tools?tab=registry&state=figma-v2&visual-qa=1`、`/admin/data-retention?state=figma-v2&visual-qa=1` 和 `/admin/usage?state=figma-v2&visual-qa=1`；视口 `759×698`、DPR `1.25`、字体 `loaded`、页面级 `scrollWidth=759`。
- [x] 已核对 Admin 查询、权限拒绝、确认摘要、revision、幂等键、取消、错误和服务端回读边界；真实模式不以 Fixture 状态替代接口结果。
- [x] 修复 Usage 页窄桌面视口筛选布局：三个固定宽度筛选项与搜索框在内容区不足时换行，验证后筛选组无内部宽度溢出和相邻控件重叠；宽表仍只在自身滚动容器内横向滚动。
- [ ] 本批次未新增后端路由、DTO 或协议；Admin 用户重置凭证、Runs 结果类型/错误码筛选等后端缺失能力继续保持 `BACKEND_CONTRACT_MISSING`，不执行 105 个 Figma 全量像素差异或花瓣像素对比，`iconfont` 继续为 `BLOCKED`。

## 当前执行批次：Auth、Workspace 与 Chat 真实消费者复核（2026-09-16）

- [x] 实际浏览器复核 `/login?visual-qa=1`、`/?state=figma-v2&visual-qa=1` 和 `/chat?state=figma-v2&visual-qa=1`；登录表单、工作区导航/会话/任务入口、Chat 用户消息/写入确认卡/运行轨迹/发送入口均可访问。
- [x] 前端 Auth、Session、Workspace、Chat、ChatRun、Agent/SSE 定向验证共 `12` 个文件、`173/173`；覆盖加载、空态、错误、取消、会话切换、迟到响应、消息编辑/删除、审批、预算、重试、checkpoint 和 SSE 重连。
- [x] 后端 API 定向 Controller 验证共 `21/21`，Application Agent/Account/Session/Recovery 验证共 `19/19`；Java 测试使用 `-am` 构建共享契约，避免把类路径问题误判为业务失败。
- [x] `npm run audit:api` 通过：Controller `126`、浏览器接口 `119`、内部接口 `7`、生产消费者证据 `119/119`；浏览器端没有新增未消费的 Auth、Session、Chat 或 Agent 接口。
- [x] Chat/Agent 继续遵守接受响应不等于终态、`Last-Event-ID`/事件去重、终态关闭、取消关闭和会话切换隔离；六个状态 Fixture 不会覆盖真实模式响应。
- [x] 用户自助密码重置已由 `/api/auth/password-reset/request`、`/api/auth/password-reset/confirm` 和对应前端页面/service 真实消费。
- [ ] Admin 用户详情重置凭证和跳过单个工具步骤仍没有公开契约；前端保持禁用/说明状态，不创建虚构请求，分别继续登记为 `BACKEND_CONTRACT_MISSING`。
- [x] 本批次完成代码消费者和代表路由复核；不代表邮件、Cookie、数据库、Runtime、云模型和生产部署全部联调完成，也不执行 105 个 Figma 画板全量像素差异或花瓣像素对比。

## 当前执行批次：Meal Planning 购物清单接口契约核对（2026-09-16）

- [x] 核对 `POST /api/meal-plans/{mealPlanId}/shopping-list` 的 Controller、Application service、Mapper 和数据库基线；该接口没有声明 `Idempotency-Key`，由服务端复用当前有效清单实现重复调用的业务幂等。
- [x] 核对 `PATCH /api/meal-plans/{mealPlanId}/shopping-list/items/{shoppingListItemId}`；购物项更新才是显式幂等接口，前端继续发送 `Idempotency-Key`，没有把两个不同契约混为一谈。
- [x] `planningService.createShoppingList` 保持不发送未定义的幂等 Header；Planning 真实页面已有同步操作锁、AbortSignal、请求序号和失败清空旧数据行为。
- [x] 前端 `planningService.test.ts`、`PlanningPage.real.test.tsx` 共 `23/23` 通过；后端 `MealPlanControllerTest` 共 `8/8` 通过，其中生成购物清单测试不携带幂等 Header。
- [x] 发现并登记后端并发边界：`shopping_lists` 当前只有 `(meal_plan_id, is_deleted)` 普通索引，没有 active 清单唯一约束；当前批次不擅自新增 HTTP Header、数据库迁移或协议字段，后续如要求抗并发重复创建，需单独设计数据库锁/唯一约束和重复请求回读策略。
- [x] 本批次契约核对完成；该结论不扩展为生产数据库、部署环境或并发压测已完成，也不执行 105 个 Figma 画板全量像素差异或花瓣像素对比。

## 当前执行批次：Intake Analysis Fixture 范围筛选契约收口（2026-09-16）

- [x] 重新读取 Figma 节点 `640:773` 的范围筛选组，确认 Fixture 只展示 `7 天`、`30 天`、`90 天`、`自定义范围` 和 `全部餐次`。
- [x] Analysis Fixture 隐藏真实模式专用的 `今天` Tab；真实模式继续保留 `today` 请求和后端返回能力。
- [x] `AnalysisPage.test.tsx` 验证 `state=v2`、`state=figma-v2` 均只渲染三个时间范围 Tab；真实模式测试继续覆盖 `今天` 请求。
- [ ] 本批次只做 Figma 代表页面结构和可读性收口，不执行 105 个画板全量 PNG/diff，不执行花瓣像素对比；iconfont 继续保持 `BLOCKED`。

## 当前执行批次：Meal Planning Fixture 窄视口卡片可读性收口（2026-09-16）

- [x] 读取 Figma 节点 `640:974`、`640:988` 的计划容器和日程网格意图；保留 32px 页面内边距、24px 区块间距和连续横向餐食网格。
- [x] `DefaultPlanningView` 增加 Fixture 专用布局标识；仅在 Fixture 模式下为日程区启用内部横向滚动和 `720px` 最小网格宽度，真实模式不改变布局或接口行为。
- [x] `state=v2` 与 `state=figma-v2` 均覆盖 Planning Fixture 的布局 class 和 Analysis Fixture 的兼容入口；定向测试共 `2` 个文件、`24/24` 通过。
- [x] 浏览器复核 `/planning?state=figma-v2&visual-qa=1`，视口 `759×698`、DPR `1.25`：页面级 `scrollWidth=759`，日程区 `scrollWidth=720`，餐食卡片宽度 `112px`，内部滚动可见。
- [x] `lint`、`format:check`、`typecheck`、`build`、`audit:api` 和 `git diff --check` 均通过；本批次没有修改 Java Controller、DTO、错误码或 HTTP/SSE 协议。
- [x] 本批次不执行 105 个画板全量 PNG/diff，不执行花瓣像素对比；本记录属于代表页面结构和可读性复核，iconfont 资料不完整时继续保持 `BLOCKED`。

## 当前执行批次：Figma Fixture 参数与短视口侧栏布局收口（2026-09-16）

- [x] Home、Analysis、Diet Records、Meal Planning 和 Chat 统一兼容 `state=v2` 与当前验收链接使用的 `state=figma-v2`，避免同一 Fixture 链接进入不同页面壳层。
- [x] Figma Fixture 侧栏在短视口下改为自然高度排列并允许整体纵向滚动，会话列表、饮食导航和账户区不再发生矩形重叠；真实模式侧栏 flex 行为保持不变。
- [x] 定向验证覆盖 `figmaFixture.test.ts`、`WorkspaceLayout.test.tsx` 和 `DietRecordsPage.test.tsx`，共 `3` 个文件、`37/37` 个用例通过。
- [x] 浏览器复核 `/analysis?view=records&state=figma-v2&visual-qa=1`，视口 `759×698`、DPR `1.25`；通过 `getBoundingClientRect()` 检查侧栏区域顺序，并滚动确认账户区可见。
- [x] 本阶段未修改 Java Controller、DTO、错误码或 HTTP/SSE 协议；`lint`、`format:check`、`typecheck`、`build`、`audit:api` 和 `git diff --check` 均已通过。
- [x] 本阶段已独立提交：`6950c1e4`（`fix(frontend): normalize figma fixture query state`）。
- [ ] 本阶段不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 资料不完整时继续保持 `BLOCKED`。

## 当前执行批次：Admin Runs 真实查询筛选契约收口（2026-09-16）

- [x] 核对 `AdminOperationalQueryController`、Application Request 和前端 `loadAdminRunsPage` 的真实查询字段；真实模式只提交后端已支持的分页、查询文本和 `status`，不再因本地 Fixture 筛选项触发无效请求。
- [x] 真实 Run 行的 `resultType` 不再使用 `status` 冒充；后端当前未返回结果类型，页面显示占位值并保留真实运行状态字段。
- [x] 真实模式禁用结果类型和错误码筛选，并通过控件说明后端当前未提供对应筛选契约；Fixture 模式仍保留本地状态预览。
- [x] `RunsTab.test.tsx` 与 `RunsTab.real.test.tsx` 定向验证 `8/8` 通过；覆盖 DLQ、卸载取消、权限和真实契约边界。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 通过；接口审计为 Controller `126`、浏览器接口 `119`、内部接口 `7`，生产消费者证据 `119/119`。
- [ ] 结果类型和错误码筛选保持 `BACKEND_CONTRACT_MISSING`，后续只有后端补充响应字段、查询参数、权限和测试后才能开放；本批次未修改 Java 接口或 HTTP 协议。
- [ ] 本批次不代表生产数据库、鉴权、外部依赖和部署环境联调完成；Figma 仍只做代表页面视觉复核，不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 当前执行批次：Auth、Workspace/Home 与 Chat 代表页面复核（2026-09-16）

- [x] 读取 Figma `647:214`、`640:256` 和 `983:3` 的设计上下文；登录页保留 400px 表单、52px 控件和 Figma Auth 语义颜色，Workspace 保留 260px 侧栏、工作台卡片层级，Chat 保留消息操作面板结构。
- [x] 读取 Auth 节点的 Motion 上下文；设计稿动画时间线为 `4500ms` 循环，当前代码已有 GSAP 实现，`visual-qa=1` 下按验收约定关闭动态干扰，没有引入新的动画或虚构状态。
- [x] 实际浏览器复核 `/login?visual-qa=1`、`/?state=figma-v2&visual-qa=1` 和 `/chat?state=figma-v2&visual-qa=1`；字体状态为 `loaded`，当前视口 `759×698`、DPR `1.25`，三个页面均无页面级横向溢出。
- [x] 登录页输入框使用 Figma 的半透明白色表面和 52px 高度；Workspace 任务状态面板使用 `#fcfcfa`、`#e3ede3` 和 `14px` 圆角；Chat 用户气泡使用 `#fff6e2` 表面和黑色正文。
- [x] Workspace 侧栏、顶栏和 Chat 用户消息的运行时人物头像均为 `/assets/avatars/default-male.svg`；绿色 Agent 方块继续带有独立的 `agent-status-marker` 语义，不作为人物头像。
- [x] Auth、Home、Chat 和 WorkspaceLayout 定向验证共 `6` 个文件、`131/131` 个用例通过；`npm run audit:api` 重新确认 Controller `126`、浏览器接口 `119`、内部接口 `7`、生产消费者证据 `119/119`。
- [x] 本批次没有修改 Java Controller、DTO、错误码或 HTTP/SSE 协议，也没有因为视觉复核新增页面代码；本批次不执行 105 个画板全量像素差异或花瓣像素对比。
- [ ] 本批次的浏览器视口复核不等同于生产 Cookie、数据库、外部依赖和部署环境联调；iconfont 实体包、CSS/Unicode 映射、来源、版本和许可证仍不完整，继续保持 `BLOCKED`。

## 当前执行批次：接口覆盖基线与历史状态收口（2026-09-15）

- [x] 核对 `126` 个 Controller 映射、`119` 个浏览器接口和 `7` 个内部接口；接口审计确认 `119/119` 个浏览器接口具有生产消费者证据。
- [x] 核对显式 checkpoint 恢复、模型治理价格/预算和数据保留三组历史遗留项；当前均已由真实页面和 service 接入，不再作为当前 `SERVICE_ONLY` 工作项。
- [x] Agent/Model Governance/Retention 定向验证共 `6` 个测试文件、`43/43` 个用例通过。
- [x] 本批次不修改后端协议；内部接口继续只由 Java/Python Runtime 闭环，不向浏览器暴露。
- [ ] 本批次只完成接口基线和文档状态收口，不代表生产数据库、外部依赖或云模型已经全部联调；Figma 继续按代表页面人工复核，iconfont 保持 `BLOCKED`。

## 当前执行批次：Auth 与 Account 请求契约收口（2026-09-15）

- [x] 为 `PasswordChangeRequest`、`PasswordResetConfirmRequest` 和 `DeletionRequest` 补充 `SNAKE_CASE` JSON 命名策略，使后端与现有前端的 `current_password`、`new_password` 和 `confirmation` 请求字段一致。
- [x] `apiClient` 保留后端 `ErrorBody.details`，认证和账户请求不再丢失字段级校验上下文；没有新增 HTTP 路由或改变错误码协议。
- [x] 前端认证、账户 service、API client 和 Auth 页面定向验证 `4` 个测试文件、`51/51` 通过；后端 `P1AccountControllerTest` `9/9` 和账户请求 JSON 契约测试 `3/3` 通过。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api`、Java Spotless 和 `git diff --check` 通过；接口审计仍为 `126/119/7`，生产消费者证据为 `119/119`。
- [ ] 本批次只证明账户请求代码契约和定向页面行为，不代表邮件服务、生产 Cookie、数据库、对象存储和部署环境联调完成；Figma 不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 当前执行批次：deterministic Agent Runtime 与 SSE 真实成功链路（2026-09-15）

- [x] 修正 PowerShell SSE 验证脚本的异步响应流读取方式；PowerShell 解析和 deterministic E2E 契约测试通过。
- [x] 在本地 Docker Java、Python Runtime、PostgreSQL、Redis、RocketMQ 和 MinIO 环境中完成注册、`POST /api/chat/runs`、AgentRun SSE 和终态验证。
- [x] Run `358252540950745088` 最终状态为 `completed`；SSE 收到 `8` 个事件，唯一终态为 `run.completed`，持久化事件数为 `8`。
- [x] 使用 `Last-Event-ID` 从终态前一个事件回放，返回 `1` 个 `run.completed`，事件 ID 未重复；会话软删除成功，Runtime 配置恢复无清理错误。
- [ ] 证据限定为本地 deterministic/stub 环境，不代表真实云模型、生产部署、长稳或全部浏览器接口完成环境联调；不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 保持 `BLOCKED`。

## 当前更新：2026-09-15 Agent 状态页操作切换隔离

- [x] Agent 真实状态页在 query/Run 切换时主动中止旧 Approval、预算、重试和 checkpoint 操作，并清理旧 Proposal 幂等键。
- [x] 旧操作即使在中止后迟到返回，也不会把审批状态、执行结果或操作提示写入新的状态页；旧 SSE 关闭和新状态初始化保持不变。
- [x] 新增路由切换回归测试，明确验证旧 Approval `AbortSignal` 已中止且不会继续调用执行接口；Agent/Chat 相关 6 个测试文件、`67/67` 通过。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 均通过；接口统计仍为 `126/119/7`，生产消费者证据仍为 `119/119`。
- [ ] 本大点证明前端 Agent 状态生命周期和 SSE/HTTP 隔离边界，不代表生产 Runtime、数据库、长连接或云依赖全部联调；不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 保持 `BLOCKED`。

## 当前更新：2026-09-15 Workspace 回收站恢复服务端回读

- [x] Workspace 真实模式恢复已删除会话后重新读取 `/api/sessions/deleted`，不再仅通过本地过滤从回收站移除会话；活动列表继续以 `/api/sessions` 回读结果为准。
- [x] 恢复流程沿用同一个操作取消信号，回收站读取或活动列表刷新失败时不会展示“已恢复”的成功提示，并保留可重试错误状态。
- [x] 新增 Workspace 真实页面回归测试，覆盖恢复请求、回收站第二次读取和活动列表刷新；Workspace、Session、Chat 定向验证共 4 个文件、`57/57` 通过。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 均通过；接口统计仍为 `126/119/7`，生产消费者证据仍为 `119/119`。
- [ ] 本大点仅证明前端 Workspace/Chat 状态和服务端回读边界，不代表生产数据库、鉴权、部署或外部依赖全部联调；Figma 继续按代表页面复核，iconfont 保持 `BLOCKED`。

## 当前更新：2026-09-15 Account/Profile 设备撤销服务端回读

- [x] Profile Security 真实模式的单设备撤销成功后改为触发 `/api/users/me/sessions` 服务端重新读取，页面不再仅用本地过滤结果宣告设备已移除。
- [x] Fixture 模式继续保留本地状态预览；真实模式成功提示明确为“正在刷新设备列表”，最终设备状态以服务端响应为准。
- [x] 新增真实页面回归测试，覆盖首次返回两台设备、撤销指定设备、服务端第二次返回单台设备的完整行为；`ProfilePage.account.real.test.tsx` 定向测试 `14/14` 通过。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 均通过；接口统计仍为 `126/119/7`，生产消费者证据仍为 `119/119`。
- [ ] 本大点仅证明前端页面请求生命周期和服务端回读边界，不代表生产 Cookie、数据库、多设备部署或其它外部依赖已经全部联调；iconfont 继续保持 `BLOCKED`。

## 当前更新：2026-09-15 Admin 工具注册表真实状态回读收口

- [x] `ToolRegistrySection` 的真实模式写操作成功回调不再直接修改本地工具列表；状态只由父级 `refreshNonce` 触发的服务端重新读取提供。
- [x] Fixture 模式继续保留本地状态更新，用于 Figma 操作状态预览；没有改变真实接口、请求字段、revision 或操作确认流程。
- [x] 新增真实模式回归测试：初始工具为“已启用”时，手动执行成功回调仍保持“已启用”；触发服务端回读返回“已停用”后页面才更新为“已停用”。
- [x] `ToolsTab.real.test.tsx` 定向验证 `4/4`；`typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 均通过。
- [x] 接口审计保持 `126` 个映射、`119` 个浏览器接口、`7` 个内部接口，生产消费者证据保持 `119/119`。
- [ ] 本大点未修改 Java 后端、Controller、DTO、错误码或 HTTP/SSE 协议；不代表外部依赖或生产环境联调完成，iconfont 继续保持 `BLOCKED`。

## 当前更新：2026-09-15 Knowledge 管理写操作真实状态回读收口

- [x] 知识库文档发布、下线、恢复草稿和删除的真实模式成功回调不再直接修改本地文档列表；状态只由 `refreshNonce` 触发的服务端读取提供。
- [x] Fixture 模式保留本地状态预览；真实模式仍使用既有 visibility 接口、权限、取消信号和操作确认流程，没有新增后端路径。
- [x] 新增真实模式回归测试，验证服务端回读前仍显示原状态，服务端返回新 visibility 后页面才更新。
- [x] Knowledge 定向验证 3 个测试文件、`11/11`；`typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 均通过。
- [ ] 本大点未修改 Java 后端或 SSE 协议，不代表 RAG、对象存储和生产环境联调完成；iconfont 继续保持 `BLOCKED`。

## 当前更新：2026-09-15 Java 21 后端契约门禁

- [x] 使用 Java `21.0.9` 执行 `mvnw.cmd verify`，Maven reactor `BUILD SUCCESS`；Shared、Application、Infrastructure、API 和 Bootstrap 的 Spotless 检查全部通过。
- [x] 模块测试结果：Shared `12` 个通过，Application `264` 个通过，Infrastructure `119` 个（其中 `20` 个跳过），API `91` 个通过，Bootstrap `59` 个（其中 `37` 个跳过）。
- [x] 本次没有修改 Java Controller、DTO、错误码、数据库结构或 HTTP/SSE 协议；该结果证明当前代码契约门禁通过，不扩展为生产数据库、外部服务和云模型已完成联调。
- [ ] 跳过的 Infrastructure/Bootstrap 测试仍需按环境条件单独执行；不把受控本地构建替代生产验收，不执行 105 个画板全量像素差异或花瓣像素对比。

## 当前更新：2026-09-15 Knowledge、Profile、Admin 页面迁移复核

- [x] 实际浏览器复核 `/knowledge?state=figma-v2&visual-qa=1`、`/profile?state=figma-v2&visual-qa=1` 和 `/admin?state=figma-v2&visual-qa=1`；搜索、筛选、表单、权限入口、表格和侧栏结构均可访问。
- [x] Knowledge 引用详情与结果列表保持独立区域；Profile 头像、资料表单和个人中心导航可见；Admin 宽表仅在自身容器横向滚动，没有页面级横向溢出。
- [x] `shadcnBoundary.test.ts` 定向验证 `1/1` 通过；生产页面未发现直接引入 `@radix-ui/*`、直接依赖 `AdminPrimitives` 或绕过 `src/components/ui` 的新增控件。
- [x] 本批次没有新增页面代码或修改后端接口，视觉复核仍以 Figma 颜色、字体、布局和交互意图为准，不把像素差异作为通过条件。
- [ ] 本批次不执行 105 个画板全量像素差异或花瓣像素对比；iconfont 资料不完整，继续保持 `BLOCKED`。

## 当前更新：2026-09-15 代表页面视觉复核与响应式布局收口

- [x] Diet Records 的操作栏已从绝对定位改为文档流，回归测试确认“饮食记录操作”位于“餐次记录”之后，不再覆盖 Lunch 的第二条记录。
- [x] Planning 移动端餐食表改为自身横向滚动，餐食卡片恢复可读宽度；页面整体不产生横向溢出。
- [x] Analysis 的容器、指标卡和图表卡补齐可收缩边界；移动端筛选条左对齐，指标卡和图表不被视口裁切。
- [x] 浏览器实际复核 `/analysis?view=records&state=figma-v2&visual-qa=1`、`/analysis?state=figma-v2&visual-qa=1`、`/planning?state=figma-v2&visual-qa=1` 和 `/chat?state=figma-v2&visual-qa=1`；Chat 用户消息使用深色前景，Workspace 与用户消息共用登记的 `default-male.svg`，绿色 `agent-status-marker` 保留为 Figma Agent 状态标识，不误当人物头像。
- [x] 受影响页面定向测试为 `6` 个文件、`76/76`；全量 Vitest 为 `70` 个文件、`529/529`；`lint`、`format:check`、`typecheck`、`build`、`audit:api` 和 `git diff --check` 均通过。
- [x] 本批次没有修改 Java Controller、DTO、错误码或 HTTP/SSE 协议；接口审计仍为 `126` 个映射、`119` 个浏览器接口、`7` 个内部接口，生产消费者证据 `119/119`。
- [ ] 本批次只做代表页面浏览器和人工视觉复核，不执行 105 个画板全量像素差异或花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 当前更新：2026-09-15 M15 FoodLog Writer HTTP E2E 收口

- [x] 修正 `ApprovalServiceImpl` 对 `nutrition_food_id` 的解析：只接受大于 `0` 的有效目录 ID，并通过现有 `ItemCommand` 传入食物写入流程；非法值返回稳定的 `INVALID_ARGUMENT`。
- [x] M15 HTTP E2E 使用真实 PostgreSQL、Docker Agent Runtime 和内部 `/foodmate/internal/v1/proposals` HTTP 通道，覆盖创建、拒绝、工具失败回滚、提案 superseded、更新、删除、恢复、revision 冲突、幂等重放、已审核单位换算和不支持单位保持 pending。
- [x] 测试命令最终结果为 `Tests run: 11, Failures: 0, Errors: 0, Skipped: 0`，Maven reactor `BUILD SUCCESS`；测试期间保持 `foodmate-postgres`、`foodmate-agent-runtime` 运行，暂时停止固定 `workerId=1` 的 Docker `foodmate`，避免测试 JVM 与应用 JVM 共享 Snowflake worker 导致主键冲突，测试结束后已恢复且健康检查为 `healthy`。
- [x] M15 仅补充内部 Runtime/Proposal 的受控联调证据，没有新增浏览器接口、没有修改 HTTP/SSE 协议，也没有把 HTTP 接受响应直接当作运行终态。
- [ ] 该证据限定为当前本地受控 Docker/PostgreSQL 环境，不代表生产部署、云模型、长稳或全部外部依赖完成；Figma 仍按代表页面和人工视觉复核执行，不做 105 个画板全量像素差异或花瓣像素对比；iconfont 继续保持 `BLOCKED`。

## 当前更新：2026-09-15 M15 FoodLog Writer RocketMQ Proposal/Result 收口

- [x] `M15FoodLogWriterProposalResultE2ETest` 通过真实 PostgreSQL、RocketMQ Proposal topic、Java Proposal consumer 和 Result consumer 验证创建、拒绝、失败回滚、superseded、更新、删除、恢复、revision 冲突、幂等重放、单位换算和 pending 分支。
- [x] 测试结果为 `Tests run: 11, Failures: 0, Errors: 0, Skipped: 0`，Maven reactor `BUILD SUCCESS`；不是通过静态脚本或 Fixture 代替消息链路。
- [x] 宿主机测试使用 `FOODMATE_DOCKER_ROCKETMQ_BROKER_IP1=host.docker.internal` 重建 Broker，使 NameServer 返回的 `brokerIP1` 对宿主机测试 JVM 可达；测试结束后已恢复 Docker 默认的 `foodmate-rocketmq-broker` 广播地址。
- [x] 测试期间暂停 Docker `foodmate` 和 `foodmate-agent-runtime`，避免固定 Snowflake `workerId=1` 冲突及 Python Result consumer 与测试 consumer 竞争；测试结束后两个容器均已恢复为 `healthy`。
- [x] 该批次仍只收口 `INTERNAL` Runtime/Proposal 通道，不新增浏览器接口、不改变 HTTP/SSE 协议；浏览器接口统计仍为 `119`，内部接口统计仍为 `7`。
- [ ] 证据限定为当前本地 Docker/PostgreSQL/RocketMQ 环境，不代表生产部署、云模型、长稳或全部外部依赖完成；不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 1.0 2026-09-15 Agent Runtime、RocketMQ 与 SSE 代表链路复核

- [x] 在当前 Docker deterministic/stub 环境验证 Java readiness、Python Runtime readiness、Redis checkpoint、RocketMQ command/event producer 与 consumer 均为 `ready`，transport 为 `rocketmq`。
- [x] 真实 Run `358079349800308736` 完成 `sse-last-event-id` 回放验证：报告 `m16-fault-6b13249c44ae4a40af8007abfba00e95`，SSE 缺口 `0`、重复事件 `0`、重复终态 `0`，游标为 `sse_358079350450425856`，队列清空。
- [x] 真实 Run `358079451101138944` 完成重复投递验证：报告 `m16-fault-c899fdd91091424c9212a29e90b28e06`，Inbox 复用既有结果，重复投递 `1` 次，重复业务副作用 `0`，队列清空。
- [x] 前端 `agentRunService.test.ts`、`realAgentService.test.ts`、`chatApi.test.ts` 定向验证 `28/28`；`m1-6-fault-matrix.tests.ps1` 契约验证通过。
- [x] 本批次没有修改 Java Controller、DTO、错误码、HTTP/SSE 协议或前端连接契约；现有 `Last-Event-ID`、多标识去重、终态关闭和取消生命周期均由真实回放结果与前端测试共同覆盖。
- [ ] 证据限定为当前本地 Docker deterministic/stub 环境，不代表云模型、生产部署、长稳或全部 119 个浏览器接口环境联调完成；不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 0.9 2026-09-15 Admin 受控接口联调收口

- [x] 修正 `M11AdminManagementE2ETest` 的旧请求样例：用户、工具、会话撤销和资源恢复均携带服务端当前 `revision`、确认摘要和独立 `Idempotency-Key`。
- [x] 修正 E2E 审计断言，使用后端实际记录的 `admin.user.status.update` action；没有修改 Controller、DTO、错误码或 HTTP/SSE 协议。
- [x] 前端 `adminExtendedService.test.ts` 定向验证 `10/10`，覆盖 Admin 写操作的 revision、确认字段、确认摘要和取消信号。
- [x] 受控本地 E2E `LocalPostgresE2ETest`、`M11AdminManagementE2ETest`、`M11ExportDownloadE2ETest` 共 `3/3` 通过，Maven reactor `BUILD SUCCESS`。
- [x] Spotless、`git diff --check` 通过；新增测试代码无英文注释，现有后端生产接口保持不变。
- [ ] 本记录只证明 local profile 下 PostgreSQL、MinIO、Cookie/CSRF 和 Admin 代表写操作的受控联调，不代表生产部署、云依赖或全部 119 个浏览器接口已完成环境验收。

## 0.7 2026-09-15 Admin 用户操作契约校准

- [x] 重新核对 `AdminUserController` 和 `AdminManagementController`，当前后端只提供用户查询、详情、状态变更和会话撤销，没有凭证重置接口。
- [x] 真实模式的用户详情页禁用“重置凭证”，并明确展示后端未提供该接口；不会发送虚构请求或把通知当作成功结果。
- [x] 用户状态变更和会话撤销的真实成功回调不再修改共享 Fixture 数据；成功后继续由 Admin 页面 `refreshNonce` 触发服务端列表/详情回读。
- [x] Fixture 模式保留“重置凭证”视觉预览入口，但文案明确说明仅用于预览，不代表真实接口可用。
- [x] 前端定向测试 `UsersTab.test.tsx`、`UsersTab.real.test.tsx` 共 `6/6` 通过；全量 Vitest `70` 个文件、`520/520` 通过。
- [x] `typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过；后端 `AdminUserControllerRbacTest` `3/3` 通过，Maven reactor `BUILD SUCCESS`。
- [ ] 本批次不新增凭证重置后端接口；如未来需要该能力，必须先补充后端请求 DTO、权限、审计、凭证生成/交付策略和接口测试，不能由前端自行推断。

## 0.8 2026-09-15 Admin 全域接口契约复核

- [x] 逐项复核 Admin Dashboard、Users、Tools、Runs、Tool Calls、SQL Audit、Trace、DLQ、Operation Audit、Export、Retention、Deleted Resources、Knowledge 和 Model Governance 的 Controller 映射、前端 service、真实页面入口和状态测试。
- [x] 未发现新的真实页面消费者缺口；用户默认列表已使用 `/api/admin/users`，筛选/翻页使用 `/api/admin/queries/users`，其它 Admin 操作继续使用矩阵登记的既有接口。
- [x] 前端 Admin 定向验证 25 个测试文件、`116/116` 通过；后端 Application `41/41`、API `27/27` 通过，Maven reactor `BUILD SUCCESS`。
- [x] 本次没有修改 Java Controller、DTO、错误码或协议；凭证重置继续保持后端契约缺失，不由前端补造。
- [ ] 以上证据证明代码契约和页面消费者已核对，不替代数据库、Cookie、外部依赖和生产环境联调；Figma 仍只做代表页面视觉复核，不执行 105 个画板全量像素差异或花瓣像素对比。

## 0.6 2026-09-15 Docker Agent 真实链路恢复

- [x] Compose 新增 Docker 专用 Broker 广播变量 `FOODMATE_DOCKER_ROCKETMQ_BROKER_IP1`，默认值为 `foodmate-rocketmq-broker`；宿主机运行 Java/Python 时仍可显式覆盖，未修改根目录 `.env`。
- [x] `agent-runtime/tests/test_docker_compose_contract.py` 定向测试 `11/11` 通过，`docker compose config --quiet` 和 `git diff --check` 通过。
- [x] 受控 deterministic Docker 环境中 Java 与 Python readiness 均返回 `200`；RocketMQ NameServer 注册地址为 `foodmate-rocketmq-broker:10911`。
- [x] 真实 `duplicate-event` 验证通过：Run `358028193040437248` 完成，Outbox 为 `published`，重复投递 `1` 次，重复业务副作用 `0`，队列清空。
- [x] 真实 `sse-last-event-id` 验证通过：Run `358028338192715776` 完成，SSE 回放缺口 `0`、重复终态 `0`、重复事件 `0`；事件序列包含唯一 `run.completed`。
- [ ] 本批次只证明受控本地 deterministic/stub 运行环境和 RocketMQ/SSE 链路；根目录 `.env` 中显式云治理配置仍按用户环境生效，未证明云模型、生产部署或全部浏览器接口已完成联调。

## 1. 总目标

完成 FoodMate 的真实前端业务闭环：后端已经存在的浏览器接口都必须有明确的前端 service、页面入口、状态处理和测试证据；后端内部接口保留在内部契约矩阵中，不强行暴露给浏览器。

当前代码扫描口径为：

- Controller 方法级映射 `126` 个。
- 浏览器可关联接口 `119` 个。
- Java/Python Runtime、Outbox、健康探针等内部接口 `7` 个。
- 逐条接口清单以《前后端接口覆盖矩阵》和《前端接口覆盖清单》为唯一登记来源，不以“Controller 存在”或 Fixture 页面展示作为完成依据。

“后端已完成”需要区分两个层次：

1. **代码契约完成**：路由、DTO、错误码、鉴权、角色、幂等键、revision 和状态迁移可以从代码和定向测试确认。
2. **运行环境完成**：真实数据库、Redis、RocketMQ、MinIO、Milvus、Python Runtime、Cookie/CSRF 和部署配置在目标环境联调通过。

本轮前端闭环以第一层为开发门槛；第二层必须按环境单独验收，不能用前端组件测试代替。

## 2. 设计与实现边界

### 2.1 Figma 使用方式

- 读取对应 Figma 页面获取布局层级、颜色、字体、间距、圆角、组件状态和交互意图。
- 允许根据真实数据长度、响应式宽度、可访问性和后端字段对布局做合理适配。
- 颜色、字体和状态优先沉淀为语义 Token，再由页面和 `src/components/ui` 使用。
- 页面验收采用少量代表路由的浏览器运行检查、关键状态截图和人工视觉复核，不建立“看起来接近”以外的伪精确指标。
- 不因视觉差异创建虚构后端字段、虚构 iconfont glyph 或伪造交互成功。

### 2.2 UI 基础设施

- 页面控件统一从 `foodmate-ui/src/components/ui` 引入 shadcn/Radix 封装。
- 页面和业务组件不直接声明原生交互控件、不直接引入 `@radix-ui/*`，不重新依赖 `AdminPrimitives`。
- Lucide 继续承担通用命令图标。
- iconfont 在实体字体包、CSS/Unicode 映射、来源、版本、SHA-256、许可证和授权范围齐全前保持 `BLOCKED`；不得创建虚构 `@font-face` 或映射。

### 2.3 真实模式原则

- `VITE_AGENT_MODE=real` 下不使用 Fixture 成功结果覆盖接口失败、空数据或处理中状态。
- HTTP 接受响应不直接等同于业务终态；异步任务必须读取服务端状态或订阅 SSE。
- 写操作成功后以服务端重新读取结果为准，不能只用本地乐观状态宣告成功。
- 页面卸载、会话切换、条件切换和用户取消必须取消读取、轮询和 SSE 连接，并阻止迟到响应写回。
- 后端不支持的业务状态不能由前端自行新增；失败原因和 retryable 能力只使用服务端返回事实。

## 3. 当前基线

已具备并有代码或测试证据的范围：

- Auth、用户资料、设备会话、头像和账户导出/删除的真实 service 与页面入口。
- Workspace、Session、Message、Memory、ChatRun、AgentRun、Approval 和 SSE 的真实页面消费者。
- Diet Records、Intake Analysis、Meal Planning、Knowledge、Profile 的主要读写路径和请求生命周期保护。
- Admin Overview、Users、Runs、Tools、Knowledge、Retention、Usage、Model Governance、Operation Audit 和 Deleted Resources 的主要真实页面入口。
- shadcn 控件边界、错误码映射、分页、请求取消、旧响应隔离和服务端刷新已经形成可复用模式。
- 本轮 Admin 删除资源、模型治理读取和操作审计导出生命周期收口后，前端门禁与后端定向测试均通过。

仍需持续收口的不是“再造一套后端”，而是：

- 按矩阵逐条检查每一个浏览器接口的页面行为是否完整。
- 对尚未有真实环境证据的接口执行受控本地联调。
- 对 Figma 页面进行分域视觉适配和代表状态复核，而不是全量像素对比。
- 完成真实失败、空态、无权限、冲突、取消、轮询和重连状态的用户可读反馈。

## 4. 分阶段执行计划

### 阶段 0：冻结基线与接口台账

**目标**：保证后续工作不会漏掉后端接口或把历史 Fixture 当成真实闭环。

**工作项**：

- 从 `foodmate-api` Controller 重新扫描方法级映射。
- 与《前后端接口覆盖矩阵》逐行比对路径、方法、DTO、角色、错误码和状态。
- 为每一行补齐页面入口、service 方法、状态证据、后端测试和最后验证提交。
- 将接口状态统一为 `REAL_CONNECTED`、`SERVICE_ONLY`、`FIXTURE_ONLY`、`INTERNAL` 或 `BLOCKED`。

**完成门槛**：`126/126` 映射有登记；`119` 个浏览器接口都有下一步说明；`7` 个内部接口明确不属于浏览器页面范围。

**当前状态**：已建立并持续维护，后续批次只允许校准事实，不允许删掉未完成项。

### 阶段 1：按业务域收口真实页面消费者

每个接口都按以下顺序处理：

1. 读取后端 Controller、request/response DTO、错误码、鉴权和测试。
2. 在 `src/services` 建立或校准唯一 service 方法，统一走 `apiClient` 或 `sseStream`。
3. 把 service 接入真实页面入口，禁止在 real 分支回退到 Fixture。
4. 覆盖加载、空数据、失败、重试、无权限、冲突、处理中和成功刷新。
5. 对读取补充 AbortController、请求序号和卸载保护；对写入补充确认、幂等键、revision 和服务端回读。
6. 添加前端定向测试和受影响后端定向测试。
7. 更新三份接口文档后再进入下一个域。

固定顺序如下：

| 顺序 | 业务域 | 主要接口范围 | 交付重点 |
| --- | --- | --- | --- |
| 1 | Auth 与 Account | `/api/auth/**`、`/api/users/me/**` | 登录错误码、会话恢复、头像、资料保存、导出、删除和账户状态 |
| 2 | Workspace 与 Chat | `/api/sessions/**`、消息、ChatRun | 会话分页、首条消息、消息历史、HTTP 接受与 SSE 终态分离 |
| 3 | Agent 与确认 | `/api/agent-runs/**`、审批和 Proposal/Result | 取消、重试、预算追加、恢复、写入确认、反馈、重连和去重 |
| 4 | Diet 与 Analysis | 饮食记录、删除恢复、营养目录、复合菜、分析 | 日期/周视图、单位、营养候选、失败清空和服务端事实 |
| 5 | Meal Planning | `/api/meal-plans/**`、购物清单和进度 | revision、幂等、计划切换、购物清单生成和状态刷新 |
| 6 | Knowledge | 知识检索、上传、批次、发布、重试、重索引和 SSE | 引用安全字段、批次终态、过期、重试和权限 |
| 7 | Profile | Profile、Memory、设备会话 | 长期记忆确认/修改/删除、摘要失效和身份清理 |
| 8 | Admin | 查询、用户、Runs、Tools、DLQ、Retention、Usage、Audit、Model Governance | 最小权限、确认状态机、导出轮询、失败重试和服务端回读 |

**完成门槛**：矩阵中每个浏览器接口都至少有一个真实页面消费者或有明确的 `SERVICE_ONLY` 阻塞理由；不能以“service 方法存在”代替页面闭环。

### 阶段 2：Admin 高风险操作与异步任务收口

重点处理需要服务端事实确认的页面操作：

- 软删除资源恢复：真实模式不本地删除行，恢复后重新读取服务端列表。
- 模型供应商状态、价格和预算：保留角色、revision、确认摘要和幂等键，成功后重新读取治理数据。
- 操作审计导出：区分 `queued`、`running`、`completed`、`failed`、`expired`、`download_consumed`；轮询结束或组件卸载时清理定时器和请求。
- Retention、DLQ、Knowledge 重索引和其它高风险操作：继续使用统一确认状态机，不将接受响应包装成业务成功。

**完成门槛**：每个写操作都有权限、确认、重复点击、失败、取消、服务端刷新和后端定向测试证据；禁止用本地更新替代服务端结果。

### 阶段 3：Figma 视觉适配与 shadcn 页面迁移

按 Auth → Workspace/Chat → Diet/Analysis/Planning → Knowledge/Profile → Admin 顺序执行。

每页执行：

- 先读取对应 Figma 节点的布局和状态，不从旧页面 CSS 反推新设计。
- 把颜色、字体、间距、圆角、阴影、边框和状态写入语义 Token。
- 用 `src/components/ui` 重组 Button、Input、Select、Dialog、Tabs、Tooltip、Table、Alert 等控件。
- 保留真实后端字段和状态，不为了追求视觉相似删除错误、加载、空态或权限信息。
- 只做桌面、移动和一个窄视口的代表性浏览器检查，重点查看文字可读性、头像一致性、溢出、控件可访问性和状态切换。

**完成门槛**：页面视觉结构与 Figma 意图一致，文字可读，真实数据长度不破坏布局；不以像素差异百分比作为通过条件。

### 阶段 4：受控后端联调

后端核心代码目前已经有较完整的业务实现和定向测试，但“代码通过”不等于“目标环境联调完成”。本阶段只做受控、可回收的本地验证：

- 启动必要的 Java、Python Runtime、PostgreSQL、Redis、RocketMQ、MinIO 和 Milvus 依赖。
- 从登录开始验证一条代表性真实链路：创建 Session → 发送消息 → AgentRun → SSE → 终态 → 页面刷新。
- 对饮食记录、餐食计划、知识检索、Admin 查询和一个高风险写操作各执行一条成功路径及一条稳定失败路径。
- 回读数据库/服务端状态，确认幂等、revision、权限、SSE 游标和错误码没有被前端误读。
- 不执行生产压测、长稳、破坏性故障注入、真实硬删除或发布操作。

**完成门槛**：只把实际运行过的接口标记为环境联调通过；未启动外部依赖的测试只能标记为代码契约通过。

### 阶段 5：小范围交付验收与文档收尾

- 每个阶段只运行受影响模块的定向测试。
- 交付前只运行受影响页面/服务的前端定向 `test`，并运行 `typecheck`、`lint`、`format:check`、`build` 和 `git diff --check`；不执行全量业务回归测试。
- 后端只运行受影响模块测试和必要的 `mvnw.cmd verify`；不为单个页面重复做全量重型测试。
- 浏览器只检查代表性路由和关键状态，不采集 105 个画板全量 PNG/diff。
- 更新《前端接口覆盖清单》《前端已完成实现清单》《前后端接口覆盖矩阵》，只记录真实命令、测试数量和已知边界。
- 每个大点单独提交，提交前检查工作区没有临时截图、缓存或未登记生成物。

## 5. 每个接口的统一验收清单

- [ ] HTTP/SSE 方法、路径、请求字段和响应字段与后端 DTO 一致。
- [ ] 鉴权、角色、CSRF、幂等键、revision 和错误码没有被前端绕过或改名。
- [ ] 有真实 service 方法和页面入口；real 模式没有 Fixture 成功回退。
- [ ] 有加载、空数据、失败、重试、无权限、冲突、取消和处理中状态。
- [ ] 读取请求支持取消、旧响应隔离和卸载清理。
- [ ] 写操作以服务端重新读取结果为准，不本地伪造成功。
- [ ] SSE 使用 `Last-Event-ID`、`sse_event_id` 去重，并在终态关闭连接。
- [ ] 前端定向测试和后端受影响测试已执行并记录。
- [ ] 矩阵、完成清单和接口覆盖清单已同步。

## 6. 明确不纳入本轮完成门槛的内容

- 105 个 Figma 画板全量像素级差异、全量 PNG 采集和花瓣像素对比。
- 生产级吞吐、P95/P99、长时间运行、完整依赖故障矩阵和多实例扩容。
- 生产部署、CI/CD、灰度、回滚、备份恢复和安全演练。
- 未经用户单独授权的数据库硬删除、对象存储删除或向量索引清理。
- iconfont 实体包、CSS/Unicode 映射、来源、版本和许可证缺失时的任何代码补齐。

## 7. 下一步执行顺序

1. 收口 Account/Profile 的当前设备契约和账户剩余状态。
2. 按矩阵复核仍有状态证据说明的浏览器接口，逐域补齐页面消费者和测试。
3. 逐页执行 Figma 视觉适配和 shadcn 控件统一，保留真实错误、空态、权限和异步状态。
4. 最后做小范围代表路由验收和受控后端联调；不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 保持 `BLOCKED`。

## 8. 当前执行记录（2026-09-15）

## 当前执行批次：Auth/Account 无效客服入口收口（2026-09-16）

- [x] 复核 `AuthController` 和 `UserController` 的账户接口，没有发现客服工单、客服跳转或客服邮箱配置；没有为页面添加虚构接口或地址。
- [x] 登录账号禁用态和 Token 已使用态保留 Figma 设计中的操作位置，但去除空 `onClick`，改为原生禁用按钮，并通过可访问名称和 `title` 明确标记“入口暂未配置”。
- [x] Auth/Token 定向验证 `2` 个测试文件、`35/35`；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [x] 本批次没有修改 Java Controller、DTO、错误码、认证协议或新增浏览器接口；接口统计保持 Controller `126`、浏览器接口 `119`、内部接口 `7`、生产消费者证据 `119/119`。
- [ ] 客服能力仍属于后端未提供的产品能力；在后端提供正式入口前，不把该按钮标记为 `REAL_CONNECTED`。
- [ ] 本批次不执行 105 个 Figma 画板全量像素差异或花瓣像素对比；iconfont 资料不完整，继续保持 `BLOCKED`。

### Auth Figma 视觉与短桌面视口收口

- [x] 读取 Figma 登录、注册、找回密码、重置密码和登录锁定态节点 `647:214`、`680:216`、`680:275`、`680:307`、`680:408`、`680:524`，并按现有 shadcn 控件和语义 Token 复核 Auth 页面。
- [x] 修复短桌面视口下认证内容垂直居中造成底部操作区初始裁切的问题；正常高度保持 Figma 的居中布局，短视口从顶部排列并允许滚动。
- [x] 浏览器复核登录与注册代表路由；不执行 105 个画板全量像素差异或花瓣像素对比。
- [x] Auth 与 Token 定向测试 `2` 个文件、`31/31` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [ ] 本批次未证明真实邮件服务、生产 Cookie 域、数据库和外部依赖联调；iconfont 仍保持 `BLOCKED`。

### Auth 请求生命周期与 CSRF Cookie 解析收口

- [x] `authService` 的登录、注册、登出、密码重置请求支持可选 `AbortSignal`；登录和注册后的当前用户读取复用同一取消信号。
- [x] Login、Register、Forgot Password、Reset Password 页面在组件卸载和请求替换时取消请求，并过滤 `AbortError`，不会把请求取消误报为认证失败。
- [x] CSRF Cookie 读取保留值中可能存在的 `=` padding，避免合法 token 被截断；没有修改后端 Cookie/CSRF 协议。
- [x] Auth 定向测试 `authService.test.ts`、`AuthPages.test.tsx` 共 `38/38` 通过；本批次完成后的全量 Vitest 为 `69` 个测试文件、`496/496` 通过。
- [x] `npm.cmd run build`、`npm.cmd run lint`、`npm.cmd run format:check` 和 `git diff --check` 通过；`typecheck` 已在本批次前后通过。
- [ ] 本批次未修改 Java 后端代码、DTO、错误码或认证协议；真实邮件、Cookie 部署域、外部依赖联调仍需按阶段 4 受控验证；iconfont 继续保持 `BLOCKED`。

### ChatRun 兼容入口请求生命周期收口

### ChatRun 兼容入口请求生命周期收口

- [x] `createChatRun`、`getChatRun`、`getChatRunEvents` 和 `cancelChatRun` 支持可选 `AbortSignal`，不修改后端 HTTP/SSE 协议。
- [x] `useRealAgentReplay` 在会话切换、组件卸载、状态/历史刷新和创建/取消请求替换时中止对应 HTTP 请求；迟到响应和 `AbortError` 不进入页面业务错误态。
- [x] ChatRun 兼容入口继续保留 SSE 关闭、游标续接、事件去重和终态关闭行为。
- [x] 前端定向测试 `chatApi.test.ts`、`realAgentService.test.ts` 共 `10/10` 通过；全量 Vitest `69` 个测试文件、`494/494` 通过。
- [x] `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`npm.cmd run build` 和 `git diff --check` 通过。
- [x] 实现提交：`179582f4`。
- [ ] 本批次未修改 Java 后端代码、DTO、错误码或 SSE 协议；未执行后端外部依赖联调、105 个画板全量像素差异或花瓣像素对比；iconfont 继续保持 `BLOCKED`。

### Workspace/Chat 消息生命周期与视觉可读性收口

- [x] `loadSessionMessagesPage` 和 `loadSessionMessages` 支持可选 `AbortSignal`，分页读取的每一页复用同一取消信号。
- [x] `RealChatPage` 在会话切换、组件卸载、完成终态后的服务端消息刷新以及编辑/删除后的回读中取消旧请求，并用请求序号阻止迟到响应写回。
- [x] 取消请求和 `AbortError` 不再显示为 Chat 业务错误；真实模式仍以服务端消息和 SSE 终态为准，没有新增后端接口或修改 SSE 协议。
- [x] 用户消息名称和时间改为按真实用户资料与统一时间格式渲染；Fixture 继续保留 Figma 所需的 `Anddy` 和 `AM/PM` 文案。
- [x] Chat 用户气泡、助手表面、消息元信息和运行轨迹颜色补充 Figma 语义 Token；助手正文在窄视口下从 `560px` 上限自适应收缩，避免文字裁切。
- [x] Chat/session 定向测试 `3` 个文件、`70/70` 通过；前端全量 Vitest `69` 个文件、`492/492` 通过。
- [x] `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`npm.cmd run build` 和 `git diff --check` 通过。
- [x] 浏览器实际复核 `http://127.0.0.1:5188/chat?state=figma-v2&visual-qa=1`，确认用户消息黑色文字、工作区/顶栏/消息头像一致、时间无重复 `PM`，并修复约 `970×698` 视口下的助手正文裁切。
- [ ] 本批次不执行 105 个 Figma 画板全量 PNG/diff、不执行花瓣像素对比；后端数据库和外部依赖联调不由前端测试代替；iconfont 资料继续不足并保持 `BLOCKED`。

## 2026-09-15 Account/Profile 写操作生命周期收口

本大点收口 Profile Basic、Security 和 Privacy 对既有账户接口的写入生命周期，不新增 Java Controller、DTO、错误码或 HTTP 协议。

- [x] `accountService` 的资料保存、密码修改、设备会话撤销、头像上传/删除、数据导出创建/查询/下载和账户注销均支持可选 `AbortSignal`，请求参数和后端路由保持不变。
- [x] Basic 资料保存成功后重新读取 `/api/users/me/profile`，以服务端返回字段更新页面；头像上传成功使用后端返回的受信任地址，失败时回滚旧头像，重复上传或删除会中止上一轮请求。
- [x] Security 的密码修改、单设备撤销和全部设备撤销在组件卸载、操作替换时中止请求；后端撤销当前会话的操作成功后只清理本地身份，不再调用已经失效的远端登出接口。
- [x] Privacy 的导出创建、状态轮询、一次性下载和账户注销在卸载、重复创建、终态和请求失败时清理定时器与 AbortController；不会把 HTTP 接受响应直接显示为导出完成。
- [x] 取消请求和 `AbortError` 不进入业务失败通知；请求序号、组件挂载状态和当前控制器身份共同阻止迟到响应写回页面。
- [x] Account/Profile 定向验证：`accountService.test.ts`、`ProfilePage.account.real.test.tsx` 共 `15/15` 通过；全量 Vitest `69` 个测试文件、`502/502` 通过。
- [x] `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`npm.cmd run build` 和 `git diff --check` 通过。
- [x] 实现提交：`eec14583 fix(frontend): close account profile request lifecycle`。
- [ ] 本大点未修改 Java 后端生产代码，未证明生产 Cookie、数据库、对象存储和外部依赖联调完成；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 2026-09-15 Workspace/Session/Chat/Agent 请求生命周期收口

本大点继续复用已有 Session、Message、AgentRun、Approval 和 SSE 接口，不新增后端路由、DTO、错误码或 SSE 协议；目标是让真实页面的读取、写入、确认操作和事件触发读取都能在卸载、会话切换和操作替换时取消。

- [x] `agentRunService` 的 AgentRun 状态、取消、预算追加、checkpoint 恢复、重试、反馈、Approval 创建/读取/确认/执行/拒绝均支持可选 `AbortSignal`，请求字段和后端路径保持不变。
- [x] `sessionService` 的会话创建、消息发送/编辑/删除、会话重命名/归档/恢复/删除、已删除会话读取和搜索均支持可选 `AbortSignal`，分页读取复用同一信号。
- [x] `RealAgentStatePage` 在查询条件变化和卸载时取消状态、消息和 Approval 读取；SSE 事件触发的 Approval 详情读取使用独立控制器，取消不会进入业务错误态。
- [x] `RealAgentStatePage` 的写入确认、拒绝、取消、预算追加、重试和 checkpoint 恢复支持操作替换、卸载取消和迟到响应隔离；HTTP 接受响应仍不被当作运行终态。
- [x] `RealChatPage` 的新会话创建、首条消息发送、Agent 操作、Approval 操作、消息编辑和删除支持取消；会话切换和卸载会关闭 SSE 并中止进行中的 HTTP 请求。
- [x] 前端定向验证：`ChatPage.agentState.real.test.tsx`、`ChatPage.real.test.tsx`、`agentRunHttpService.test.ts`、`sessionService.test.ts` 共 `4` 个文件、`50/50` 通过，覆盖信号透传、卸载取消和 SSE 事件读取取消。
- [x] `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`npm.cmd run build` 和 `git diff --check` 通过；本批次新增注释使用中文。
- [ ] 本批次未修改 Java 后端生产代码，未证明数据库、Cookie、SSE 部署和外部依赖环境联调完成；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 2026-09-15 Diet Records 与 Intake Analysis 写操作生命周期收口

本大点继续复用既有饮食记录、删除恢复、营养目录和分析接口，不新增 Java Controller、DTO、错误码或修改后端协议；目标是让真实页面的写操作在关闭弹窗、操作替换和组件卸载时都能安全取消。

- [x] `foodLogService` 的新增、编辑、删除和恢复方法支持可选 `AbortSignal`；`compositeDishService` 的新增、编辑和删除方法支持可选 `AbortSignal`，幂等键、revision 和请求路径保持不变。
- [x] Diet Records 的饮食记录新增/编辑/删除/恢复，以及复合菜保存/删除均使用独立 AbortController 和请求序号；当前控制器失效或请求被取消时，迟到响应不会更新页面。
- [x] 关闭饮食记录对话框、收起回收站、关闭复合菜编辑器和组件卸载会终止对应写请求；`AbortError` 不进入错误提示或成功通知。
- [x] 保留真实冲突、权限错误、服务端营养计算和成功后重新读取逻辑，没有把本地状态当作后端最终事实。
- [x] Diet Records、Analysis 与相关 service 定向验证为 `5` 个测试文件、`39/39` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [ ] 本批次未修改 Java 后端生产代码，未证明数据库、鉴权和外部依赖环境联调完成；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 2026-09-15 Meal Planning 写操作生命周期收口

本大点继续复用既有餐食规划接口，不新增 Java Controller、DTO、错误码或修改后端协议；目标是让计划写入、Agent 生成草稿和购物清单写入在计划切换、页面卸载和操作替换时安全取消。

- [x] `planningService` 的计划创建、更新、校验、保存、删除和恢复方法支持可选 `AbortSignal`；请求路径、幂等键和 revision 参数保持不变。
- [x] `PlanningPage` 的草稿保存、计划校验/保存/删除/恢复统一使用写操作控制器和请求序号；计划或视图切换、组件卸载时会中止旧请求。
- [x] Agent 生成草稿所使用的 Session 创建和首条消息提交复用同一取消信号；取消和迟到响应不会跳转或显示成功状态。
- [x] 保留购物清单读取、生成和购物项勾选已有的服务端回读、幂等和取消机制；`AbortError` 不进入业务错误态。
- [x] Meal Planning 定向验证为 `2` 个测试文件、`22/22` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [ ] 本批次未修改 Java 后端生产代码，未证明数据库、鉴权和外部依赖环境联调完成；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 2026-09-15 Knowledge 请求生命周期收口

本大点复用既有 Knowledge 检索、文档上传、批次详情、可见性操作、重试、重索引和 SSE 接口，不新增后端路由、DTO、错误码或修改 SSE 协议。

- [x] `searchKnowledge`、`loadAdminKnowledge`、`loadKnowledgeBatch`、单文件/批量上传、可见性变更、批次重试和重索引均支持可选 `AbortSignal`，信号实际传递到 `apiClient` 的 `fetch`。
- [x] `KnowledgePage` 对重复检索、空查询、页面卸载和重试使用请求序号与 AbortController；迟到结果不会覆盖当前关键词，`AbortError` 不显示为检索失败。
- [x] `KnowledgeSection` 对列表读取、上传、可见性确认操作分别维护请求身份和取消控制器；筛选切换、Dialog 关闭、组件卸载和操作替换不会让旧响应写回。
- [x] `BatchProgress` 的批次详情读取、重试、重索引和 SSE 订阅均具备独立生命周期；共享 `openSseStream` 支持取消信号，取消后关闭 EventSource 并阻止重连，继续保留 `Last-Event-ID`、多标识去重和终态关闭。
- [x] 定向验证为 `7` 个测试文件、`27/27` 通过，覆盖检索 signal、旧查询隔离、Admin Knowledge service signal、批次 SSE 去重/重连/取消、真实批次上传/重试/重索引和过期/错误状态。
- [x] `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`npm.cmd run build` 和 `git diff --check` 通过；本批次新增注释使用中文。
- [ ] 本批次未修改 Java 后端生产代码，未证明知识库数据库、对象存储、RAG、鉴权和部署环境联调完成；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 2026-09-15 Admin 高风险操作请求生命周期统一收口

- [x] Admin 操作级 `AbortController` 已统一覆盖确认执行、取消、操作替换和组件卸载；Fixture 延迟操作也支持取消。
- [x] Admin 操作请求增加序号和挂载状态保护，迟到结果不会调用 `onApply`、刷新页面或覆盖新的操作状态；`AbortError` 不进入失败状态。
- [x] Admin 用户、工具、资源恢复、DLQ、Retention、Model Governance 和 Knowledge 操作均支持信号透传；Retention 读取和 Model Governance 刷新读取也支持取消与迟到响应隔离。
- [x] 定向验证为 13 个测试文件、79/79 个用例通过；`npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`npm.cmd run build` 和 `git diff --check` 均通过。
- [x] 本批次未修改 Java Controller、DTO、错误码、幂等协议或 SSE 协议；新增代码注释保持中文。
- [ ] 本批次只证明 Admin 前端代码契约和请求生命周期，不证明后端生产数据库、鉴权、外部依赖和部署联调完成；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续为 `BLOCKED`。

## 2026-09-15 Agent/SSE 高层连接信号收口

- [x] `agentRunService.openAgentRunStream` 和 `chatApi.streamChatRun` 暴露可选 `AbortSignal`，并透传到共享 `openSseStream`。
- [x] ChatRun 状态刷新把当前请求信号绑定到 SSE；页面卸载、会话切换或刷新请求取消时，EventSource 会关闭且不会继续重连。
- [x] 保留 `connecting`、`connected`、`reconnecting`、`closed`、`exhausted`、`attempt`、`maxAttempts`、`lastEventId`、事件去重和终态关闭契约。
- [x] Agent/SSE 定向验证覆盖 6 个测试文件、65/65 个用例；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [x] 本批次未修改后端 Controller、DTO、错误码或 SSE 协议；新增代码注释使用中文。
- [ ] 本批次不证明部署环境 SSE 长连接、数据库和 Runtime 外部依赖联调完成；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续为 `BLOCKED`。

## 2026-09-15 Account/Profile 当前设备契约收口

- [x] `AuthSessionView` 增加 `current` 字段；真实用户会话查询从 `foodmate_session` Cookie 提取当前 Token，并在 Application 层转为 Hash 后传给持久化端口。
- [x] MyBatis 会话查询按当前 Token Hash 计算当前设备；无当前会话上下文的 Admin 查询返回 `current=false`；内存实现使用同一 Hash 比较规则。
- [x] 前端 `AuthSession` 和 Profile Security 页面仅依据服务端 `current` 判断当前设备，不再使用 `device_id === "current"` 的隐式约定；活跃设备数量按非当前会话计算。
- [x] Application `UserAccountServiceImplTest` `8/8`、API `P1AccountControllerTest` `9/9`、Admin RBAC `3/3` 通过；前端 Account/Profile 定向测试 `16/16` 通过。
- [x] 前端 `typecheck`、`lint`、`format:check` 已通过；完整构建和本大点 Maven 门禁在提交前执行。
- [ ] 本批次不证明生产 Cookie、数据库部署和对象存储联调完成；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 2026-09-15 local-stub 启动通道与账户契约验证

- [x] `application-local-stub.yml` 明确使用 `foodmate.runtime.transport=http`；`FoodMateApplicationTest` 不再通过测试注解覆盖 transport，启动测试现在实际验证 `local-stub` 配置。
- [x] `foodmate-bootstrap` 打包成功，`local-stub` 应用在 `8080` 启动成功，`GET /actuator/health/readiness` 返回 `200` 和 `{"status":"UP"}`。
- [x] 使用独立 Web 会话完成真实 HTTP 契约检查：注册、`GET /api/users/me`、`GET /api/users/me/sessions` 均返回 `200`；会话列表返回 `current=true`，并确认会话 Cookie 已建立。
- [x] `FoodMateApplicationTest` 定向测试 `1/1` 通过；`mvnw.cmd -P alibaba-code-style verify` 成功，Application `263/263`、Infra `119` 通过且 `20` 项按配置跳过、API `91/91`、Bootstrap `59` 项通过且 `37` 项按配置跳过；Spotless 和阿里 Checkstyle 均无违规。
- [ ] 本验证只证明 `local-stub`、代码契约和账户代表链路；Docker、PostgreSQL、Redis、RocketMQ、MinIO、Milvus、Runtime 及生产部署仍未完成真实环境联调。
- [ ] 本批次不执行 105 个 Figma 画板全量像素差异或花瓣像素对比；Figma 继续作为视觉参考，iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 2026-09-15 Runtime 缺失时 Agent 创建闭环收口

- [x] 发现并修复 `local-stub` 在没有 `RuntimeClientPort` 时仍写入用户消息、返回 `agent_run_id` 并停留 `queued` 的问题。
- [x] `AgentRunCommandServiceImpl` 在生成 Run ID、写入消息和创建运行记录前检查 Runtime 派发能力；缺失时返回稳定的 `RUNTIME_UNAVAILABLE`，不伪造排队成功。
- [x] 新增 Application 回归测试，证明 Runtime 缺失时不会调用消息写入或 ID 生成；定向测试 `4/4` 通过。
- [x] 重新打包 Bootstrap 并通过真实 HTTP 验证：`POST /api/sessions/{sessionId}/messages` 返回 `500`、`error.code=TOOL_FAILED`、`runtime_code=RUNTIME_UNAVAILABLE`；消息列表返回 `total=0`。
- [x] 新增 Java 注释使用中文，Spotless 格式化和 Bootstrap 打包通过。
- [ ] 本批次仍未证明 PostgreSQL、Redis、RocketMQ、MinIO、Milvus、Agent Runtime 和生产部署环境的成功 SSE 终态；后续需在 Runtime 可用环境继续验证 Workspace → Chat → AgentRun → SSE。
- [ ] 不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 1.1 2026-09-15 Diet Records、Intake Analysis 与 Meal Planning 接口闭环证据

- [x] Diet Records 已由 `foodLogService`、`compositeDishService` 和 `nutritionFoodService` 真实消费饮食记录增删改查、删除恢复、复合菜详情、营养目录搜索和服务端营养计算接口；写操作保留 `revision`、幂等键、取消信号和成功后的服务端回读。
- [x] Intake Analysis 已由 `analysisService` 和 `AnalysisPage` 真实消费 today/7d/30d 分析接口；切换范围时会取消旧请求并隔离迟到响应，真实失败会清空过期数据。
- [x] Meal Planning 已由 `planningService` 和 `PlanningPage` 真实消费计划列表、详情、创建、编辑、校验、保存、删除、恢复、进度、购物清单和购物项更新接口；计划和购物清单写入均保留 revision、幂等键、取消信号和服务端回读。
- [x] 前端定向验证覆盖 8 个文件、`62/62`；Application 定向验证覆盖 6 个测试类、`40/40`；API 定向验证覆盖 5 个测试类、`21/21`。
- [x] `real_nutrition_catalog_e2e_contract`、`real_food_log_e2e_contract` 和 `real_meal_plan_e2e_contract` 三个脚本契约检查通过；`npm.cmd run audit:api` 报告 Controller 映射 `126`、浏览器接口 `119`、内部接口 `7`、生产消费者证据 `119/119`。
- [x] 当前 Docker Java/Python readiness 为 `200/UP`，Agent Runtime 的 SSE 回放与重复投递代表链路已有独立证据；本大点没有新增浏览器接口，也没有修改后端 HTTP/SSE 协议。
- [x] M15 FoodLog Writer 的本地 HTTP 受控联调已完成：`M15FoodLogWriterHttpE2ETest` 为 `11/11` 通过；此前的 Broker 地址和 `TOOL_EXECUTION_FAILED` 问题通过测试实例隔离后得到可重复的成功证据。该结果不扩展为生产环境闭环完成。
- [ ] 当前 PowerShell 未提供 `FOODMATE_E2E_ADMIN_USERNAME`、`FOODMATE_E2E_ADMIN_PASSWORD`，因此没有执行需要管理员凭据或付费云模型的真实脚本；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 1.2 2026-09-15 RocketMQ 宿主机消费者联调边界

- [x] 受控检查确认 Docker 默认 Broker 广播地址已恢复为 `foodmate-rocketmq-broker`；Java、RocketMQ Broker、Proxy 和 Agent Runtime 容器均恢复 `healthy`，没有删除容器、数据库或卷。
- [x] 在宿主机测试模式下曾验证 Outbox 可以进入 `published`，并记录 Broker `mq_message_id`；这只证明发布确认，不等同于消费端已完成 Envelope 断言。
- [x] `M14RocketMqTransportE2ETest` 已在 `foodmate` Docker 网络内使用 Java 21/Maven 3.9.11 重跑通过，结果为 `Tests run: 1, Failures: 0, Errors: 0`；断言覆盖实际消费、原始 Envelope、`foodmate_run_id`、`foodmate_message_type`、`foodmate_request_hash`、消息 key、Outbox `published` 和 Broker `mq_message_id`。
- [x] M14 测试夹具显式关闭 Agent admission，继续由测试显式驱动 Relay；默认 Compose 广播地址无需改写，也没有删除数据库、容器或卷。
- [ ] 宿主机 JVM 直接运行该测试在默认 Docker 广播配置下仍不受支持；如需宿主机运行，必须先提供稳定且可达的 `FOODMATE_DOCKER_ROCKETMQ_BROKER_IP1`，否则使用 Docker 网络命令。该边界不影响浏览器 HTTP/SSE 协议。
- [x] M14 的本地受控消费链路已收口；该证据不扩展为生产 Broker、长稳、故障注入或云依赖完成，也不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 2026-09-15 Admin 用户与工具详情服务端回读补强

- [x] Admin 用户列表完成写操作后的列表回读后，当前选中用户详情会同时重新读取；用户状态、登录会话和操作历史不会继续展示旧详情对象。
- [x] Admin 工具注册表完成服务端刷新后，右侧已选工具详情按工具唯一 key 重新绑定最新对象；首次加载保持未选中，不自动打开配置弹窗。
- [x] 兼容工具表的操作回调增加真实模式保护；真实模式不直接修改行对象，最终状态继续由 `refreshNonce` 触发的服务端回读提供。
- [x] 定向验证覆盖 Admin、Knowledge、Deleted Resources、Profile 相关 `7` 个测试文件、`38/38` 个用例；新增用户详情刷新和工具详情刷新回归测试。
- [x] `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`npm.cmd run audit:api` 和 `git diff --check` 通过；接口统计为 Controller `126`、浏览器接口 `119`、内部接口 `7`、生产消费者证据 `119/119`。
- [x] 本批次未修改 Java Controller、DTO、错误码、幂等协议或 SSE 协议；新增代码注释使用中文。
- [ ] 本批次只证明前端状态回读和现有接口契约，不证明后端生产数据库、鉴权、外部依赖和部署环境联调完成；不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续为 `BLOCKED`。

## 2026-09-15 Admin 操作审计导出异步状态收口

本批次只收口已有 Admin 导出接口在前端的异步状态语义，不新增后端路由、DTO、错误码或对象存储能力。Figma 继续作为颜色、字体、布局和状态参考，不执行 105 个画板全量像素差异，也不执行花瓣像素对比。

- [x] 导出状态按服务端事实映射 `queued`、`running`、`completed`、`failed`、`expired` 和 `download_consumed`；完成时间已过期的任务在页面显示 `expired`，不再显示下载入口。
- [x] 失败任务展示服务端返回的 `failure_code`，不会把对象存储或其它外部依赖失败包装成前端成功。
- [x] 导出轮询继续在 `queued/running` 时运行，在完成、失败、过期或下载消费后停止；未修改既有 HTTP 接口和状态协议。
- [x] 定向验证 `OperationAuditTab.export.real.test.tsx` 为 `3/3`，覆盖完成、过期和失败错误码展示。
- [x] 完整前端门禁已执行：Vitest `70` 个文件、`540/540`；`typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 均通过。
- [ ] 后端生产对象存储、数据库、鉴权和外部依赖联调不由本批次前端测试代替。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍未提供，资源登记继续保持 `BLOCKED`。

## 2026-09-15 Knowledge/RAG local Milvus 受控链路收口

本批次针对 Knowledge 页面真实消费者对应的后端异步索引依赖，恢复并验证本地 Milvus 受控链路。使用 local deterministic provider，不调用付费 Embedding 或 Chat；Figma 仍只作为视觉和交互参考，不执行 105 个画板全量像素差异或花瓣像素对比。

- [x] 按现有 `docker/compose.yml` 启动 `milvus`，连带 `milvus-etcd`、`milvus-minio`；三者健康检查通过，未删除或重建持久化卷。
- [x] 发现 local deterministic 配置必须清空 OpenAI profile；通过进程级 Compose 覆盖设置空 profile、空 Embedding URL/Key 和独立 collection，Runtime 启动失败不再被误报为业务测试失败。
- [x] 新增 `agent-runtime/tests/test_milvus_local_integration.py`；只有显式设置 `FOODMATE_RUN_MILVUS_INTEGRATION_TESTS=true` 才连接真实 Milvus，使用随机临时 collection，finally 清理测试数据。
- [x] 真实集成测试覆盖向量写入、检索、published/disabled 可见性切换、恢复、版本删除和删除后不可检索，结果为 `1 passed`；默认运行结果为 `1 skipped`。
- [x] Python Knowledge/RAG/Runtime 定向测试 `128 passed、4 subtests passed`；Java Knowledge Application `19/19`、API `6/6`；前端 Knowledge/SSE `6` 个文件、`22/22` 通过。
- [x] 验证结束后恢复仓库 `.env` 原有 Runtime 配置，Java 与 Agent Runtime 均恢复 `healthy`；临时 collection 残留核对为 `[]`。
- [ ] 当前证据不代表真实云 Embedding、云 Chat、生产 RAG、长稳、容量、故障矩阵或管理员凭据驱动的 `real-rag-e2e.ps1` 已完成；这些仍需明确凭据和目标环境。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍未提供，资源登记继续保持 `BLOCKED`。

## 2026-09-15 Auth 字段级错误展示收口

- [x] `apiClient` 保留后端 `ErrorBody.details`，新增字段错误读取工具，兼容 camelCase 和 snake_case 字段名及字符串数组错误。
- [x] `AuthField`、`PasswordField` 支持字段错误、`aria-invalid` 和 `aria-describedby`；错误文本沿用 Auth 语义 Token，不改变 Figma 页面布局意图。
- [x] Register、Forgot Password、Reset Password 真实模式均展示后端字段级错误；用户重新编辑对应字段时清除该字段错误。
- [x] Auth 定向验证覆盖 `4` 个测试文件、`54/54`；`typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 通过。
- [x] 接口审计保持 Controller `126`、浏览器接口 `119`、内部接口 `7`、生产消费者证据 `119/119`。
- [ ] 本批次只证明前端错误契约和认证页面行为，不证明邮件服务、生产 Cookie、数据库或部署环境联调完成；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍未提供，继续保持 `BLOCKED`。

## 2026-09-15 Auth/Account 登录字段错误与导出消费状态收口

- [x] `LoginPage` 真实模式消费 `INVALID_ARGUMENT.details`，支持 `username_or_email`/`username` 与 `password` 字段错误；重新编辑字段时只清除对应错误，并保留 `aria-invalid` 与 `aria-describedby`。
- [x] `ProfilePage` 真实模式根据 `download_consumed_at` 映射一次性导出消费状态；下载链接成功生成后隐藏重复下载入口，Fixture 模式仍只用于状态预览。
- [x] 前端定向验证覆盖 `AuthPages.test.tsx`、`ProfilePage.account.real.test.tsx` 和 `accountService.test.ts`，共 `51/51` 个用例通过。
- [x] 后端 `P1AccountControllerTest` 与 `AccountRequestJsonContractTest` 共 `12/12` 个用例通过；没有新增路由、DTO、错误码或修改 HTTP 协议。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 均通过；审计统计保持 `126/119/7`，生产消费者证据保持 `119/119`。
- [ ] 本批次只证明账户代码契约和页面行为，不证明邮件服务、生产 Cookie、数据库、对象存储或部署环境联调完成；Figma 继续按代表页面人工复核，不执行 105 个画板全量像素差异或花瓣像素对比。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍未提供，继续保持 `BLOCKED`。

## 当前执行批次：Profile Memory 写操作互斥收口（2026-09-15）

- [x] 发现 `RealMemoriesTab` 的确认、编辑和删除操作缺少统一的重复点击保护；新增单一写操作锁，锁定请求及服务端列表回读期间的全部记忆操作。
- [x] 写入成功后继续重新读取 `/api/memories`，没有把 HTTP 成功响应直接当作本地最终状态；失败、取消和组件卸载行为保持原有隔离逻辑。
- [x] 前端定向测试 `ProfilePage.real.test.tsx`、`memoryService.test.ts` 共 `8/8`；后端 `MemoryCandidateServiceImplTest` 共 `12/12`。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 通过；接口审计保持 `126` 个映射、`119` 个浏览器接口、`7` 个内部接口、`119/119` 生产消费者证据。
- [x] 本批次未修改后端 Controller、DTO、错误码、幂等协议或 SSE 协议；新增代码注释使用中文。
- [ ] 本批次只证明 Profile Memory 的前端请求生命周期和既有后端契约，不代表生产数据库、部署和外部依赖联调完成；Figma 不执行 105 个画板全量像素差异或花瓣像素对比。
- [ ] iconfont 实体包、CSS/Unicode 映射、来源和许可证资料仍不完整，继续保持 `BLOCKED`。

## 当前完成批次：Admin 统一操作状态机同步防重复确认（2026-09-16）

- [x] Admin 父级操作状态机新增按请求 ID 绑定的同步执行锁；同一确认周期内重复触发不会创建第二个 AbortController 或第二个真实写请求。
- [x] 请求完成、失败、取消、操作替换和组件卸载都会释放或失效当前执行锁；请求序号、控制器身份和挂载状态继续阻止迟到响应写回。
- [x] 新增真实模式工具状态变更回归测试，确认快速重复确认只发送一个 `/api/admin/tools/{name}/status` 请求；既有卸载中止测试继续通过。
- [x] Admin 定向验证：21 个测试文件、`101/101`；本次聚焦状态组件与真实 Admin 页面为 `7/7`。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 通过；接口统计保持 Controller `126`、浏览器接口 `119`、内部接口 `7`、生产消费者证据 `119/119`。
- [x] 本批次未修改 Java Controller、DTO、错误码、幂等协议或 HTTP/SSE 协议；新增代码注释使用中文。
- [ ] 本批次只收口 Admin 父级重复确认竞态，不代表所有 Admin 业务状态、生产数据库、鉴权、外部依赖和部署环境联调已完成；Figma 继续按代表页面人工复核，不执行 105 个画板全量像素差异或花瓣像素对比。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证资料仍不完整，继续保持 `BLOCKED`。

## 当前完成批次：Chat/Agent 运行操作同步防重复（2026-09-16）

- [x] `RealAgentStatePage` 使用 `actionControllerRef` 作为立即可见的同步执行锁；确认/拒绝写入、创建提案、取消 Run、追加预算、重试和 checkpoint 恢复在 React 状态刷新前重复触发时只允许一个请求继续执行。
- [x] 请求完成、失败、取消、操作替换和组件卸载继续通过控制器身份、请求状态和挂载状态隔离迟到响应；没有改变既有后端 HTTP/SSE 协议。
- [x] Chat Agent 真实模式定向验证为 `2` 个测试文件、`39/39`；`typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 均通过。
- [x] 接口审计保持 Controller `126`、浏览器接口 `119`、内部接口 `7`、生产消费者证据 `119/119`。
- [x] 本批次新增代码注释使用中文，未新增虚构接口、成功结果或 iconfont 映射。
- [ ] 本批次只收口 Agent 页面操作竞态，不代表生产 Runtime、数据库、部署和外部依赖联调完成；Figma 不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 当前完成批次：ChatRun SSE 会话切换生命周期回归（2026-09-16）

- [x] `useRealAgentReplay` 在会话切换时关闭旧 ChatRun SSE 连接，并阻止旧连接迟到事件写入新会话。
- [x] 后端实际 SSE 契约已核对：两个流均支持 `Last-Event-ID` 请求头和 `lastEventId` 查询参数，稳定 `sse_event_id` 由服务端映射到持久化游标；前端继续使用兼容查询参数，不修改协议。
- [x] AgentRun、ChatRun、页面真实状态定向验证共 `5` 个测试文件、`68/68`；连接状态、事件去重、重连耗尽、终态关闭、取消和会话切换均有证据。
- [x] `typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 均通过；新增测试无英文代码注释。
- [ ] 本批次不代表生产 SSE 长稳、部署和外部依赖联调完成；不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 当前执行批次：Admin 概览时间筛选与真实字段边界（2026-09-16）

- [x] Admin Overview Fixture 支持 `全部`、`近 24h`、`近 7 天` 和 `近 30 天` 时间筛选；切换筛选时重置到第一页，不再使用空处理器。
- [x] Fixture 行使用相对时间元数据执行本地窗口过滤；真实模式不读取该元数据，继续只使用服务端 `from` 查询参数。
- [x] 真实模式复用既有 `GET /api/admin/queries/runs`，将筛选窗口转换为 `from` 查询参数；后端 `AdminOperationalQueryController`、Application Request 和 Mapper 已有对应过滤能力，本批次未新增接口。
- [x] 真实运行查询没有返回降级字段，因此真实模式的“降级筛选”明确禁用并提供原因提示；不通过前端本地数据伪造该筛选结果。
- [x] Admin 定向验证为 `2` 个测试文件、`36/36`；`typecheck`、`lint`、`format:check`、`build`、`audit:api` 和 `git diff --check` 全部通过。
- [x] 接口审计保持 Controller `126`、浏览器接口 `119`、内部接口 `7`、生产消费者证据 `119/119`；本批次没有修改 Java 后端接口、DTO、错误码、幂等协议或 SSE 协议。
- [ ] 本批次只证明 Admin 概览筛选的前端代码契约和既有接口消费，不代表真实数据库、鉴权、部署和外部依赖联调完成；Figma 继续按代表页面复核，不执行 105 个画板全量像素差异或花瓣像素对比。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证资料仍不完整，继续保持 `BLOCKED`。

## 当前完成批次：deterministic Agent Runtime 与 SSE 本地成功链路复验（2026-09-16）

- [x] 使用当前 Docker Compose 环境重新执行 `script/local/deterministic-agent-e2e.ps1 -RunTimeoutSeconds 180`，显式切换 Agent Runtime 到 `deterministic:local`，并在结束后恢复原始 Runtime 配置。
- [x] 真实注册、`POST /api/chat/runs`、`GET /api/agent-runs/{runId}/stream` 和 Run 状态读取均通过；Run `358350018140180480` 最终状态为 `completed`。
- [x] 首次 SSE 收到 `7` 个持久化事件，事件 ID 唯一，唯一终态为 `run.completed`；服务端事件查询返回 `7` 个事件。
- [x] 使用终态前一个事件的 `Last-Event-ID` 回放返回 `1` 个 `run.completed`，没有重复旧事件；会话软删除成功，清理错误为空。
- [x] 前端全量 Vitest 当前为 `71` 个文件、`562/562`；`npm.cmd run audit:api` 保持 Controller `126`、浏览器接口 `119`、内部接口 `7`、生产消费者证据 `119/119`。
- [ ] 证据限定为当前本地 deterministic/stub 环境，不代表真实云模型、生产部署、长稳、故障恢复或全部外部依赖环境联调完成；不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 当前完成批次：代表页面 Figma 视觉与响应式人工复核（2026-09-16）

- [x] 在 `1440×900` 下复核 Workspace/Home、Diet Records、Intake Analysis、Meal Planning、Knowledge 和 Profile；六页字体状态均为 `loaded`，页面 `scrollWidth` 与 `bodyScrollWidth` 均等于视口宽度 `1440`。
- [x] 在 `390×844` 下复核上述六页；页面 `scrollWidth` 与 `bodyScrollWidth` 均等于 `390`，没有页面级横向溢出，移动端没有出现内容覆盖或裁切。
- [x] 在 `1440×900`、`1366×768` 和 `390×844` 下复核 Admin User Detail；桌面视口没有页面级溢出，移动端的横向滚动仅存在于数据表内部，详情面板继续按页面流布局。
- [x] 复核 Chat 反馈项：用户气泡实际为浅黄背景 `rgb(255, 246, 226)` 与黑色文字 `rgb(0, 0, 0)`；工作区侧栏、顶栏和消息头像均指向 `/assets/avatars/default-male.svg`。
- [x] 本批次未修改页面代码、接口或 Figma 文件；复核以可读性、布局稳定性、字体加载、头像一致性和响应式边界为准，没有使用像素差异替代人工判断。
- [ ] 本批次只覆盖代表页面和指定视口，不代表 105 个画板完成全量视觉验收；不执行花瓣像素对比，iconfont 实体包、映射、来源和许可证继续保持 `BLOCKED`。

## 当前完成批次：deterministic Agent Runtime 启动幂等与 SSE 链路复验（2026-09-16）

- [x] 发现并修复 Docker Compose 重启时 RocketMQ `rocketmq-init` 重复执行 `updateSubGroup` 导致启动等待过长的问题；已有 consumer group 现在先通过 `getConsumerConfig` 只读确认，缺失时才创建。
- [x] 修正 deterministic E2E 的容器时序：显式等待 `rocketmq-init` 正常退出，并确认 Agent Runtime 从 `created` 进入 `running` 后才开始 readiness 和业务请求；恢复原始 Runtime 配置也复用该检查。
- [x] `deterministic-agent-e2e.tests.ps1` 增加上述启动时序和 RocketMQ 幂等路径的契约断言，脚本契约验证通过。
- [x] 当前 Docker 真实联调 Run `358357806165790720` 最终为 `completed`；SSE 收到 `8` 个唯一事件，唯一终态为 `run.completed`，事件回放返回 `1` 个终态事件，持久化事件 `8` 个，会话软删除成功，清理错误为空。
- [x] 本批次只修改本地联调脚本和 RocketMQ 初始化脚本，没有修改浏览器 HTTP/SSE 协议、后端业务接口或 Figma 页面；新增代码注释使用中文。
- [ ] 证据仍限定为本地 deterministic/stub Docker 环境，不代表真实云模型、生产部署、长稳、故障矩阵或全部外部依赖完成；不执行 105 个 Figma 画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。

## 当前完成批次：Knowledge 本地 deterministic 业务闭环（2026-09-16）

- [x] 修复 `script/local/import-public-knowledge.ps1` 的 Windows PowerShell 5.1 兼容性：显式读取 UTF-8 manifest、加载 `System.Net.Http`，并使用实际生效的幂等键请求头。
- [x] 导入脚本新增显式 `-RuntimeMode stub|local|unknown` 输出；不再把未知运行模式硬编码为 `local-stub`。
- [x] 在隔离 deterministic Runtime 和临时 Milvus collection 中完成管理员注册、权限提升、单文档批量上传、索引完成、批次详情和批次 SSE 读取。
- [x] 普通用户检索到已发布文档；下线后不可检索；恢复后按后端契约回到 `draft` 且仍不可检索；再次显式发布后恢复检索；删除后不可检索。
- [x] 数据库终态确认文档为 `is_deleted=true`、`visibility=deleted`；两名临时账号软删除为 `disabled`，临时资料、临时环境文件和隔离 collection 均已清理，Runtime 已恢复原 `.env` 配置并 healthy。
- [x] PowerShell 5.1 parser、`real-rag-e2e.tests.ps1` 契约检查和 `git diff --check` 通过；本批次没有调用付费 Chat/Embedding。
- [ ] 证据限定为本地 deterministic 业务环境，不代表真实云 RAG、生产部署、长稳或故障矩阵完成；Figma 不执行 105 个画板全量像素差异或花瓣像素对比，iconfont 继续保持 `BLOCKED`。
