# M1-2 会话消息与前端真实接入实现逻辑

> 本文档用于记录 M1-2 的已完成实现逻辑。
> 后续 AI 阅读本文档时，必须按功能点拆分为独立小节，不能把多个功能点写成一大段；不得把 M1-3 的 AgentRun、Runtime、AI 回复或 SSE 功能提前写入 M1-2。

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 功能名称 | 会话、消息与前端真实接入 |
| 功能编号/阶段 | M1-2 |
| 文档版本 | v1.0 |
| 实现日期 | 2026-07-25 |
| 适用环境 | local / dev |
| 实现状态 | 已验收 |
| 关联方案或需求 | M1-2 会话、消息与前端真实接入实施方案 |

## 2. 功能概述

### 2.1 功能目标

- 把前端会话、消息、登录态恢复切到真实 PostgreSQL 接口。
- 让用户能真实创建会话、查看消息、发送用户消息、重命名、归档、删除和恢复会话。
- 统一 Cookie + CSRF 的请求方式，替换掉前端 mock 数据。

### 2.2 适用范围

- 真实登录、注册、退出、密码重置。
- 会话列表、搜索、分页、重命名、归档、删除、恢复。
- 用户消息持久化与消息读取。
- 前端真实 API 接入、登录态恢复、错误展示。

### 2.3 不包含范围

- AgentRun 创建与运行状态机。
- Runtime 调度、SSE、AI assistant 回复。
- 工具调用、RAG、附件写入。

## 3. 接口清单

| 接口地址 | HTTP 方法 | 接口名称 | 是否需要登录 | 是否需要 CSRF | 允许角色 | 幂等性 |
|---|---|---|---|---|---|---|
| /api/auth/register | POST | 用户注册 | 否 | 否 | public | 否 |
| /api/auth/login | POST | 用户登录 | 否 | 否 | public | 否 |
| /api/auth/logout | POST | 用户退出 | 是 | 是 | user | 否 |
| /api/users/me | GET | 恢复登录态 | 是 | 否 | user | 是 |
| /api/sessions | GET/POST | 会话列表/创建 | 是 | GET 否 / POST 是 | user | POST 否 |
| /api/sessions/search | GET | 会话搜索 | 是 | 否 | user | 是 |
| /api/sessions/{id} | PATCH/DELETE | 重命名/删除 | 是 | 是 | user | 否 |
| /api/sessions/{id}/archive | POST | 归档会话 | 是 | 是 | user | 否 |
| /api/sessions/{id}/unarchive | POST | 取消归档 | 是 | 是 | user | 否 |
| /api/sessions/{id}/restore | POST | 恢复会话 | 是 | 是 | user | 否 |
| /api/sessions/{id}/messages | GET/POST | 消息读取/保存 | 是 | POST 是 | user | POST 否 |

## 4. 功能点实现

### 4.1 真实登录、注册和退出

- 代码入口是 foodmate-api/src/main/java/com/foodmate/api/controller/AuthController.java。
- register 和 login 调用 UserAccountService，成功后由服务端签发 foodmate_session 和 foodmate_csrf Cookie。
- logout 撤销当前会话并清空相关 Cookie。
- 前端通过 foodmate-ui/src/services/authService.ts 和 foodmate-ui/src/services/apiClient.ts 发送真实请求，不再依赖 mock。

### 4.2 登录态恢复

- 应用启动时，前端调用 /api/users/me 检查当前 Cookie 是否仍有效。
- foodmate-ui/src/App.tsx 根据返回结果决定进入登录页还是工作台。
- 如果服务端返回 401，前端清空认证状态并回到登录页。
- 刷新页面后仍使用服务端会话，不伪造已登录状态。

### 4.3 会话创建、列表和搜索

- 代码入口是 SessionController.create、SessionController.list、SessionController.search。
- 创建时后端从当前登录用户取得 user_id，生成会话 ID 并写入 sessions。
- 列表和搜索都带用户归属条件，只能看到自己的会话。
- PostgreSQL 模式下按标题和消息正文匹配，并按分页数据返回。

### 4.4 会话重命名、归档、删除和恢复

- PATCH /api/sessions/{id} 只修改标题，不改变会话归属。
- archive 和 unarchive 只切换状态，不删除消息。
- DELETE /api/sessions/{id} 写入软删除字段，保留恢复机会。
- restore 要求会话属于当前用户且仍在恢复期内。

### 4.5 用户消息保存与读取

- POST /api/sessions/{id}/messages 只接受 role=user。
- UserAccountService 生成消息序号、写入 messages，并更新会话最近消息时间。
- GET /api/sessions/{id}/messages 按序读取已持久化消息。
- ChatPage.tsx 先显示发送中状态，再用服务端结果替换临时消息。
- 本阶段只保存用户消息，不生成 assistant 回复。

### 4.6 前端 API 封装与 CSRF

- foodmate-ui/src/services/apiClient.ts 统一处理 credentials: include、JSON 请求头和错误映射。
- POST、PATCH、DELETE 等非安全方法自动带 X-CSRF-Token。
- foodmate-ui/vite.config.ts 提供本地代理，满足后端同源 CSRF 校验。
- 页面层只处理统一后的成功、失败、401、403 和网络异常状态。

## 5. 涉及的数据库表

- users
- user_auth_sessions
- sessions
- messages
- session_status_history

## 6. 验收记录

| 验收项 | 验证方式/命令 | 结果 | 验证日期 | 证据位置 |
|---|---|---|---|---|
| 登录注册与退出 | 浏览器真实链路 + 后端测试 | 通过 | 2026-07-25 | M1-2 实施方案和测试输出 |
| 会话创建、搜索、重命名、归档、删除、恢复 | 浏览器真实链路 + PostgreSQL E2E | 通过 | 2026-07-25 | M1-2 实施方案和测试输出 |
| 用户消息保存与读取 | 浏览器真实链路 | 通过 | 2026-07-25 | M1-2 实施方案和测试输出 |
| Cookie/CSRF 与登录态恢复 | 后端测试 + 前端构建 | 通过 | 2026-07-25 | M1-2 实施方案和测试输出 |

## 7. 已知限制和后续工作

- M1-2 只负责会话和用户消息，不生成 AgentRun。
- assistant 回复、SSE 和运行态留给 M1-3。
- 附件、工具调用和 RAG 留给后续阶段。

## 8. 2026-09-14 契约收口增量

### 8.1 搜索分页响应

- `GET /api/sessions/search` 现在返回统一的 `items`、`total`、`page`、`size` 分页结构，不再返回没有总数的数组。
- PostgreSQL 模式增加搜索总数查询；本地内存模式对标题和用户消息正文使用相同的匹配语义，并按最近消息时间和会话 ID 稳定排序。
- `WorkspaceLayout` 使用后端总数计算分页，不以当前页返回条数代替总数。

### 8.2 消息历史分页与顺序

- `loadSessionMessagesPage` 保留单页读取能力。
- `loadSessionMessages` 首次读取后根据 `total/size` 继续读取后续页，并按 `sequence_no`、创建时间和消息 ID 排序。
- Chat 页面已有的会话切换请求序列保护继续生效，旧会话响应不能覆盖当前会话。

### 8.3 会话操作重复提交防护

- 新建、重命名、归档、取消归档、删除、回收站加载和恢复共用一个前端操作锁。
- 操作进行时相关按钮禁用；失败只展示错误并释放锁，成功后重新读取服务端列表。
- 操作锁是前端交互保护，不替代后端权限、幂等和事务裁决。

### 8.4 本批次验证

| 验证项 | 结果 |
|---|---|
| 前端 Workspace、Session、Home、Chat 定向测试 | `42/42` 通过 |
| 前端 `typecheck`、`lint`、格式检查 | 通过 |
| Application `UserAccountServiceImplTest` | `7/7` 通过 |
| API `M12SessionControllerTest` | `1/1` 通过 |
| Application/Infra/API 受影响模块编译 | 通过 |

本批次不执行 105 个画板全量像素差异或花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证仍不足，继续保持 `BLOCKED`。
