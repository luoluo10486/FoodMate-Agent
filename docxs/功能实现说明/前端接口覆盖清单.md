# 前端接口覆盖清单

## 1. 文档目的

本清单以当前工作树中的 `foodmate-api` Controller 源码为唯一后端路径来源，以 `foodmate-ui/src/services` 和页面调用为前端接入来源。它记录接口是否已经被前端 service 封装，不把 mock Fixture、页面静态展示或“存在 Controller”当成真实闭环完成。

当前执行边界：

- Figma 用于布局、颜色、字体、组件状态和交互参考，不要求 105 个画板像素级一比一，也不执行大范围像素差异测试。
- 用户端和管理端接口全部登记；服务间内部接口登记边界，但不直接暴露给浏览器。
- 页面真实接入按业务域推进；已完成域必须同时记录页面行为、状态测试和验证证据，未完成域继续标记“待页面接入”。
- iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证尚未收到，资源登记继续保持 `BLOCKED`。

## 2. 2026-09-15 Workspace/Home 真实读取与 Figma 视觉收口

- [x] `GET /api/sessions` 继续由 `sessionService.loadSessionSummariesPage` 和 `HomePage` 消费；本批次为首页调用补充 `AbortSignal`，组件卸载、刷新和请求替换时取消旧读取。
- [x] `GET /api/nutrition-analysis?range=today` 继续由 `analysisService.loadNutritionAnalysis` 和 `HomePage` 消费；营养摘要请求补充取消信号，失败时清空过期摘要，不保留旧营养数值。
- [x] Home 两个并行读取使用请求序号隔离；只有当前请求的服务端响应能更新页面，取消不会被展示为网络错误，真实模式不回退到 Fixture。
- [x] 以 Figma 节点 `640:256` 为视觉来源，首页基础背景、输入器、快捷任务、指标卡、信息面板和错误提示的颜色收口到语义 Token；不改变后端路由、DTO、SSE 协议或接口字段。
- [x] 定向验证为 HomePage、真实 Home、WorkspaceLayout 和 SidebarSessionList 共 `4` 个文件、`36/36`；`typecheck`、`lint`、`format:check` 和 `git diff --check` 通过。
- [ ] 本批次只完成首页代表性浏览器复核，不执行 105 个画板全量像素差异或花瓣像素对比；iconfont 资料缺失，继续标记 `BLOCKED`。

## 2026-09-14 饮食记录 stale 状态与 Workspace 可访问性收口

- [x] `DietRecordsPage` 在复合菜列表和已删除记录列表重新读取失败时清空旧数据，页面不会继续展示可能过期的服务端事实。
- [x] “已删除记录”每次展开都会调用 `GET /api/food-logs/deleted`；收起后再次展开会重新读取，而不是只依赖本地列表是否为空。
- [x] 展开态 Workspace 导航移除重复的可访问名称，折叠态保留 `aria-label` 和 Tooltip；不改变 Session、Food Log 或 SSE 接口契约。
- [x] 前端定向验证 9 个测试文件、`71/71` 通过；`typecheck`、`lint`、`format:check`、`build`、`git diff --check` 均通过。
- [x] 本批次未修改后端接口；实现提交为 `332dfaf0`。
- [ ] 本批次不执行 105 个画板全量 PNG/diff，不执行花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 2026-09-14 全量接口契约门禁复核

- [x] Controller、service 和页面调用重新核对，接口统计保持 `126` 个映射、`119` 个浏览器接口和 `7` 个内部接口。
- [x] 前端全量 Vitest：`66` 个测试文件、`453/453` 通过。
- [x] 后端 `\.\mvnw.cmd -P alibaba-code-style verify`：`BUILD SUCCESS`；Application `262/262`、Infra `119/119`、API `90/90`、Bootstrap `59/59` 通过，Checkstyle 为 `0`。
- [x] 内部接口只保留后端测试证据，不创建浏览器 service；浏览器接口继续要求真实页面、状态测试和服务端响应证据才能维持 `REAL_CONNECTED`。
- [ ] Infra 有 `20` 项、Bootstrap 有 `37` 项依赖外部环境的测试被跳过；本地门禁不等于 PostgreSQL/Redis/RocketMQ/Runtime 外部联调完成。
- [ ] 本批次不执行 105 个画板全量 PNG/diff，不执行花瓣像素对比；iconfont 资源登记继续保持 `BLOCKED`。

## 2026-09-14 共享前端基础设施与 shadcn 边界复核

- [x] 页面和业务组件未发现直接声明原生交互控件、直接引入 Radix 或依赖 `AdminPrimitives` 的生产代码。
- [x] 页面层原生 `input/textarea` 只存在于 `src/components/ui` 共享实现；请求统一经过 `apiClient.ts`，实时流统一经过 `sseStream.ts`。
- [x] `shadcnBoundary.test.ts` 作为架构边界测试随全量 Vitest 运行；当前 `66` 个文件、`453/453` 通过。
- [x] 本批次没有修改后端接口、SSE 协议或 iconfont 资源边界；扫描结论不替代真实页面状态和外部依赖联调证据。
- [ ] 本批次不执行 105 个画板全量 PNG/diff，不执行花瓣像素对比；Figma 继续作为定向视觉参考，iconfont 保持 `BLOCKED`。

## 2026-09-14 Chat 首条消息与运行生命周期收口

- [x] 新会话真实流程先调用 `POST /api/sessions`，再调用 `POST /api/sessions/{sessionId}/messages`；只有首条消息被服务端接受后才切换到新会话路由并重新读取消息和 AgentRun。
- [x] 首条消息失败时保留服务端已创建的会话，使用 Router state 传递错误和待发送内容；重试复用当前会话，不重复创建会话。
- [x] 新 Run 清理旧 Run 的工具、运行意图、流式回答、回答时间、反馈消息 ID 和引用；既有 `Last-Event-ID`、事件去重、取消、重试、预算追加和终态关闭行为保持不变。
- [x] `run.completed` 后刷新消息时检查 SSE 生命周期和组件挂载状态；刷新失败展示真实错误，迟到响应不会写入已卸载或已切换的页面。
- [x] 本批次未修改 Java 后端生产代码、SSE 协议或接口字段；相关接口继续按矩阵标记为 `REAL_CONNECTED`。
- [x] 前端定向验证共 `6` 个文件、`57/57` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [ ] 本批次不执行 105 个画板全量 PNG/diff，不执行花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 2026-09-14 Workspace 侧栏交互收口

- 本批次只完善 `WorkspaceLayout` 的壳层交互，不新增、不修改后端 HTTP/SSE 接口；现有 `/api/sessions`、`/api/sessions/search`、`/api/chat/runs/*` 和 `/api/agent-runs/*` 的真实 service 契约保持原状。
- 侧栏折叠只改变导航壳层的显示方式，不改变会话分页、消息读取、AgentRun 创建、SSE 订阅、取消或终态处理。
- 折叠状态保留所有业务导航入口，隐藏会话历史和搜索；`820px` 以下恢复完整侧栏。折叠态按钮通过 `aria-label` 和 Tooltip 保留可操作语义。
- 证据：`WorkspaceLayout.test.tsx`、`sessionService.test.ts`、`agentRunService.test.ts`、`realAgentService.test.ts`、`ChatPage.chatRun.test.tsx`、`ChatPage.real.test.tsx` 共 `66/66`；前端 `typecheck`、`lint`、`format:check`、`build`、`git diff --check` 均通过。
- 本批次没有把布局交互测试当作后端接口完成证据；接口状态仍以 Controller、service、页面行为和业务测试共同判定。

## 2026-09-14 真实头像端点与统一头像组件收口

- `GET /api/users/me/avatar` 的后端响应仍是 302 图片资源；`authService.loadCurrentUser` 现在保留真实 `avatar_url`，由 `AvatarImage` 在真实主动上传入口中加载，不将它误判为不可信地址。
- 头像解析只信任当前站点或配置 API 基址下的 `/api/users/me/avatar`；外部 CDN、旧 `/uploads`、Figma 人物素材和其它持久化地址继续按性别回退到登记的默认 SVG。
- `AvatarImage`、`WorkspaceLayout`、`ProfilePage`、`ChatPage`、`AdminPage` 和 `UsersTab` 已统一使用同一头像边界；Fixture/默认入口保持 `default-only`，真实上传入口才允许 `persisted-upload` 或临时 `blob:` 预览。
- 头像相关定向验证为 `6` 个测试文件、`80/80` 个用例；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 均通过。
- 真实环境中的 302、Cookie、对象存储权限和浏览器加载失败仍需单独联调；本条不把本地测试描述为生产对象存储验收。

## 2026-09-14 Profile 数据导出真实状态收口

- `POST /api/users/me/export` 创建任务后，Profile 真实页面只使用服务端返回的导出任务 ID，并通过 `GET /api/users/me/export/{id}` 读取离散状态。
- 真实模式不展示百分比进度或固定文件大小。当前后端 `ExportJob` 没有返回这两个字段，页面不会补造数据；下载 URL 仍由服务端下载接口提供。
- 轮询在新任务、`completed`、`failed`、`expired`、读取异常和组件卸载时清理；Fixture 进度只用于设计预览。
- 定向验证：`ProfilePage.account.real.test.tsx`、`accountService.test.ts` 共 `6/6`；`typecheck`、`lint`、`format:check`、`build`、`git diff --check` 均通过。
- iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍未收到，资源登记继续为 `BLOCKED`。

## 2026-09-14 Auth、Profile、Workspace 与 Session 请求生命周期收口

- [x] 真实缓存身份只有 `status=active` 才映射为 `authenticated`；`disabled`、`locked`、缺少状态和损坏缓存分别进入受限或过期路径，不能仅凭 localStorage 存在判定已登录。
- [x] `loadCurrentUser`、`getProfile`、`getAuthSessions`、Workspace 会话列表和搜索请求支持 `AbortSignal`；组件卸载、页面切换和新查询会取消旧请求，取消不会被误报为网络错误。
- [x] Profile 设备会话请求增加请求序列保护；新请求开始时清除旧设备数据，失败和重试不会继续展示过期设备列表；密码修改和“退出全部设备”在后端已撤销当前会话后只清理本地身份，不重复调用必然失效的远端登出接口。
- [x] Session service 保留原有分页和查询参数，新增可选取消信号；Workspace 继续使用请求序列保护和重复操作锁，不修改后端 HTTP/SSE 协议。
- [x] 定向验证覆盖 Auth、API client、Account、Session、Workspace 和 Profile，共 `6` 个文件、`54/54` 通过；前端全量验证为 `66` 个文件、`466/466` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [x] 本批次未新增后端接口；后端已有 `126` 个映射、`119` 个浏览器接口和 `7` 个内部接口的统计口径保持不变。
- [ ] 当前后端 `AuthSessionView.device_id` 在部分登录流程中可能为空，前端不会把空值伪造为“当前设备”；精确当前设备标识仍需后端补充设备写入或明确 current 字段。
- [ ] 本批次不执行 105 个画板全量 PNG/diff，不执行花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 2. 统计口径

| 范围 | 数量 | 口径 |
| --- | ---: | --- |
| Controller 映射接口 | 126 | `foodmate-api/src/main/java/com/foodmate/api/controller` 下 `Get/Post/Put/Patch/DeleteMapping` 的实际扫描结果；包含 `UserController` 中使用全限定名声明的头像上传映射 |
| 浏览器可关联接口 | 119 | Auth、用户、会话、饮食、知识库、Agent 和 Admin 接口 |
| 内部服务接口 | 7 | Runtime Gateway、Runtime Proposal、System Probe 和内部取消/派发/事件入口 |
| 本批次新增或补齐的 service 能力 | 28 | 会话分页/消息写入、计划生命周期、Agent/审批/反馈、Admin 治理和保留策略等方法 |

状态说明：

- `已封装（既有）`：已有真实 service 方法，页面或真实模式已有调用证据。
- `已封装（本批次）`：本批次新增或补齐了类型化方法，并有请求契约测试。
- `待页面接入`：service 已就绪，但真实页面还没有把主流程切换到该方法。
- `特殊 302`：接口返回重定向资源，不是 JSON，必须作为图片地址使用。
- `内部服务调用`：只允许 Java/Python 服务间调用，不提供浏览器 service。

## 3. Auth 与账户

| # | 方法 | Figma/页面入口 | 接口路径 | 前端 service | 权限与关键契约 | 状态 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | POST | `/register` | `/api/auth/register` | `authService.register` | 未登录；JSON `username/email/password` | 已封装（既有） |
| 2 | POST | `/login` | `/api/auth/login` | `authService.login` | 未登录；JSON `username_or_email/password` | 已封装（既有） |
| 3 | POST | `/login` 登出 | `/api/auth/logout` | `authService.logout` | 已登录；Cookie/CSRF | 已封装（既有） |
| 4 | POST | 登录态恢复 | `/api/auth/refresh` | `apiClient.refreshAuthSession` | Refresh Cookie；401 时只允许单飞刷新 | 已封装（既有） |
| 5 | POST | `/forgot-password` | `/api/auth/password-reset/request` | `authService.requestPasswordReset` | 未登录；JSON `email` | 已封装（既有） |
| 6 | POST | `/reset-password` | `/api/auth/password-reset/confirm` | `authService.confirmPasswordReset` | 未登录；JSON `token/new_password` | 已封装（既有） |
| 7 | GET | Workspace/Profile 初始化 | `/api/users/me` | `authService.loadCurrentUser` | 已登录；返回用户和头像资源地址 | 已封装（既有） |
| 8 | GET | `/profile` 基本资料 | `/api/users/me/profile` | `accountService.getProfile` | 已登录 | 已封装（既有） |
| 9 | PUT | `/profile` 基本资料保存 | `/api/users/me/profile` | `accountService.updateProfile` | 已登录；资料字段校验 | 已封装（既有） |
| 10 | POST | `/profile?tab=security` | `/api/users/me/password` | `accountService.changePassword` | 已登录；当前密码、新密码 | 已封装（既有） |
| 11 | GET | `/profile?tab=security` | `/api/users/me/sessions` | `accountService.getAuthSessions` | 已登录；登录设备列表 | 已封装（既有） |
| 12 | DELETE | `/profile?tab=security` | `/api/users/me/sessions/{id}` | `accountService.revokeAuthSession` | 已登录；撤销指定设备 | 已封装（既有） |
| 13 | POST | `/profile?tab=security` | `/api/users/me/sessions/revoke-all` | `accountService.revokeAllAuthSessions` | 已登录；撤销全部设备 | 已封装（既有） |
| 14 | POST | `/profile` 头像上传 | `/api/users/me/avatar` | `accountService.uploadAvatar` | `multipart/form-data`；主动上传才允许临时预览 | 已封装（既有） |
| 15 | GET | `/profile` 头像展示 | `/api/users/me/avatar` | `accountService.getAvatarUrl`、`authService.loadCurrentUser`、`AvatarImage` | 已登录；服务端返回 `302 Location`，不走 JSON 解析；真实模式保留受信任端点 | 已接入真实页面（头像资源 302） |
| 16 | DELETE | `/profile` 头像删除 | `/api/users/me/avatar` | `accountService.deleteAvatar` | 已登录；Cookie/CSRF | 已封装（既有） |
| 17 | POST | `/profile?tab=privacy` | `/api/users/me/export` | `accountService.requestDataExport` | 已登录；异步导出任务 | 已封装（既有） |
| 18 | GET | `/profile?tab=privacy` | `/api/users/me/export/{id}` | `accountService.getDataExport` | 已登录；轮询导出状态 | 已封装（既有） |
| 19 | POST | `/profile?tab=privacy` | `/api/users/me/export/{id}/download` | `accountService.downloadDataExport` | 已登录；一次性下载地址 | 已封装（既有） |
| 20 | POST | `/profile?tab=privacy` | `/api/users/me/deletion` | `accountService.requestAccountDeletion` | 已登录；`DELETE_MY_ACCOUNT`、当前密码 | 已封装（既有） |

## 4. 会话与消息

| # | 方法 | Figma/页面入口 | 接口路径 | 前端 service | 权限与关键契约 | 状态 |
| ---: | --- | --- | --- | --- | --- | --- |
| 21 | GET | Workspace/Chat 侧栏 | `/api/sessions` | `sessionService.loadSessionsPage/loadSessions` | 已登录；`page/size/q/status` | 已接入真实页面（2026-09-14） |
| 22 | GET | Profile/归档入口 | `/api/sessions/deleted` | `sessionService.loadDeletedSessionsPage/loadDeletedSessions` | 已登录；分页 | 已接入全量分页读取（2026-09-14） |
| 23 | GET | Workspace 会话搜索 | `/api/sessions/search` | `sessionService.searchSessions` | 已登录；`q/page/size`；分页响应 | 已接入真实分页响应（2026-09-14） |
| 24 | POST | 新建 Chat 会话 | `/api/sessions` | `sessionService.createSession` | 已登录；JSON `title/mode` | 已接入真实页面和重复提交防护（2026-09-14） |
| 25 | PATCH | 会话重命名 | `/api/sessions/{sessionId}` | `sessionService.renameSession` | 会话所有者；JSON `title` | 已接入真实页面和重复提交防护（2026-09-14） |
| 26 | POST | 会话归档 | `/api/sessions/{sessionId}/archive` | `sessionService.archiveSession` | 会话所有者 | 已接入真实页面和重复提交防护（2026-09-14） |
| 27 | POST | 会话恢复为 active | `/api/sessions/{sessionId}/unarchive` | `sessionService.unarchiveSession` | 会话所有者 | 已接入真实页面和重复提交防护（2026-09-14） |
| 28 | DELETE | 会话删除 | `/api/sessions/{sessionId}` | `sessionService.deleteSession` | 会话所有者；软删除 | 已接入真实页面和重复提交防护（2026-09-14） |
| 29 | POST | 已删除会话恢复 | `/api/sessions/{sessionId}/restore` | `sessionService.restoreSession` | 会话所有者 | 已接入真实页面和重复提交防护（2026-09-14） |
| 30 | GET | Chat 消息历史 | `/api/sessions/{sessionId}/messages` | `sessionService.loadSessionMessagesPage/loadSessionMessages` | 会话所有者；`page/size`；按 `sequence_no` 排序 | 已接入全量分页读取（2026-09-14） |
| 31 | POST | Chat 发送消息 | `/api/sessions/{sessionId}/messages` | `sessionService.sendUserMessage` | 只接受 `role=user`；真实模式触发 AgentRun | 已封装（既有） |
| 32 | PATCH | Chat 消息编辑 | `/api/sessions/{sessionId}/messages/{messageId}` | `sessionService.updateMessage` | 会话所有者；JSON `content` | 已接入真实页面（a6c7dc61） |
| 33 | DELETE | Chat 消息删除 | `/api/sessions/{sessionId}/messages/{messageId}` | `sessionService.deleteMessage` | 会话所有者；软删除/审计以服务端为准 | 已接入真实页面（a6c7dc61） |

## 5. Chat、AgentRun 与审批

| # | 方法 | Figma/页面入口 | 接口路径 | 前端 service | 权限与关键契约 | 状态 |
| ---: | --- | --- | --- | --- | --- | --- |
| 34 | POST | `/chat?transport=chat-run` 发送请求 | `/api/chat/runs` | `chatApi.createChatRun`、`ChatRunPage` | 已登录；不把接受响应当终态 | 已接入真实页面（本批次） |
| 35 | GET | `/chat?transport=chat-run` 运行状态 | `/api/chat/runs/{runId}` | `chatApi.getChatRun`、`ChatRunPage` | Run 所有者；状态查询不能替代 SSE 终态 | 已接入真实页面（本批次） |
| 36 | GET | `/chat?transport=chat-run` 历史事件 | `/api/chat/runs/{runId}/events` | `chatApi.getChatRunEvents`、`ChatRunPage` | Run 所有者；回放后按游标续接 | 已接入真实页面（本批次） |
| 37 | POST | `/chat?transport=chat-run` 取消运行 | `/api/chat/runs/{runId}/cancel` | `chatApi.cancelChatRun`、`ChatRunPage` | Run 所有者；等待服务端 cancelled 事件 | 已接入真实页面（本批次） |
| 38 | GET | `/chat?transport=chat-run` 事件流 | `/api/chat/runs/{runId}/stream` | `chatApi.streamChatRun`、`ChatRunPage` | SSE；支持 Last-Event-ID、去重、重连和终态关闭 | 已接入真实页面（本批次） |
| 39 | GET | AgentRun 状态 | `/api/agent-runs/{runId}` | `agentRunService.loadAgentRun` | Run 所有者；返回 `accepted_event_count` | 已封装（本批次） |
| 40 | POST | Agent 取消 | `/api/agent-runs/{runId}/cancel` | `agentRunService.cancelAgentRun` | Run 所有者；JSON `reason` | 已封装（本批次） |
| 41 | POST | 预算追加 | `/api/agent-runs/{runId}/budget-extensions` | `agentRunService.extendAgentRunBudget` | Run 所有者；请求 DTO 使用驼峰字段、`sha256:` 摘要 | 已封装（本批次） |
| 42 | POST | 显式 checkpoint 恢复 | `/api/agent-runs/{runId}/recover` | `agentRunService.recoverAgentRun`、`RealAgentStatePage`、`RealChatPage` | Run 所有者；请求 DTO 使用 `checkpoint_version`、`checkpoint_digest`、`completed_invocation_ids` 蛇形字段 | 已接入真实页面（本批次） |
| 43 | POST | 持久化 checkpoint 恢复 | `/api/agent-runs/{runId}/recover-from-checkpoint` | `agentRunService.recoverAgentRunFromCheckpoint`、`RealAgentStatePage`、`RealChatPage` | Run 所有者；浏览器不提交 checkpoint 内容 | 已接入真实页面（本批次） |
| 44 | GET | AgentRun V1 事件流 | `/api/agent-runs/{runId}/stream` | `agentRunService.openAgentRunStream` | SSE；共享连接生命周期，`Last-Event-ID` 由浏览器查询参数兼容传递 | 已封装（本批次） |
| 44a | POST | Agent 失败重试 | `/api/agent-runs/{runId}/retry` | `agentRunService.retryAgentRun`、`ChatPage`、`RealAgentStatePage` | Run 所有者；仅后端明确 `retryable=true` 的失败 Run 可调用；接受响应不代表运行终态 | 已接入真实页面（8b531dc6） |
| 45 | POST | Agent 回答反馈 | `/api/agent-runs/{runId}/messages/{messageId}/feedback` | `agentRunService.submitAgentFeedback` | 只提交结构化反馈，不提交回答正文；幂等键 | 已封装（本批次） |
| 46 | POST | 创建写入提案 | `/api/approvals/proposals` | `agentRunService.createApprovalProposal` | 已登录；`operation/resource/parameters`，服务端生成摘要 | 已接入真实页面（c9ec284d） |
| 47 | GET | 查询写入提案 | `/api/approvals/{approvalRequestId}` | `agentRunService.loadApprovalProposal` | 提案所有者 | 已接入真实页面（c9ec284d） |
| 48 | POST | 确认写入提案 | `/api/approvals/{approvalRequestId}/confirm` | `agentRunService.confirmAgentWrite` | 只改变确认状态，不代表业务写入已完成 | 已封装（既有） |
| 49 | POST | 拒绝写入提案 | `/api/approvals/{approvalRequestId}/reject` | `agentRunService.rejectAgentWrite` | 只改变提案状态 | 已封装（既有） |
| 50 | POST | 执行写入提案 | `/api/approvals/{approvalRequestId}/execute` | `agentRunService.executeAgentWrite` | 确认后执行真实业务写入，不能前端伪造成功 | 已封装（既有） |

## 6. 饮食、分析与记忆

| # | 方法 | Figma/页面入口 | 接口路径 | 前端 service | 权限与关键契约 | 状态 |
| ---: | --- | --- | --- | --- | --- | --- |
| 51 | POST | Diet Records 新增 | `/api/food-logs` | `foodLogService.createFoodLog` | 用户数据；`Idempotency-Key` | 已封装（既有） |
| 52 | GET | Diet Records 列表 | `/api/food-logs` | `foodLogService.loadFoodLogs` | 用户数据；`from/to` | 已封装（既有） |
| 53 | GET | Diet Records 回收站 | `/api/food-logs/deleted` | `foodLogService.loadDeletedFoodLogs` | 用户数据 | 已封装（既有） |
| 54 | PATCH | Diet Records 编辑 | `/api/food-logs/{foodLogId}` | `foodLogService.updateFoodLog` | `revision` 乐观锁；幂等键 | 已封装（既有） |
| 55 | DELETE | Diet Records 删除 | `/api/food-logs/{foodLogId}` | `foodLogService.deleteFoodLog` | `revision` 乐观锁；软删除 | 已封装（既有） |
| 56 | POST | Diet Records 恢复 | `/api/food-logs/{foodLogId}/restore` | `foodLogService.restoreFoodLog` | `revision` 乐观锁；幂等键 | 已封装（既有） |
| 57 | POST | 复合菜新增 | `/api/composite-dishes` | `compositeDishService.createCompositeDish`、DietRecordsPage | 营养目录 ID 必须来自后端搜索结果 | 已接入真实页面 |
| 58 | GET | 复合菜列表 | `/api/composite-dishes` | `compositeDishService.loadCompositeDishes`、DietRecordsPage | 用户数据；失败可重试 | 已接入真实页面 |
| 59 | GET | 复合菜详情 | `/api/composite-dishes/{compositeDishId}` | `compositeDishService.loadCompositeDish`、`DietRecordsPage` | 用户数据；编辑复合菜时读取详情和最新 revision | 已接入真实页面（本批次） |
| 60 | PATCH | 复合菜编辑 | `/api/composite-dishes/{compositeDishId}` | `compositeDishService.updateCompositeDish`、DietRecordsPage | `revision` 乐观锁；幂等键 | 已接入真实页面 |
| 61 | DELETE | 复合菜删除 | `/api/composite-dishes/{compositeDishId}` | `compositeDishService.deleteCompositeDish`、DietRecordsPage | `revision` 乐观锁；软删除 | 已接入真实页面 |
| 62 | POST | 餐食计划创建 | `/api/meal-plans` | `planningService.createMealPlan` | local profile；`Idempotency-Key`；日期转 `days` | 已封装（既有），PlanningPage 已接入真实新建 |
| 63 | GET | 餐食计划列表 | `/api/meal-plans` | `planningService.loadMealPlans` | 用户数据 | 已封装（既有），PlanningPage 已接入真实列表 |
| 64 | GET | 餐食计划详情 | `/api/meal-plans/{mealPlanId}` | `planningService.loadMealPlan` | 用户数据 | 已封装（既有），PlanningPage 已接入真实详情 |
| 65 | PATCH | 餐食计划修改 | `/api/meal-plans/{mealPlanId}` | `planningService.updateMealPlan` | `revision` 乐观锁；幂等键 | 已封装（本批次），PlanningPage 已接入真实编辑 |
| 66 | POST | 计划校验 | `/api/meal-plans/{mealPlanId}/validate` | `planningService.validateMealPlan` | `revision`；幂等键；返回校验事实 | 已封装（本批次），PlanningPage 已接入校验 |
| 67 | POST | 计划保存 | `/api/meal-plans/{mealPlanId}/save` | `planningService.saveMealPlan` | `revision`；幂等键；不能提前声称已保存 | 已封装（本批次），PlanningPage 已接入保存 |
| 68 | DELETE | 计划删除 | `/api/meal-plans/{mealPlanId}` | `planningService.deleteMealPlan` | `revision`；幂等键；软删除 | 已封装（本批次），PlanningPage 已接入删除确认 |
| 69 | POST | 计划恢复 | `/api/meal-plans/{mealPlanId}/restore` | `planningService.restoreMealPlan` | `revision`；幂等键 | 已封装（本批次），PlanningPage 已接入恢复 |
| 70 | POST | 生成购物清单 | `/api/meal-plans/{mealPlanId}/shopping-list` | `planningService.createShoppingList` | 用户数据；后端生成结果 | 已封装（本批次），PlanningPage 已接入生成/刷新 |
| 71 | GET | 读取购物清单 | `/api/meal-plans/{mealPlanId}/shopping-list` | `planningService.loadShoppingList` | 用户数据 | 已封装（既有），PlanningPage 已接入真实读取 |
| 72 | GET | 计划完成进度 | `/api/meal-plans/{mealPlanId}/progress` | `planningService.loadMealPlanProgress` | 用户数据；兼容后端 record 的驼峰嵌套字段 | 已封装（本批次），PlanningPage 已接入进度展示 |
| 73 | PATCH | 购物项勾选 | `/api/meal-plans/{mealPlanId}/shopping-list/items/{shoppingListItemId}` | `planningService.updateShoppingItemPurchased` | 幂等键；JSON `purchased` | 已封装（既有），PlanningPage 已接入真实更新 |
| 74 | GET | Intake Analysis/Home | `/api/nutrition-analysis` | `analysisService.loadNutritionAnalysis` | `range=today/7d/30d`；真实数据可能不完整 | 已接入真实页面（2026-09-14） |
| 75 | GET | 食材候选搜索 | `/api/nutrition-foods/search` | `nutritionFoodService.searchNutritionFoods` | `query/limit`；只读审核目录 | 已封装（既有） |
| 76 | GET | Profile 记忆列表 | `/api/memories` | `memoryService.loadMemories`、`RealMemoriesTab` | 用户数据；支持取消、请求序列和失败重试 | 已接入真实页面（a003925d） |
| 77 | PATCH | Profile 记忆编辑 | `/api/memories/{memoryId}` | `memoryService.updateMemory`、`RealMemoriesTab` | 用户数据；服务端记录确认状态；卸载时取消后续写回 | 已接入真实页面（a003925d） |
| 78 | DELETE | Profile 记忆删除 | `/api/memories/{memoryId}` | `memoryService.deleteMemory`、`RealMemoriesTab` | 用户数据；软删除/审计；卸载时取消后续写回 | 已接入真实页面（a003925d） |
| 79 | POST | Profile 记忆确认 | `/api/memories/{memoryId}/confirm` | `memoryService.confirmMemory`、`RealMemoriesTab` | 用户明确确认；冲突状态以服务端刷新结果为准 | 已接入真实页面（a003925d） |

## 7. Knowledge

| # | 方法 | Figma/页面入口 | 接口路径 | 前端 service | 权限与关键契约 | 状态 |
| ---: | --- | --- | --- | --- | --- | --- |
| 80 | POST | Knowledge 搜索 | `/api/knowledge-base/search` | `knowledgeService.searchKnowledge`、`KnowledgePage` | 已登录；只搜索 Java 授权的公共发布范围；真实引用仅展示后端返回字段和错误码 | 已接入真实页面（本批次） |
| 81 | POST | Admin Knowledge 单文件上传 | `/api/admin/knowledge` | `adminService.uploadKnowledgeDocument`、`KnowledgeSection` | Admin/Superadmin；`multipart/form-data`；真实页面支持单文件上传 | 已接入真实页面（本批次） |
| 82 | PATCH | Admin Knowledge 状态 | `/api/admin/knowledge/{id}/status` | `adminService.updateKnowledgeStatus`、`KnowledgeSection` | Admin/Superadmin；状态由后端枚举校验；真实操作后刷新列表 | 已接入真实页面 |
| 83 | POST | Admin Knowledge 批量上传 | `/api/admin/knowledge-documents/upload-batches` | `adminService.uploadKnowledgeBatch`、`KnowledgeSection` | Admin/Superadmin；最多 20 个文件；来源与许可证字段 | 已接入真实页面 |
| 84 | POST | 发布文档 | `/api/admin/knowledge-documents/{id}/publish` | `adminService.changeKnowledgeVisibility`、`KnowledgeSection` | Admin/Superadmin；真实生命周期 | 已接入真实页面 |
| 85 | POST | 下线文档 | `/api/admin/knowledge-documents/{id}/disable` | `adminService.changeKnowledgeVisibility`、`KnowledgeSection` | Admin/Superadmin；真实生命周期 | 已接入真实页面 |
| 86 | POST | 恢复文档为草稿 | `/api/admin/knowledge-documents/{id}/restore` | `adminService.changeKnowledgeVisibility`、`KnowledgeSection` | Admin/Superadmin；真实生命周期 | 已接入真实页面 |
| 87 | POST | 删除文档 | `/api/admin/knowledge-documents/{id}/delete` | `adminService.changeKnowledgeVisibility`、`KnowledgeSection` | Admin/Superadmin；软删除 | 已接入真实页面 |
| 88 | GET | 批次详情 | `/api/admin/knowledge-upload-batches/{batchId}` | `adminService.loadKnowledgeBatch`、`KnowledgeSection` | Admin/Superadmin；支持错误码、过期终态和重新加载 | 已接入真实页面（本批次） |
| 89 | POST | 批次文档重试 | `/api/admin/knowledge-upload-batches/{batchId}/documents/{documentId}/retry` | `adminService.retryKnowledgeItem`、`KnowledgeSection` | Admin/Superadmin；只允许失败项重试；操作后刷新详情 | 已接入真实页面 |
| 90 | POST | 批次文档重索引 | `/api/admin/knowledge-upload-batches/{batchId}/documents/{documentId}/reindex` | `adminService.reindexKnowledgeItem`、KnowledgeTab | Admin/Superadmin；仅 `indexed`/`index_failed` 条目；返回 `pending` 后刷新并恢复 SSE | 已接入真实页面（517c3d9f） |
| 91 | GET | 批次进度 SSE | `/api/admin/knowledge-upload-batches/{batchId}/events` | `adminService.streamKnowledgeBatch`、KnowledgeTab | Admin/Superadmin；共享 `lastEventId`、事件去重、重连上限和终态关闭 | 已接入真实页面 |

## 8. Admin 概览、查询与写操作

| # | 方法 | Figma/页面入口 | 接口路径 | 前端 service | 权限与关键契约 | 状态 |
| ---: | --- | --- | --- | --- | --- | --- |
| 92 | GET | `/admin?view=overview` | `/api/admin/dashboard` | `adminService.loadAdminDashboard` | Admin/Operator/Superadmin；概览聚合 | 已封装（既有） |
| 93 | GET | `/admin?view=users` | `/api/admin/users` | `adminService.loadAdminUsers/loadAdminUsersPage` | Admin/Operator/Superadmin；分页查询 | 已封装（既有） |
| 94 | GET | `/admin?state=user-detail` | `/api/admin/users/{id}/detail` | `adminService.loadAdminUserDetail` | Admin/Operator/Superadmin；用户详情脱敏 | 已封装（既有） |
| 95 | PATCH | Admin 用户状态 | `/api/admin/users/{id}/status` | `adminService.updateAdminUserStatus` | Admin/Superadmin；revision、确认摘要、幂等键 | 已封装（既有） |
| 96 | POST | Admin 撤销用户会话 | `/api/admin/users/{id}/sessions/revoke-all` | `adminService.revokeAdminUserSessions` | Admin/Superadmin；revision、确认摘要 | 已封装（既有） |
| 97 | GET | `/admin?view=tools` | `/api/admin/tools/registry` | `adminService.loadAdminToolRegistry` | Admin/Operator/Superadmin；工具注册表 | 已封装（既有） |
| 98 | PATCH | 工具启停 | `/api/admin/tools/{name}/status` | `adminService.updateAdminToolStatus` | Admin/Superadmin；确认摘要、幂等键 | 已封装（既有） |
| 99 | POST | 删除资源恢复 | `/api/admin/resources/{type}/{id}/restore` | `adminService.restoreAdminResource` | Admin/Superadmin；资源类型和 revision | 已封装（既有） |
| 100 | GET | `/admin?view=runs/tools/sql/traces/dlq` | `/api/admin/queries/{resource}` | `adminService.loadAdminQuery` | Admin/Operator/Superadmin；分页、查询、状态、角色、时间筛选 | 已封装（既有） |
| 101 | GET | Admin Trace 详情 | `/api/admin/queries/traces/{traceId}` | `adminService.loadAdminTraceDetail` | Admin/Operator/Superadmin；不返回敏感 Prompt | 已封装（既有） |
| 102 | GET | `/admin?view=audit` | `/api/admin/audit-reports/current` | `adminService.loadAdminAuditReport` | Admin/Operator/Superadmin；只读聚合报告 | 已接入真实页面（cdc1bb43） |
| 103 | POST | DLQ 重放 | `/api/admin/dlq/{dlqId}/replay` | `adminService.replayAdminDlq` | 仅 Superadmin；服务端稳定确认摘要、幂等键 | 已接入真实页面（cdc1bb43） |
| 104 | POST | Admin 导出创建 | `/api/admin/exports` | `adminService.requestAdminExport` | Admin/Superadmin；异步任务、字段白名单 | 已封装（既有） |
| 105 | GET | Admin 导出状态 | `/api/admin/exports/{jobId}` | `adminService.loadAdminExportStatus` | Admin/Superadmin | 已封装（既有） |
| 106 | POST | Admin 导出下载 | `/api/admin/exports/{jobId}/download` | `adminService.downloadAdminExport` | Admin/Superadmin；消费一次性地址 | 已封装（既有） |
| 107 | GET | `/admin?view=model-governance` | `/api/admin/model-governance` | `adminService.loadModelGovernance` | Operator/Admin/Superadmin；可选 `from/to` | 已封装（本批次增强） |
| 108 | PATCH | 模型供应商启停 | `/api/admin/model-governance/providers/{providerCode}/status` | `adminService.updateModelProviderStatus` | 仅 Superadmin；revision/确认摘要/幂等键 | 已封装（既有） |
| 109 | PATCH | 模型目录启停 | `/api/admin/model-governance/models/{id}/status` | `adminService.updateModelCatalogStatus` | 仅 Superadmin；revision/确认摘要/幂等键 | 已封装（既有） |
| 110 | PUT | 模型路由修改 | `/api/admin/model-governance/routes/{id}` | `adminService.updateModelRoute` | 仅 Superadmin；revision/确认摘要/幂等键 | 已封装（既有） |
| 111 | POST | 模型价格创建 | `/api/admin/model-governance/prices` | `adminService.createModelPrice`、`ModelGovernanceSection` | 仅 Superadmin；版本确认摘要、revision、幂等键 | 已接入真实页面（bb036c49） |
| 112 | POST | 模型预算创建 | `/api/admin/model-governance/budgets` | `adminService.createModelBudget`、`ModelGovernanceSection` | 仅 Superadmin；scope/window/版本确认摘要、revision、幂等键 | 已接入真实页面（bb036c49） |
| 113 | POST | 清理请求创建 | `/api/admin/data-retention/purge-requests` | `adminService.requestRetentionPurge`、`RetentionSection` | Admin/Superadmin；资源类型、确认摘要、幂等键 | 已接入真实页面（1a99103c） |
| 114 | GET | 清理请求详情 | `/api/admin/data-retention/purge-requests/{requestId}` | `adminService.loadRetentionPurge`、`RetentionSection` | Admin/Operator/Superadmin | 已接入真实页面（1a99103c） |
| 115 | GET | 清理请求预检 | `/api/admin/data-retention/purge-requests/{requestId}/preflight` | `adminService.loadRetentionPurgePreflight`、`RetentionSection` | Admin/Operator/Superadmin；只读，不产生副作用 | 已接入真实页面（1a99103c） |
| 116 | POST | 清理请求审批 | `/api/admin/data-retention/purge-requests/{requestId}/approve` | `adminService.approveRetentionPurge`、`RetentionSection` | 仅 Superadmin；确认摘要、幂等键 | 已接入真实页面（1a99103c） |
| 117 | POST | 创建法律保留 | `/api/admin/data-retention/holds` | `adminService.placeRetentionHold`、`RetentionSection` | Admin/Superadmin；资源与原因确认 | 已接入真实页面（1a99103c） |
| 118 | POST | 释放法律保留 | `/api/admin/data-retention/holds/{holdId}/release` | `adminService.releaseRetentionHold`、`RetentionSection` | 仅 Superadmin；确认摘要、幂等键 | 已接入真实页面（1a99103c） |

## 9. 内部服务接口

以下接口虽然计入后端 Controller 总数，但不是浏览器端 API。浏览器不得从 `foodmate-ui` 直接调用；Java API 通过服务凭据、内部网络边界或消息链路与 Runtime 交互。

| # | 方法 | 接口路径 | 调用方 | 浏览器前端状态 |
| ---: | --- | --- | --- | --- |
| 119 | GET | `/foodmate/_system/ping` | 运行时探针/部署检查 | 内部服务调用 |
| 120 | POST | `/foodmate/internal/v1/agent-events` | Python Runtime -> Java Runtime Gateway | 内部服务调用 |
| 121 | POST | `/foodmate/internal/v1/proposals` | Python Runtime -> Java Proposal Gateway | 内部服务调用 |
| 122 | POST | `/foodmate/internal/v1/runtime/recovered` | Python Runtime -> Java Runtime Gateway | 内部服务调用 |
| 123 | POST | `/internal/runtime/runs:cancel` | Java -> Runtime Gateway | 内部服务调用 |
| 124 | POST | `/internal/runtime/runs:dispatch` | Java -> Runtime Gateway | 内部服务调用 |
| 125 | POST | `/internal/runtime/runs:events` | Java -> Runtime Gateway | 内部服务调用 |

> 注：当前 Controller 映射接口总数为 `126`，其中浏览器可关联接口 `119`、内部接口 `7`。新增的 Agent 重试接口为 `44a`，用于保持既有历史编号稳定；编号不是统计口径，路径和数量才是权威字段。

### 9.1 当前接口覆盖审计基线（2026-09-14）

| 审计项 | 当前结论 | 证据边界 |
| --- | --- | --- |
| Controller 映射数量 | `126` | 以 `foodmate-api/src/main/java/com/foodmate/api/controller` 的方法级映射扫描为准；额外核对出 Agent 重试映射 |
| 浏览器可关联接口 | `119` | Auth、用户、会话、饮食、知识库、Agent 和 Admin 接口；必须继续核对页面主流程和状态证据 |
| 内部服务接口 | `7` | 只通过 Java/Python 服务链路或后端测试闭环，不能创建浏览器 service |
| 数据保留接口 `113-118` | 已接入真实 Admin 页面 | `RetentionSection` 调用六个真实 service 方法；`RetentionTab.real.test.tsx` 覆盖请求、权限和刷新行为；实现提交为 `1a99103c` |
| 全部后端生产可用性 | 尚未宣告完成 | 接口存在和 service 已封装不等于真实环境部署、数据库、鉴权、幂等和端到端行为均已验收 |

本基线只修正当前事实，不改写前面按日期记录的历史批次结论。后续页面迁移必须把“已封装”进一步升级为“已接入真实页面”，并同时留下成功、加载、空态、错误、权限和冲突证据。

## 10. 关键契约规则

### 10.1 统一 HTTP 边界

- JSON 请求统一经过 `foodmate-ui/src/services/apiClient.ts` 的 `apiRequest`。
- 非安全方法自动携带 `credentials: include` 和 `X-CSRF-Token`。
- 401 只触发一次 refresh；refresh 失败后清理本地登录缓存并跳转登录页。
- 403、网络失败、业务错误码和空响应不能被 service 静默吞掉。
- 上传接口保留 `FormData`，不能手工设置 multipart boundary。

### 10.2 JSON 命名

- 明确使用 `@JsonNaming(SNAKE_CASE)` 的后端 DTO：恢复请求、会话、饮食记录、餐食计划和保留策略等使用蛇形字段。
- 未声明 `@JsonNaming` 的后端 DTO：预算追加、取消、审批、模型治理和部分 Admin 写请求使用 Java 默认驼峰字段。
- SSE payload 的领域字段以事件协议为准，不能按页面变量名随意重命名。

### 10.3 写入与幂等

- `revision` 是后端乐观锁事实，前端不得固定写死并吞掉冲突。
- 需要幂等键的写操作由 service 生成唯一键；用户重试必须保持业务动作独立，不能把 HTTP 200 接受响应当成业务完成。
- Agent 写入必须遵循 `proposal -> confirm/reject -> execute`；只有 execute 返回后才能刷新业务数据。
- Admin 高风险写操作使用后端约定的确认摘要；摘要算法只用于构造请求，最终有效性由后端校验。

### 10.4 SSE

- 页面保留已显示文本，重连使用最后一个服务端事件 ID。
- `sse_event_id`、SSE message ID 和业务事件 ID 按 service 层规则去重。
- `completed/failed/cancelled/superseded` 是终态；收到终态后关闭连接。
- 组件卸载、会话切换和用户取消必须关闭连接和定时器。

## 11. 后端完成性判定

当前清单证明的是“接口路径已登记、前端 service 契约已建立或已有实现”。后端是否可交付仍需以以下证据为准：

1. 受影响模块测试通过。
2. `mvnw.cmd verify` 通过，包含 Spotless 和架构检查。
3. 真实 local profile 下接口鉴权、数据库、幂等、revision 和错误码行为通过定向 E2E。
4. 前端真实模式页面能够消费响应并处理 loading、空态、错误和权限状态。

本批次前端定向证据：`6` 个 service/组件测试文件、`29/29` 用例通过；完整前端回归、类型检查、构建和页面逐域接入在本批次收尾时继续执行。

## 2026-09-13 接口统计与 HTTP 边界校准

- [x] 重新扫描 `foodmate-api` Controller 的方法级映射，确认实际数量为 `125`；其中 Runtime Gateway、Runtime Proposal、System Probe 共 `7` 个内部入口，浏览器可关联接口为 `118`。
- [x] 补计 `UserController` 中使用全限定 `@PostMapping` 声明的 `/api/users/me/avatar` 上传接口；该接口已有 `accountService.uploadAvatar` 封装，不新增虚构接口。
- [x] 账户、Chat HTTP 和 Knowledge multipart 请求统一经过 `foodmate-ui/src/services/apiClient.ts`，共享 Cookie、CSRF、401 刷新、403 和业务错误处理；头像 GET 仍保留独立 302 资源地址，不按 JSON 解析。
- [x] 取消和预算追加响应按后端实际 DTO 命名归一化：`CancelResult`、`ExtensionResult` 使用默认驼峰返回，RecoveryResult 继续使用蛇形返回；新增测试验证两种边界不会泄漏到页面契约。
- [x] 当前状态仍只证明接口登记和 service 边界完成，不代表所有页面已接入、后端生产部署已验收或 iconfont 阻塞已解除。

## 12. 2026-09-13 复合菜真实闭环证据

- 实现提交：`9c764f2d feat(frontend): 收口复合菜真实闭环`。
- 页面和 service：`DietRecordsPage`、`compositeDishService`；覆盖复合菜列表、新建、编辑、删除和记录这道菜入口。
- 真实状态：列表加载失败可重试；创建、编辑、删除成功后重新读取服务端列表；编辑和删除使用服务端 `revision`；冲突和权限错误保留当前操作上下文并显示明确提示。
- 删除边界：当前后端没有复合菜恢复接口，因此页面不虚构恢复操作；已有饮食记录的营养快照不随复合菜删除而删除。
- 前端定向验证：DietRecordsPage 与 `compositeDishService` 共 `3` 个文件、`30/30` 通过。
- 前端门禁：`npm run typecheck`、`npm run lint`、`npm run format:check`、`npm run build`、`git diff --check` 通过。
- 后端定向验证：`CompositeDishServiceImplTest 2/2`、`CompositeDishControllerTest 4/4`，Maven reactor `BUILD SUCCESS`；Spotless 已通过。
- 本批次不执行 105 个画板全量像素差异，也不执行与花瓣的像素对比；iconfont 实体包、CSS 映射、来源和许可证继续保持 `BLOCKED`。

## 13. 2026-09-13 Admin 知识库批次 SSE 真实接入

- 实现提交：`6fd41d5e feat(admin): 统一知识库批次 SSE 生命周期`。
- `adminService.streamKnowledgeBatch` 已复用 `openSseStream`，支持 `connecting`、`connected`、`reconnecting`、`closed`、`exhausted`，使用 `Last-Event-ID` 续接并按事件 ID 去重。
- `KnowledgeTab` 批次进度区域已显示连接状态；连接耗尽后可保留游标手动重连，条目重试成功后会重新订阅批次流。
- `knowledge.batch.progress` 的 `completed`、`partial_failed` 和 `failed` 被视为批次终态；详情刷新仍是批次权威状态来源，未修改后端 SSE 协议。
- 前端定向验证：批次 SSE service 和 `KnowledgeTab.real.test.tsx` 共 `2` 个文件、`3/3` 通过；同时通过 `npm run typecheck`、`npm run lint`、`npm run format:check`、`npm run build` 和 `git diff --check`。
- 本批次不执行 105 个画板全量像素差异，也不执行与花瓣的像素对比；iconfont 实体包、CSS 映射、来源和许可证继续保持 `BLOCKED`。

## 14. 后续顺序

1. 按 Auth -> Workspace/Chat -> Diet/Analysis/Planning -> Knowledge/Profile -> Admin 逐域完成 shadcn 控件迁移和状态复核。
2. 继续补齐各业务域的真实错误、权限、冲突和空态证据，不把历史 Fixture 结果计入真实闭环。
3. 每个业务域完成后运行定向测试、类型检查、构建和 `git diff --check`，单独提交 Git。
5. 继续保持 Figma 视觉参考和 iconfont `BLOCKED` 边界，不恢复 105 画板全量像素验收。

## 2026-09-14 Admin 模型治理价格与预算创建真实页面接入

- [x] `ModelGovernanceSection` 新增模型价格版本和预算策略创建 Dialog；只有 `superadmin` 显示写入口，operator 保持只读。
- [x] 价格创建调用 `POST /api/admin/model-governance/prices`，预算创建调用 `POST /api/admin/model-governance/budgets`；请求携带 `confirmed`、`confirmationDigest`、`revision` 和 `Idempotency-Key`。
- [x] 价格的 `datetime-local` 值在 service 边界转换为后端需要的 ISO 时间；表单校验失败不会发起请求。
- [x] 操作通过 Admin 统一确认状态机；成功后重新读取服务端治理数据，不使用本地 Fixture 伪造新增结果。
- [x] 页面和 service 文件：`foodmate-ui/src/pages/AdminPage/tabs/ModelGovernanceTab.tsx`、`foodmate-ui/src/services/adminService.ts`；后端契约测试文件：`foodmate-api/src/test/java/com/foodmate/api/controller/account/ModelGovernanceControllerTest.java`。
- [x] 前端定向测试 `3` 个文件、`13/13` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过；后端模型治理测试 `5/5`、service 测试 `4/4` 通过。
- [x] 实现提交：`bb036c49`。
- [ ] 数据保留页面、显式 `/recover` 页面仍未完成；本批次不执行 105 个画板全量 PNG/diff 或花瓣像素对比，iconfont 资源登记继续保持 `BLOCKED`。

## 15. 2026-09-13 ChatRun 兼容入口真实页面接入

- [x] 真实模式新增 `/chat?transport=chat-run` 入口；无会话时由后端创建会话和用户消息，成功后跳转到带 `sessionId` 的 ChatRun 页面。
- [x] `chatApi` 已覆盖 ChatRun 创建、状态查询、历史事件、取消和 SSE；兼容蛇形/驼峰响应，不修改后端协议。
- [x] `realAgentService` 已接入历史回放、实时事件、Last-Event-ID 游标、事件去重、重连上限、手动重连、终态关闭、会话切换清理和卸载清理。
- [x] 取消请求保留独立等待态；HTTP 接受响应不被当作 `cancelled` 终态，收到服务端取消事件前保留已接收文本。
- [x] 后端定向测试 `ChatControllerTest`、`RunStreamControllerTest` 共 `3/3` 通过；前端 ChatRun/SSE 定向测试共 `4` 个文件、`21/21` 通过。
- [x] 前端 `typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 均通过；代码提交为 `003797de`。
- [ ] 本阶段不执行 105 个画板全量像素差异，也不执行与花瓣的像素对比；Figma 仍只作为颜色、字体、布局和状态参考，iconfont 继续保持 `BLOCKED`。

## 16. 2026-09-13 Agent 状态动作真实接口收口

- [x] `write-confirmation` 真实动作已按后端两阶段语义执行：先确认提案，再执行真实写入；取消使用拒绝接口，未把本地状态当作写入成功。
- [x] `budget-limit` 真实动作调用预算追加和当前 Run 取消接口；页面展示后端返回的 status/attempt，未创建伪造会话。
- [x] `tool-failed-retryable` 真实重试调用专用 `/api/agent-runs/{runId}/retry`；只有进入该 retryable 状态入口才显示重试动作。
- [x] 后端当前没有跳过单个工具步骤的公开接口；真实模式“跳过此步骤”明确不发请求，Fixture 模式仍只记录视觉预览动作。
- [x] 前端定向验证共 `4` 个文件、`63/63` 通过；后端 ApprovalService `19/19`、V1AgentRunController `5/5` 通过。
- [x] 代码提交：`8b531dc6 feat(frontend): 收口 Agent 状态真实动作`；typecheck、lint、format、build 和 `git diff --check` 通过。
- [ ] Approval proposal 的创建/查询仍需在接口矩阵中区分“运行时生成/浏览器消费”的边界；后续只在确有页面动作和真实响应证据时调整状态。

## 20. 2026-09-14 Agent 状态真实数据页面接入

- [x] `ChatPage` 的六个 Agent 状态页已区分 Fixture 和真实模式：`write-confirmation`、`budget-limit`、`tool-failed-retryable`、`safety-degraded`、`user-cancelled`、`sse-reconnecting`；真实模式缺少 `run_id`、`session_id` 和 `approval_id` 时不会回退显示 Fixture。
- [x] 真实模式已接入 `GET /api/agent-runs/{runId}`、`GET /api/agent-runs/{runId}/stream`、`GET /api/approvals/{approvalRequestId}`、`POST /api/approvals/proposals`、`POST /api/approvals/{approvalRequestId}/confirm|reject|execute`、预算追加、取消、重试和 checkpoint 恢复接口。
- [x] 写入确认遵循后端 `confirm -> execute` 两阶段；拒绝只调用 `reject`；预算追加沿用当前 Run；取消、重试和恢复均展示后端返回的状态，不把 HTTP 接受响应当成运行终态。
- [x] SSE 连接使用 `Last-Event-ID`、`sse_event_id`/`event_id` 去重，处理连接状态、重连次数、游标和终态关闭；当前后端没有公开的“跳过此步骤”接口，真实模式不发送虚构请求。
- [x] Proposal 创建仅接受 URL 中明确提供的 `operation`、`resource_type` 和 JSON `parameters`；Approval 查询不自行补充后端未返回的热量、蛋白质、来源或估算字段。
- [x] 前端定向验证：7 个 Chat/SSE 测试文件、`82/82` 通过；全量 Vitest：58 个文件、`387/387` 通过。
- [x] 前端门禁：`npm run typecheck`、`npm run lint`、`npm run format:check`、`npm run build` 和 `git diff --check` 均通过；实现提交为 `c9ec284d`。
- [ ] 本批次不执行 105 个画板全量 PNG/diff，也不执行与花瓣的像素对比；Figma 只作为视觉、布局、状态和交互参考，iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 21. 2026-09-14 Admin 运行治理与操作审计真实操作

- [x] 实现提交：`cdc1bb43 feat(admin): 接入运行治理与操作审计真实操作`。
- [x] `OperationAuditSection` 真实模式调用 `GET /api/admin/audit-reports/current`，展示服务端报告状态、生成时间、陈旧阈值、检查项、待处理数、失败数和原因码；加载失败支持重试且不回退到 Fixture。
- [x] operator 可以查看操作审计和只读报告；导出仍只对 admin/superadmin 显示，写操作权限没有随只读权限扩大。
- [x] Runs 的 DLQ 表格接入 `POST /api/admin/dlq/{dlqId}/replay`；只有 superadmin、有效 DLQ ID 且状态为 `pending` 或 `needs_attention` 时显示重放入口，操作经过统一确认状态机并携带后端要求的确认摘要和幂等键。
- [x] DLQ 页面只展示安全摘要，不渲染原始消息载荷；真实响应状态由后端返回，不将异步 `queued` 误判为完成。
- [x] 前端定向验证：`OperationAuditTab.test.tsx`、`OperationAuditTab.real.test.tsx`、`RunsTab.real.test.tsx` 共 `3` 个文件、`9/9` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [x] 后端定向验证：`AdminAuditReportControllerTest` `2/2`、`AdminDlqReplayControllerTest` `3/3`，Maven reactor `BUILD SUCCESS`。
- [ ] 本大点未接入知识库重索引、模型价格/预算和数据保留页面；显式 checkpoint `/recover` 仍未接入页面；不执行 105 个画板全量像素差异，也不执行与花瓣的像素对比。
- [ ] iconfont 实体包、CSS/Unicode 映射、来源和许可证继续缺失，资源登记保持 `BLOCKED`。

## 22. 2026-09-14 Admin 数据保留真实页面接入

- [x] `RetentionSection` 已接入清理申请创建、清理详情查询、Preflight 查询、Superadmin 审批、Legal Hold 创建和 Superadmin 释放六个后端操作；真实模式不使用 Fixture 伪造结果。
- [x] Admin/Superadmin 执行后端允许的写操作，operator 仅保留详情和 Preflight 读取；页面只展示策略、保留期、任务状态和稳定阻断码，不展示对象键、目标 JSON、向量或原文。
- [x] 清理申请、审批、Legal Hold 创建和释放复用 Admin 确认状态机；成功后按服务端结果刷新，查询失败会清空旧详情和 Preflight，避免新旧状态混显。
- [x] 前端定向验证：Retention、Admin 和 service 共 `4` 个文件、`38/38` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [x] 后端定向验证：数据保留 Application、Infra、API 测试共 `30/30` 通过，Maven reactor `BUILD SUCCESS`；本批次未修改后端生产代码，也未开启硬删除执行开关。
- [x] 显式 `/api/agent-runs/{runId}/recover` 页面入口已接入；本批次不执行 105 个画板全量 PNG/diff，不执行与花瓣的像素对比，iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 23. 2026-09-14 显式 checkpoint 恢复真实页面接入

- [x] `RealAgentStatePage` 和 `RealChatPage` 已接入显式 `/api/agent-runs/{runId}/recover`；完整 checkpoint 元数据缺失时走既有 `/recover-from-checkpoint`。
- [x] 前端 service 使用后端要求的蛇形字段，页面不提交 checkpoint 内容；恢复结果只展示服务端返回的状态和 dispatch attempt。
- [x] 前端定向验证：4 个文件、`42/42` 通过；typecheck、lint、format、build 和 `git diff --check` 通过。
- [x] 后端定向验证：`RuntimeRecoveryServiceTest` `6/6`、`V1AgentRunControllerTest` `5/5`，Maven reactor `BUILD SUCCESS`。
- [ ] 本批次不执行 105 个画板全量 PNG/diff、不执行花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 24. 2026-09-14 SSE 多标识去重与 Agent 终态收口

- [x] `openSseStream` 同时登记 `MessageEvent.lastEventId`、`sse_event_id` 和业务 `event_id`；任一标识已消费时不重复派发事件，同时推进新的 SSE 游标。
- [x] `agentRunService` 的终态判断同时检查标准终态事件名和 payload `status`，兼容 `run.event` 携带的 `FAILED`、`CANCELLED`、`SUPERSEDED`、`SUCCEEDED` 等大小写变体。
- [x] `agentRunService`、`chatApi` 和 `adminService` 的 parser 均向共享流提供完整事件 ID 集合；不修改后端 SSE 协议和页面业务状态。
- [x] 前端定向验证：3 个 SSE service 测试文件、`22/22` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [x] 本大点未修改 Java 后端代码；后端接口是否达到生产可用仍需按鉴权、数据库、幂等、revision、错误码和端到端证据单独核验。
- [ ] 本大点不执行 105 个画板全量 PNG/diff，不执行花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 25. 2026-09-14 首页真实数据与兼容 Agent 终态

- Home 真实模式并行读取 `/api/sessions` 和 `/api/nutrition-analysis?range=today`；一项失败时保留另一项已成功的数据，并通过明确错误提示提供重试入口。
- 真实首页不渲染 Fixture 待确认饮食项目；营养目标缺失时显示 `--`，不在浏览器端推算进度。
- `agentEvent.ts` 统一处理标准终态事件、`run.event` 外层 `state/status`、嵌套 `payload` 以及 `event_type` 兼容字段；AgentRun 和 ChatRun 页面共用该归一化结果。
- 兼容终态仍遵循 `Last-Event-ID` 和多标识去重规则，收到 `completed/failed/cancelled/superseded` 后关闭连接。
- 前端定向测试为 4 个文件、`39/39` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。本大点未修改 Java 后端。
- 本大点不执行 105 个画板全量 PNG/diff，不执行花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 2026-09-14 Admin 模型用量真实页面接入

- [x] `adminService.loadAdminUsagePage` 已封装 `GET /api/admin/queries/usage`，沿用现有分页、关键词和状态参数，并在 service 边界归一化后端实际字段。
- [x] `UsageSection` 真实模式已切换为服务端用量明细；Figma 模式仍保留原有设计预览数据，两条路径互不回退。
- [x] 真实页面只渲染 `provider`、`model`、`scene`、`tokens`、`cost`、`latency_ms`、`status`；当前页摘要明确标记为当前页范围，不伪造全量成本、趋势或输入/输出拆分。
- [x] 已覆盖真实接口成功、分页/筛选、错误重试和禁止显示 Figma 用量数据；Admin/Profile 的 Fixture query 隔离也有真实模式回归测试。
- [x] 本批次聚焦测试共 `5` 个文件、`38/38` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [x] 接口矩阵中 Admin Usage 从“仅有 service/Fixture 来源”更新为真实页面消费者；本批次没有修改后端 Java 代码。
- [ ] 本批次不执行 105 个画板全量像素差异、不执行花瓣像素对比；iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 2026-09-14 Knowledge 真实错误与批次终态收口

- [x] `KnowledgePage` 真实检索失败时展示后端 `ApiError.code` 和 message；请求失败清空旧结果，不回退到 Fixture 内容。
- [x] Knowledge 引用详情只展示后端实际返回的标题、版本、章节、文档 ID、引用 ID 和摘要；后端未返回来源 URL 时不显示“打开原始来源”。
- [x] `KnowledgeSection` 批次状态覆盖 `uploaded`、`indexing`、`completed`、`partial_failed`、`failed` 和 `expired`；已过期批次显示终态提示，不继续显示“上传已提交”。
- [x] 批次详情读取失败展示稳定错误码，并提供“重新加载批次”操作；重试成功后清除旧错误并恢复服务端详情。
- [x] 前端定向验证：`KnowledgePage.real.test.tsx`、`KnowledgeTab.real.test.tsx`、`knowledgeService.test.ts`、`adminExtendedService.test.ts` 共 `4` 个文件、`15/15` 通过。
- [x] `npm run typecheck`、`npm run lint`、`npm run format:check`、`npm run build` 和 `git diff --check` 通过；本批次未修改 Java 后端生产代码。
- [ ] 本批次不执行 105 个画板全量像素差异，不执行花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证仍缺失，资源登记保持 `BLOCKED`。

## 2026-09-14 Auth、账户与 Profile 真实契约收口

- [x] Profile 更新请求补齐 `allergens`、`dislikes` 和 `preferred_units`；前端提交自然 JSON 数组/对象，后端映射到现有 JSONB 字段，不新增虚构路由。
- [x] API request 使用类型化 `ProfileRequest.PreferredUnits` 表达 `weight` 和 `energy`，符合项目 API 边界 ArchUnit 约束，不使用 `Map` payload。
- [x] Profile 页面读取端兼容 JSON 数组字符串和历史分隔文本，真实保存后使用服务端返回的 Profile 重新建立已保存状态，避免本地表单掩盖持久化失败或服务端归一化。
- [x] 设备会话列表过滤已撤销和已过期数据；真实模式对后端“撤销全部会话”和“修改密码后撤销会话”的结果执行本地身份清理并返回登录页。
- [x] Auth service 覆盖真实登出、密码重置请求和确认请求的契约测试；Profile 真实页面增加 JSON 列表解析、提交和服务端响应回写测试。
- [x] 前端定向测试为 5 个文件、`39/39` 通过；全量 Vitest 为 65 个文件、`428/428` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [x] 后端 `UserAccountServiceImplTest` `7/7`、`P1AccountControllerTest` `8/8` 通过；完整 `mvnw.cmd -P alibaba-code-style verify` 为 `BUILD SUCCESS`，ArchUnit `7/7`，所有模块 Checkstyle `0` 违规。
- 范围声明：本批次没有执行 105 个画板全量像素差异和花瓣对比；Figma 继续作为视觉参考。
- [ ] iconfont 实体包、CSS/Unicode 映射、来源和许可证仍缺失，登记保持 `BLOCKED`。

## 2026-09-14 页面 shadcn 控件边界收口

- 页面、布局和 Agent/Brand/Common/Planning/Workspace 业务组件已完成原生交互控件审计；可见交互控件均通过 `src/components/ui` 下的共享组件承载。
- ESLint 和 `src/architecture/shadcnBoundary.test.ts` 共同阻止页面直接声明原生表单控件、直接引入 `@radix-ui/*` 或重新依赖 `AdminPrimitives`；`src/components/ui` 保留底层 Radix 实现例外。
- Admin 无权限返回入口改用 `Button asChild` 承载 Router `Link`，不再产生 `Link > Button` 的嵌套交互语义。
- 本批次不改变 `126` 个后端映射、`119` 个浏览器接口和 `7` 个内部接口的统计或状态；控件边界收口不等同于接口页面消费者全部完成。
- 定向验证：边界与 Admin 测试共 `2` 个文件、`30/30`；`lint`、`typecheck`、`format:check`、`build` 和 `git diff --check` 通过。
- 本批次不执行 105 个画板全量像素差异或花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 2026-09-14 Auth 真实错误码与回跳验证

- [x] `/api/auth/login` 的 `401 AUTH_INVALID_CREDENTIALS` 不再被 `apiClient` 重定向为登录过期，真实 `LoginPage` 会展示凭证错误状态并允许重新提交。
- [x] `/api/auth/login` 的 `403 AUTH_ACCOUNT_LOCKED`、`AUTH_ACCOUNT_DISABLED` 保留后端错误码，页面分别展示锁定和禁用状态；未新增后端状态或虚构接口。
- [x] 受保护接口的 `401` 仍执行单飞 refresh 和失败回登录；`/api/auth/refresh` 自身失败仍清理会话并回到登录入口。
- [x] 登录成功后的 `redirect` 只接受同源内部路径；真实登录、错误码透传和 API 请求层测试共 `38/38` 通过。
- [x] `npm run typecheck`、`npm run lint`、`npm run format:check`、`npm run build` 和 `git diff --check` 均通过。
- [ ] 本批次只完成 Auth 契约收口，不代表 `119` 个浏览器接口全部完成生产环境端到端验收；iconfont 资料继续保持 `BLOCKED`。

## 2026-09-14 Workspace 会话列表真实状态收口

- [x] 会话搜索清除动作同时重置分页状态，避免查询条件与旧页码不一致。
- [x] 会话列表读取失败时清空旧数据和分页总数，保留可重试错误，不展示过期结果。
- [x] 真实 Session 接口仍由 `WorkspaceLayout`、`sessionService` 和 `SidebarSessionList` 消费；本批次只修复页面状态，不新增后端接口。
- [x] 定向验证：5 个文件、`48/48` 通过；前端 typecheck、lint、format、build 和 `git diff --check` 通过。
- [ ] 不执行 105 个画板全量像素差异或花瓣像素对比；iconfont 继续保持 `BLOCKED`。

## 2026-09-14 Diet Records 与 Intake Analysis 读取生命周期收口

- [x] 饮食记录日期/周视图读取、已删除记录读取、营养目录搜索、复合菜列表和复合菜详情均已通过 service 传递可选 `AbortSignal`。
- [x] Diet Records 使用请求序列和 AbortController 隔离日期切换、候选输入切换、复合菜编辑目标切换、Dialog 关闭、回收站收起和组件卸载；取消请求不显示为网络错误。
- [x] Analysis 的 today/7d/30d 范围切换在加载期间可继续操作，旧请求会被取消，迟到结果不会覆盖当前范围；真实模式错误、空态和服务端结果展示保持不变。
- [x] 失败时清理过期饮食记录、复合菜列表、已删除记录和分析结果；真实模式没有 Fixture 成功回退。
- [x] 定向验证：8 个页面/service 测试文件、`52/52` 通过；`typecheck`、`lint`、`format:check`、`build` 和 `git diff --check` 通过。
- [x] 本批次未新增后端接口、未修改后端协议；视觉检查继续以 Figma 颜色、字体、布局和状态为参考，不执行 105 个画板全量像素差异或花瓣像素对比。
- [ ] iconfont 实体字体包、完整 CSS/Unicode 映射、来源和许可证仍未提供，资源登记继续保持 `BLOCKED`。

## 2026-09-14 Meal Planning 请求生命周期收口

- [x] 计划列表 `GET /api/meal-plans`、计划详情 `GET /api/meal-plans/{id}`、购物清单 `GET/POST /api/meal-plans/{id}/shopping-list`、进度 `GET /api/meal-plans/{id}/progress` 和购物项 `PATCH` 继续使用既有真实 service 与后端接口。
- [x] 上述读取和购物操作支持可选 `AbortSignal`；计划切换、查询重试和组件卸载会取消旧请求，并使用请求序号阻止迟到结果覆盖当前计划。
- [x] 购物清单生成、购物项勾选、计划校验/保存/删除/恢复和草稿保存均保持服务端 revision、幂等键和响应事实，不将取消或 HTTP 接受响应当作业务成功。
- [x] 真实失败会清理对应旧详情、清单和进度，`AbortError` 不展示为网络错误，也不回退到 Fixture。
- [x] Planning 相关定向测试 3 个文件、`36/36` 通过；全量前端测试 `66` 个文件、`474/474` 通过。
- [x] 本批次没有新增后端路由或虚构 DTO；Figma 继续作为颜色、字体、布局和交互参考，不执行 105 个画板全量像素差异或花瓣像素对比。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍未提供，资源登记继续保持 `BLOCKED`。

## 2026-09-14 Profile Memories 请求生命周期收口

- [x] `GET /api/memories` 增加可选 `AbortSignal`；Profile `RealMemoriesTab` 使用请求序号和 AbortController，刷新、重复读取和组件卸载不会让旧响应覆盖当前页面。
- [x] 记忆读取失败时清空过期列表并展示后端错误消息和“重试”入口，不把旧数据继续当作当前服务端事实，也不回退到 Fixture。
- [x] `POST /confirm`、`PATCH` 和 `DELETE` 记忆操作均支持取消信号；组件卸载或取消后不再展示伪造的成功提示，成功后继续重新读取服务端列表。
- [x] 前端定向验证：`ProfilePage.real.test.tsx`、`memoryService.test.ts` 共 `4/4` 个用例通过；全量 Vitest `67` 个文件、`477/477` 个用例通过。
- [x] `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`npm.cmd run build` 和 `git diff --check` 均通过。
- [x] 实现提交：`a003925d fix(frontend): close profile memory lifecycles`；本批次没有新增或修改 Java 后端接口。
- [ ] 本批次不执行 105 个画板全量 PNG/diff，不执行花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 2026-09-14 Admin 核心读取请求生命周期收口

本大点只补齐 Admin 核心真实读取页面的请求生命周期，不新增后端路由、不修改 HTTP/SSE 协议，也不把前端测试当作后端生产环境可用性证明。

- [x] `loadAdminDashboard`、`loadAdminToolRegistry`、`loadAdminQuery`、`loadAdminTraceDetail`、`loadAdminUsersPage` 和 `loadAdminUserDetail` 支持可选 `AbortSignal`，信号实际传递到 `apiClient` 的 `fetch` 请求。
- [x] Admin Overview、Users、Tools Registry、Tool Calls、Runs 和 Trace 详情使用请求序号与 AbortController；筛选、分页、重试、刷新、用户切换和组件卸载不会让旧响应覆盖当前页面。
- [x] 真实请求取消不会进入错误态；失败时清空过期列表或详情，页面提供加载、空数据、错误和重试状态，不回退到 Figma Fixture。
- [x] `UsersSection` 接收 Admin 父级 `refreshNonce`；真实用户状态或会话操作完成后会重新读取服务端用户列表，不把本地乐观状态作为最终事实。
- [x] 定向验证：Admin service、Overview/Users/Tools/Runs 相关测试共 `6` 个文件、`22/22` 个用例通过，包含概览失败重试、工具注册表和运行治理卸载取消验证。
- [x] 全量前端验证：Vitest `67` 个文件、`482/482` 个用例；`npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`npm.cmd run build` 和 `git diff --check` 通过。
- [x] 新增代码注释使用中文；本大点未修改 Java 后端代码或接口协议。
- [ ] 后端鉴权、数据库、外部依赖、幂等、revision 和生产部署联调仍需按环境单独验收；不据此宣告后端生产闭环完成。
- [ ] 本大点不执行 105 个画板全量像素差异、不执行花瓣像素对比；Figma 继续作为颜色、字体、布局和交互参考，iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 2026-09-15 Admin 删除资源、模型治理与操作审计生命周期收口

- [x] `DeletedSection` 真实读取增加 AbortController、请求序号、加载态、失败清空和重试；真实恢复成功后不本地删除行，等待 Admin 父级刷新后重新读取服务端列表。
- [x] `ModelGovernanceSection` 刷新会取消旧请求并清空旧治理数据；读取失败不保留过期服务端结果，AbortError 不进入错误态，价格/预算/供应商操作继续以服务端重新读取为准。
- [x] `OperationAuditSection` 的审计列表、报告和导出状态读取均支持取消与请求序号；导出轮询覆盖 `queued`、`running`、`completed`、`failed`、`expired` 和 `download_consumed`，下载后重新读取服务端状态，不本地伪造消费成功。
- [x] 组件卸载、刷新、筛选切换和导出任务替换会清理旧请求与轮询定时器；真实模式失败展示后端错误并提供重试，不回退到 Figma Fixture。
- [x] 前端定向验证：8 个测试文件、`29/29` 通过；全量 Vitest：69 个测试文件、`488/488` 通过。
- [x] 后端定向验证：Application `18/18`、API `14/14`，合计 `32/32` 通过，Maven reactor `BUILD SUCCESS`，覆盖导出、运营查询、审计报告和模型治理契约。
- [x] `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`npm.cmd run build` 和 `git diff --check` 通过。
- [ ] 后端真实数据库、外部依赖、部署鉴权和生产环境联调仍需按环境单独验收；本批次不把页面测试等同于生产闭环。
- [ ] 本批次不执行 105 个画板全量像素差异、不执行花瓣像素对比；iconfont 实体包、CSS/Unicode 映射、来源和许可证继续保持 `BLOCKED`。

## 2026-09-15 Diet Records 与 Intake Analysis 写操作生命周期收口

- [x] `POST/PATCH/DELETE /api/food-logs` 和恢复接口继续由 `foodLogService`、`DietRecordsPage` 消费；新增、编辑、删除、恢复均传递独立 `AbortSignal`。
- [x] `POST/PATCH/DELETE /api/composite-dishes` 继续由 `compositeDishService`、`DietRecordsPage` 消费；保存和删除保留服务端 revision、幂等键和冲突处理。
- [x] 关闭写入对话框、收起回收站、组件卸载和写操作替换会取消旧请求；旧响应不会覆盖当前页面。
- [x] `GET /api/nutrition-analysis` 的 today/7d/30d 读取生命周期完成回归验证，未新增接口或改变响应契约。
- [x] 本批次定向验证为 `5` 个测试文件、`39/39` 通过；没有将 Fixture 成功结果当作真实接口完成证据。
- [ ] 本批次未修改 Java 后端接口；生产环境数据库、鉴权、外部依赖和部署联调仍需单独验证，iconfont 资料不足继续为 `BLOCKED`。

## 2026-09-15 Meal Planning 写操作生命周期收口

- [x] `POST/PATCH/POST(validate|save)/DELETE/POST(restore) /api/meal-plans` 继续由 `planningService`、`PlanningPage` 消费；计划写操作均传递独立 `AbortSignal`。
- [x] 计划草稿保存、校验、保存、删除和恢复保留后端 revision、幂等键和真实响应，页面切换或卸载后不会写回旧计划。
- [x] Agent 生成草稿的 Session 创建和首条消息提交共享取消信号；没有将接受响应当作 Agent 生成完成。
- [x] 购物清单生成和购物项更新继续由 `PlanSidebar` 使用真实接口、服务端回读和既有取消机制。
- [x] 本批次定向验证为 `2` 个测试文件、`22/22` 通过；没有将 Fixture 成功结果当作真实接口完成证据。
- [ ] 本批次未修改 Java 后端接口；生产环境数据库、鉴权、外部依赖和部署联调仍需单独验证，iconfont 继续为 `BLOCKED`。

## 2026-09-15 Knowledge 请求生命周期收口

| 接口 | 前端 service / 页面 | 状态与证据 |
| --- | --- | --- |
| `POST /api/knowledge-base/search` | `searchKnowledge`、`KnowledgePage` | `REAL_CONNECTED`；支持 signal、空查询、旧查询隔离、错误码展示和真实 citation 渲染 |
| `GET /api/admin/queries/knowledge` | `loadAdminKnowledge`、`KnowledgeSection` | `REAL_CONNECTED`；分页、关键词、状态、可见性筛选和请求取消已接入 |
| `POST /api/admin/knowledge` | `uploadKnowledgeDocument`、`KnowledgeSection` | `REAL_CONNECTED`；单文件 multipart 上传支持取消和重复提交锁 |
| `POST /api/admin/knowledge-documents/upload-batches` | `uploadKnowledgeBatch`、`KnowledgeSection` | `REAL_CONNECTED`；来源、版本、许可证和幂等字段保持既有契约 |
| `PATCH /api/admin/knowledge/{id}/status` | `updateKnowledgeStatus`、`KnowledgeSection` | `REAL_CONNECTED`；Fixture 路径和真实路径分离，操作信号可取消 |
| `POST /api/admin/knowledge-documents/{id}/publish|disable|restore|delete` | `changeKnowledgeVisibility`、`KnowledgeSection` | `REAL_CONNECTED`；统一确认状态机，成功后由服务端列表回读 |
| `GET /api/admin/knowledge-upload-batches/{batchId}` | `loadKnowledgeBatch`、`BatchProgress` | `REAL_CONNECTED`；批次详情请求支持取消、错误码和迟到响应隔离 |
| `GET /api/admin/knowledge-upload-batches/{batchId}/events` | `streamKnowledgeBatch`、`BatchProgress` | `REAL_CONNECTED`；Last-Event-ID、多标识去重、重连上限、终态关闭和取消关闭已验证 |
| `POST /api/admin/knowledge-upload-batches/{batchId}/documents/{documentId}/retry` | `retryKnowledgeItem`、`BatchProgress` | `REAL_CONNECTED`；独立操作状态、取消和服务端批次刷新 |
| `POST /api/admin/knowledge-upload-batches/{batchId}/documents/{documentId}/reindex` | `reindexKnowledgeItem`、`BatchProgress` | `REAL_CONNECTED`；独立操作状态、取消和服务端批次刷新 |

- [x] Knowledge 本批次定向测试 `7` 个文件、`27/27` 通过；`typecheck`、`lint`、`format:check`、`build`、`git diff --check` 通过。
- [ ] `REAL_CONNECTED` 表示前端已调用现有后端契约并正确处理页面生命周期，不表示目标生产环境的数据库、RAG、对象存储、权限和部署联调已经完成。
- [ ] 不执行 105 个 Figma 画板全量 PNG/diff 或花瓣像素对比；iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证不足时继续保持 `BLOCKED`。

## 2026-09-15 Admin 写操作请求生命周期证据

| 接口范围 | Service / 页面 | 当前结论 |
| --- | --- | --- |
| Admin 用户、工具、资源恢复、DLQ | `adminService`、`UsersTab`、`ToolsTab`、`DeletedResourcesTab`、`RunsTab` | 已接入统一取消、迟到响应隔离和服务端刷新 |
| Retention 详情、Preflight、审批和 Legal Hold | `adminService`、`RetentionTab` | 已支持读取取消、写入取消和卸载清理 |
| Model Governance 供应商、模型、路由、价格、预算 | `adminService`、`ModelGovernanceTab` | 已支持写入取消及刷新请求信号继承 |
| Knowledge 状态与批次操作 | `adminService`、`KnowledgeTab` | 已支持中心取消信号，旧操作不会伪造成功 |

- [x] 定向验证：13 个测试文件、79/79 个用例通过。
- [x] `typecheck`、`lint`、`format:check`、`build`、`git diff --check` 通过。
- [ ] 本记录只证明前端已消费现有接口并正确处理请求生命周期，不证明后端生产部署、数据库和外部依赖联调；iconfont 继续为 `BLOCKED`。
