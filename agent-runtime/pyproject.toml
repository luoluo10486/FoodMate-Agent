[project]
name = "foodmate-agent-runtime"
version = "0.1.0"
description = "FoodMate Python Agent Runtime V1"
requires-python = ">=3.11"
dependencies = [
    "cryptography==49.0.0",
    "redis==6.4.0",
    "rocketmq-python-client==5.1.1",
    "langgraph==0.6.11",
]

[tool.setuptools]
py-modules = ["runtime_server", "runtime_env", "mq_runtime", "agent_core", "model_provider", "proposal_protocol", "knowledge_rag"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: requires explicitly configured external services or credentials",
]
